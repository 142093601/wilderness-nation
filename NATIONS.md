# Statecraft · 国家与邦交系统（设计文档 v1）

> **这个 mod 是什么**：《荒野建国》的**配套 mod**，负责"这个世界的政治"——
> 国家的成长与推演、国家之间的战争与结盟、各国怎么看待你们、你们怎么跟他们打交道、
> 以及怎么把他们收进来。
>
> **中文注解**：邦交（国与国之间的往来）。mod id 用 `statecraft`（MC 命名空间只允许
> 小写字母/数字/下划线，中文进不去）；**玩家看到的一切界面、周报、物品名全部是中文**。
>
> **状态**：设计已定案（2026-09-19），**尚未开始写代码**。实现计划见后续的写计划环节。

---

## 一、定位与边界

### 做

- 国家的**状态与推演**（规模、发展度、政治倾向、军力、国库、互相之间的态度）
- **信息流**：可建造的「望楼/情报站」+ 情报册，收各国动态，出周报（《文明》式的抽象表现，不做世界内扩张模拟）
- **外交**：6 个动作，状态机驱动
- **吞并**：打下一国的都城即吞并（唯一的实体化战场）
- **把状态落成世界里的东西**：都城、守军、可交易的商品

### 不做（明确交给谁）

| 不做 | 交给 |
|---|---|
| 战斗、兵种、攻城器械、成建制行军 | `hundred-years-warfare`（已装：103 个实体、两套敌对势力、攻城器械、群体寻路）|
| 领地规则与权限 | `OPAC`（已装）|
| 尸潮 / 夜袭等威胁事件 | `The Hordes` + `Undead Nights`（已装）|
| 建筑美术 | 复用已装建筑 mod 的积木 + 生成器组合（不手搭）|
| 世界生成期的结构注入 | **不做**——国家都在运行时实体化，因此**不需要重建世界** |

---

## 二、与现有系统的接口

| 接口 | v1 实现 | 以后 |
|---|---|---|
| **读时代** | `EraProvider` 接口。v1：内部"累计在线时间"计数器 + 命令强制推进；时代时长取 `DESIGN.md` §17 的 12/20/25/25/30/38 小时 | 日历/国力系统做出来后换实现类，`core` 不动 |
| **写世界** | 运行时放置结构、刷 HYW 兵种实体、开交易 | 都城升级套件 |
| **吞并落账** | 生成「占领碑」+ 事件入国史 + 都城登记进玩家属地清单 | 接管进 OPAC 领地（**需先核实 OPAC 是否提供可调用 API**，v1 不依赖它）|
| **HYW** | 只做"点名"：某国用哪套皮肤包 / 哪个兵种档位（I~IV） | 可让 HYW 部队承接国家的出击 |
| **命令通道** | 只用于调试与验收 | 已知本机 RCON 通道不可靠（见第八节风险），故**一切自动化测试不依赖命令** |

---

## 三、术语

| 术语 | 含义 |
|---|---|
| **国家（Nation）** | 一个政治实体。有国名、文化、都城、规模、发展度、政治倾向、军力、国库、态度、状态 |
| **规模（size）** | 1~10 的抽象值，表示国家有多大。**不进世界**，只在数据与周报里存在 |
| **发展度（development）** | 0~100。与**规模反比**：大国低、小国高 |
| **文化（culture）** | 决定外观与兵种档位，复用 HYW 现成势力（`bandit` / `desert_raider`）+ 可扩自造 |
| **都城（capital）** | 每国一座，**唯一必须实体化的东西**（因为吞并要有一个真能打进去的地方）|
| **关系（Relation）** | 两国之间的状态：态度值、关系状态（和平/战争/同盟/停战冷却）、最近一次互动 |
| **事件（Event）** | 一条带时代戳的记录：谁对谁做了什么 + 中文摘要。周报、情报站界面、国史共用这一个数据源 |
| **未读队列** | 玩家还没看到的事件。玩家回来时按顺序呈现，不刷屏 |
| **时代档位** | 六档（对应六个时代）。时代越高，全局数值系数越高 |

---

## 四、架构

三层，**依赖方向单向向下**（上层可以调下层，下层不许 import 上层）：

```
┌─ presentation（薄）─────────────────────────────────────┐
│  望楼方块 · 情报册物品 · 自定义界面(网络包) · 周报成书          │
├─ integration（薄）──────────────────────────────────────┤
│  EraProvider · 世界写入(结构/守军/交易) · HYW 适配 · OPAC 适配  │
├─ core（厚，纯 Java，零 Minecraft 依赖）───────────────────┤
│  NationState · WorldState · SettlementEngine(结算器)      │
│  DiplomacyStateMachine · EventFactory · 文本模板 · seed 随机 │
└────────────────────────────────────────────────────────┘
```

**为什么这样切**（这是"质量"和"少开游戏验证"的关键）：

- `core` 装着**全部规则**——时代结算、国家间战争推演、外交状态机、吞并判定、周报文本。
  它不 import 任何 Minecraft 类，所以**用 JUnit 就能把六个时代一次跑完**：
  断言"到时代 5 时累计战争次数""灭亡的国家数""周报条数""同 seed 复现一致"。
- `integration` / `presentation` 只把 `core` 的状态翻译成世界里的东西。
  **预计 85~90% 的逻辑不用开游戏就能验完**。
- 换 MC 版本或换加载器时，`core` 一行不动。

---

## 五、数据模型

### Nation

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 稳定标识，生成后不变 |
| `name` | string | 中文国名（从名字池里取，避免随机拼音）|
| `culture` | enum | `bandit` / `desert_raider` / 自造（决定外观与兵种档位）|
| `capital` | 坐标 + 维度 | 都城位置 |
| `size` | 1~10 | 规模（抽象）|
| `development` | 0~100 | 发展度，与 size 反比 |
| `traits` | 结构 | 政治倾向 `stance`（−100 主和 … +100 主战）+ 尚武 / 富庶 / 记仇 / 开放（各 0~100）|
| `military` | float | 抽象军力 |
| `treasury` | float | 抽象国库 |
| `status` | enum | `alive` / `dead` / `vassal`（v1 只用前两个）|
| `attitudeToPlayer` | −100~100 | 对玩家方的态度 |
| `met` | bool | 玩家是否已接触过（未接触的国家在情报里显示为"传闻"）|

### Relation（A↔B）

| 字段 | 说明 |
|---|---|
| `attitude` | −100~100 |
| `state` | `peace` / `war` / `alliance` / `truce`（停战冷却） |
| `warScore` | 战争中的累计战果（正数 = A 占优）|
| `lastEvent` | 最近一次互动的事件 id |

### Event

| 字段 | 说明 |
|---|---|
| `era` / `seq` | 时代序号 + 结算序号（决定顺序，也是随机种子的一部分）|
| `type` | `war_declared` / `battle` / `alliance` / `truce` / `trade` / `collapse` / `player_action` … |
| `parties` | 参与国 id（可为空 = 世界事件）|
| `text` | **中文摘要**（由模板生成，变量来自状态）|
| `visible` | 玩家是否立刻可见（未接触的国家的事只有传闻级）|

### WorldState

`{ schemaVersion, seed, eraIndex, seq, nations[], relations{}, events[](保留 500 条，更早的归档), unread[](上限 200), playerLedger{} }`

- **序列化**：NeoForge `SavedData` 存 NBT；`schemaVersion` 参与迁移判定。
- **随机**：所有随机数取自 `hash(seed, seq, 用途标签)` → **同 seed 同输入必得同结果**。

---

## 六、开局生成

| 项 | 默认值（走配置）|
|---|---|
| 国家数量 | **12**（可配 8~16）|
| 国家间距 | **国家与国家之间最小 1500 格**（用松弛/最小间距采样布点）；**不判定与出生点的距离** |
| 出生点附近 | 距出生点最近的那个国家**强制 `stance ≤ 0`（主和）且开局对玩家中立**，避免打乱时代 1 的求生节奏 |
| 规模 | 每国随机 1~10，整体分布均匀 |
| 发展度 | `dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))` → **大而落后、小而先进** |
| 政治倾向 | `stance = clamp(−100, 100, 60 − dev + rng(−30,+30))` → **落后更主战、先进更主和** |
| 文化 | 按 `culture` 池分配（默认只用已装 HYW 的两套势力外观）|
| 名字 | 从中文名字池取（按文化分池），保证全局唯一 |

---

## 七、结算模型

**时间口径**：与全包一致——**按"累计在线时间"**，不跟世界时间、也不跟现实时间。

### 三种触发

| 触发 | 频率 | 内容 |
|---|---|---|
| **时代推进**（主节拍） | 6 次 | 全体成长（development / military / treasury）+ 生成 1~3 条国家间事件 + 更新对玩家态度 + 出一封「天下大势」总报 |
| **周期小结算** | 每 **2 小时**累计在线时间 | 推进进行中的战争、停战冷却、态度向 0 回归 |
| **玩家动作** | 即时 | 宣战/求和/结盟/通商/求援/朝贡 立刻结算并回应 |

### 时代系数

| 时代 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 全局系数 | 1.00 | 1.15 | 1.30 | 1.50 | 1.75 | 2.00 |

作用于军力/国库增长与兵种档位（I~IV）。

### 国家之间怎么打（抽象、确定、不发生成实体）

1. 开战：由 `stance`、`attitude`、邻接关系共同决定概率（主战国更容易宣战）。
2. 每步（一次小结算）：`战力 = military × (1 + stance/200) × rng(0.85, 1.15)` 比较，胜者推进 `warScore`。
3. 产出 `battle` 事件（中文摘要，例如"赤沙国攻破白岩国一处边镇"）。
4. 灭亡：`warScore` 落后超过阈值且 `development < 25` 且连输 ≥3 步 → 出 `collapse` 事件，`status = dead`。
5. **不生成军队、不改世界方块。**

### 玩家不在线

**不推进。** 玩家回来时取"未读队列"按顺序呈现；未读超过 200 条时折叠为"更早的 N 条已归档"（**已归档 ≠ 丢失**：事件本体仍留在 `events[]` 里，只是不再逐条推送）。

---

## 八、玩家侧

### 信息站升级路径（决定：早期物品 → 后期可建造）

| 阶段 | 载体 | 能力 |
|---|---|---|
| 时代 1~2 | **情报册**（物品） | 看已接触国家的基本信息、读未读事件 |
| 时代 3+ | **可建造的「望楼」**（方块，可升级） | 更细情报（军力/态度/正在进行的战争）+ **主动遣使**发起外交 |
| 时代 5+ | 望楼 II | 缩短情报延迟、增加可同时追踪的国家数 |

### 界面

列表（国家 + 状态徽记）→ 详情（属性、对玩家态度、最近 5 条事件、可发起的外交动作）→ 未读队列。

**实现顺序**：v1 先用**成书**把数据与规则跑通 → 再补自定义界面（网络包）。
两条路都保留，但**规则与数据先对**。

### 推送

- 出事**即时**（聊天 + 入档）
- 时代末一封「天下大势」总报

### 外交 6 动作

| 动作 | 前置 | 后果 |
|---|---|---|
| **宣战** | 已接触；非同盟 | 进入 `war`；对方态度大跌；可攻打其都城 |
| **求和** | 处于 `war` | 按 `warScore` 判定成败；成功转 `truce`（停战冷却）|
| **结盟** | 态度 ≥ +40；非战争 | 转 `alliance`；互相在战争中可能驰援（抽象：提升彼此战力）|
| **通商** | 态度 ≥ 0 | 提升双方 treasury；解锁该国的可交易商品（在都城交易）|
| **求援** | 处于 `alliance` | 按对方军力与态度决定是否驰援：抽象上提升你方战力；**可选**派出援军实体（复用 HYW 兵种，不新增内容）|
| **朝贡** | 你方发展度高于对方 | 支付资源换态度与"不侵犯"承诺（v1 只做态度与承诺）|

所有动作都是 `core` 里的状态机转移（前置条件 + 成功率 + 后果）；界面只是把它暴露出来。

### 吞并（唯一的实体化战场）

对某国宣战 → **打它的都城**（战斗交给 HYW）→ 都城守军被清空 → 触发占领：

1. 生成「占领碑」方块
2. 该国 `status = dead`，从名单消失
3. 都城坐标登记进玩家属地清单
4. `collapse` 事件入国史

> v1 **不依赖 OPAC**：接管进领地系统作为后续项（需先核实 OPAC 是否提供可调用 API）。

---

## 九、失败与边界

| 情形 | 处理 |
|---|---|
| **存档 schema 变更** | `schemaVersion` 参与判定；不匹配走迁移；最坏情况"重置国家系统"而**不让存档报废** |
| **结算途中崩溃** | 写事务日志（结算序号 + 输入哈希）；因确定性 + seed，重启后可从上一个完整结算重放 |
| **外部输入异常** | 配置与命令的所有数值做区间钳制 + 日志，绝不写入非法状态 |
| **国家灭亡 / 玩家占领** | 事件与国史保留；关系矩阵清理该国的行/列；`dead` 国家不再参与结算 |
| **性能** | 关系矩阵 O(n²)：12 国 = 66 对，可忽略；实体化只在玩家靠近时发生，一次一组结构、守军上限 **40** 实体 |
| **重复实体化** | 每次实体化记 `materializedAt(seq)`，同一结算序号不重复放置 |

### 已知风险（本机实测）

- **RCON 命令通道不可靠**：本机实测项目自带客户端与手写裸客户端都只拿到空响应，且日志无命令痕迹。
  → 所以**所有自动化测试都跑在 `core` 层（纯逻辑）**，命令只留作人工调试。
- **HYW 的实体能力已核实为静态证据**（103 个注册实体、两套敌对势力、攻城器械、群体寻路），
  但"运行时刷出并对玩家作战"的**活体验证尚未完成**，是本 mod 的**第一个验证项**。

---

## 十、测试与验收

### JUnit（主力，离线，天天跑）

| 用例 | 断言 |
|---|---|
| 六时代快进 | 一次跑完 6 个时代不抛异常；事件数/周报数落在预期区间 |
| seed 复现 | 同 seed 同输入 → 两次运行状态逐字段相等 |
| 大小与发展度反比 | 生成 1000 次，相关系数显著为负 |
| 战争推演 | 强打弱必胜概率高；灭亡条件边界（dev=24 vs 25、连输 2 步 vs 3 步）|
| 外交状态机 | 每个动作的前置条件被违反时**必须拒绝**；状态转移合法 |
| 态度漂移 | 回归速率与事件加成符合表 |
| 未读队列 | 200 条上限与折叠行为 |
| schema 迁移 | 旧版本快照能迁到当前版本；损坏数据不崩溃 |
| 周报文本 | 快照测试（中文模板不漏变量、不出现 `%s`）|

### 游戏内（攒成一次，不零散占用时间）

1. 望楼建造与界面手感
2. 靠近都城时实体化的观感
3. 一次完整链路：宣战 → 打都城 → 占领

### 交付物

- mod 源码（`mods/statecraft/`，NeoForge MDK + Gradle，Java 21）
- 数据包 / 资源包（名字池、事件模板、外观映射）
- README：**怎么加一个国家 / 一条条约 / 一个事件**
- 配置项清单与默认值说明

---

## 十一、里程碑（详细实现计划另行编写）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1** | `core` 骨架：数据模型 + 生成 + 结算器 + 外交状态机 + 文本模板 | JUnit 全绿（含六时代快进）|
| **M2** | 持久化（SavedData + schema 版本 + 事务日志）+ 命令 | 存档往返一致；崩溃重放 |
| **M3** | 情报册 + 望楼 + 周报（成书） | 游戏内可读，内容来自真实状态 |
| **M4** | 都城实体化 + 守军 + 占领 → 吞并 | 完整链路真机跑通一次 |
| **M5** | 自定义界面（网络包）+ 望楼升级 + 打磨 | 手感验收 |
| **后续** | OPAC 接管、附庸/朝贡深度、自造文化外观、AI 史官接周报 | — |

---

## 十二、明确不在 v1 范围

- 世界内的扩张模拟（**按设计决策，永久不做**——用信息流表现）
- 附庸国的内政与朝贡结算深度（v1 只有"灭亡"）
- 都城建筑的时代升级套件（v1 只做"放一次结构 + 刷守军"）
- 接入 AI 史官（v1 周报用模板生成）
- 接管进 OPAC 领地（需先核实 API）
- 多文化自造外观（v1 复用 HYW 现成的两套）
