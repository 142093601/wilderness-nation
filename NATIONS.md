# Statecraft · 国家、城邦与邦交系统（设计文档 v1.2）

> **这个 mod 是什么**：《荒野建国》的**配套 mod**。它管两件事：
> **① 这个世界的政治**——国家的成长与推演、国家之间的战争与兼并、各国怎么看待你们、外交、以及夺城吞并；
> **② 你们城里的人**——建筑怎么来（图纸 → 图纸加农炮）、以及每座建筑里那个**绑定它的村民**。
>
> **mod id**：`statecraft`（MC 命名空间只允许小写字母/数字/下划线）。
> **玩家看到的一切界面、周报、物品名、村民对话全部是中文。**
>
> **状态**：设计定案（2026-09-19）。**尚未开始写代码**，下一步是写实现计划。

---

## 附：v1.1 → v1.2 修订清单

这一版把你新提的两块设计并了进来，并收掉了上一版悬着的问题。

| # | 变更 | 说明 |
|---|---|---|
| 1 | **新增「建筑与村民」**（第十节） | 建筑 = 图纸（壳）+ **绑定村民**（核）。这正是 `DESIGN.md` §三"**图纸是壳，模块是核**"的落地：**把"模块"实现成一个 NPC** |
| 2 | **村民不劳动**（边界写死） | 只做"人气 + 界面"。不盖房、不生产、不搬运、不参战 → 本 mod **不写任何劳动 AI**；建造仍由玩家的**图纸加农炮**完成，所以 **`DESIGN.md` 的"建造不外包"原则没有被动过**（我上一版误读成"包工头替你盖"，那个担心作废）|
| 3 | **建筑工 NPC 卖图纸** | 图纸是商品。`.nbt` 随包分发（`Portable Blueprints` 做服务端分发与材料代扣），建筑工卖的是**"图纸授权/工料单"**（物品）；买了才允许在自己领地内打这张图纸 |
| 4 | **新增「夺城环节」**（第十一节） | 玩家发起吞并 → 事件化攻城：**百姓隐藏休眠** + 临时刷守城军 + 刷指挥官 → 完成条件 = **插旗 + 指挥官已死** |
| 5 | **术语分开：灭亡 vs 吞并** | 国家对国家 = **灭亡**（纯推演，零实体）；玩家对国家 = **吞并**（走一次夺城环节）|
| 6 | **国家兼并做"吸收"** | 胜者 `size` 变大 → 因 size 与发展度反比，**它发展度会掉**（"打赢了反而变落后"）；配 **扩张疲劳** 防赢家通吃 |
| 7 | **mod 的三条 AI 为零** | 不写劳动 AI · 不写战斗 AI（守城军是刷 HYW 现成单位）· 不跑扩张模拟。全部集中在"结算 + 事件 + 生成" |
| 8 | 都城**有人** | 都城按规模生成若干**平民村民**（人气，不绑定功能），夺城时一并隐藏 |
| 9 | 架构补一层边界 | 环境判定（屋顶/封闭度/体积/领地）的**规则在 `core`（可 JUnit）**，世界读数由 `integration` 提供"环境快照" |
| 10 | 里程碑重排 | M3 加"建筑工 + 情报站职员"，M4 改为**夺城环节**，M5 起才是内容层（其余建筑的职员） |

---

## 一、定位与边界

### 做

- 国家的**状态与推演**（规模、发展度、政治倾向、军力、国库、互相之间的态度）
- **信息流**：情报册 + 可建造的**情报站**，收各国动态、出周报（《文明》式抽象表现，**不做世界内扩张模拟**）
- **外交 6 动作**：状态机驱动
- **国家与玩家的战争**：国家会宣战、会派兵来打你们
- **夺城吞并**：唯一的实体化战斗，且有始有终（事件化，不常驻）
- **建筑与村民**：图纸 → 图纸加农炮打出壳 → 顺带生成**绑定村民**（人气 + 界面）

### 不做（明确交给谁）

| 不做 | 交给 |
|---|---|
| 战斗、兵种、攻城器械、成建制行军 | `hundred-years-warfare`（已装，**软依赖**）|
| **施工/劳动 AI** | **不需要**——建造是玩家用 Create 的**图纸加农炮**逐块打出（材料真实消耗）|
| 领地规则与权限 | `OPAC`（已装）|
| 尸潮 / 夜袭等威胁事件 | `The Hordes` + `Undead Nights`（已装，可选联动）|
| 建筑美术 | 图纸（`.nbt`）随包分发；生成器与手搭结合 |
| 世界生成期的结构注入 | 不做——都在**运行时**实体化，**不需要重建世界** |

**三条 AI 为零**（这是本 mod 的性能底线）：不写劳动 AI · 不写战斗 AI · 不跑扩张模拟。

---

## 二、与现有系统的接口

| 接口 | v1 实现 | 以后 |
|---|---|---|
| **读时代** | `EraProvider`，数据来自 `eras.json`（第七节）。v1：内部"累计在线时间"计数器 + 命令强制推进 | 日历/国力系统出来后**换实现类**，`core` 不动 |
| **建造（已有链路）** | 玩家用 **Create 的图纸加农炮（Schematicannon）** 打图纸；`create-pattern-schematics` 提供图案；`portable-blueprints` 做服务端分发与材料代扣；`effortless-building` 做建筑辅助 | — |
| **图纸分发** | `.nbt` 随包放进 `schematics/`；建筑工卖**授权物品** | 若 `Portable Blueprints` 支持"图纸即物品"，可改走那条路（**能力待核实**）|
| **写世界** | 生成建筑锚点、生成绑定村民、夺城时刷守军与指挥官、开交易 | 建筑升级套件 |
| **吞并落账** | 占领碑 + 事件入国史 + 都城进**玩家属地清单** | 接管进 OPAC 领地（**需先核实 API**，v1 不依赖）|
| **HYW（软依赖）** | 夺城时刷它的现成单位；国家用哪套皮肤/兵种档位 | 让 HYW 部队承接国家的出击 |
| **The Hordes（可选）** | 村民被感染的既有机制天然生效；"国家来袭"可配成一次 horde 事件 | — |
| **命令通道** | 只用于人工调试 | 已知本机 RCON 通道不可靠（第十五节），故**一切自动化测试不依赖命令** |

---

## 三、术语

| 术语 | 含义 |
|---|---|
| **国家（Nation）** | 政治实体：国名、文化、都城、规模、发展度、政治倾向、军力、国库、态度、状态 |
| **规模（size）** | 1~10。**不进世界**，但决定实体化规模（居民数、守军数、都城档位）|
| **发展度（development）** | 0~100，与**规模反比**：大国低、小国高 |
| **文化（culture）** | 决定外观与兵种档位（默认复用 HYW 的 `bandit` / `desert_raider`）|
| **都城（capital）** | 每国一座，**必须实体化**（夺城要有地方发生）|
| **都城居民** | 都城里的**平民村民**（按 size 生成），只提供"人气"，不劳动；夺城时隐藏 |
| **旗座** | 都城中心的一个方块，**事件期间**才可插旗 |
| **灭亡（collapse）** | **国家对国家**的结果：输家从名单消失。纯推演，**零实体** |
| **吞并（annex）** | **玩家对国家**的结果：走一次**夺城环节**，成功后都城进玩家属地 |
| **吸收** | 国家兼并后，胜者 `size` 变大、`dev` 重算（"打赢了反而变落后"）|
| **扩张疲劳** | 每吸收一次，胜者军力恢复变慢、被围攻概率上升（防赢家通吃）|
| **建筑（Building）** | 图纸（壳）+ **绑定村民**（核）。图纸是壳，模块是核（`DESIGN.md` §三）|
| **锚点方块** | 建筑定义里指定的那个方块（情报站 = 书桌）——**村民绑定它** |
| **村民 / 职员** | 绑定在建筑上的 NPC。**只做"人气 + 界面"**，不劳动 |
| **图纸授权** | 建筑工出售的物品。拿到后才能在领地内打这张图纸 |
| **国书（Letter）** | 国家主动递给玩家的可交互事件（条件 + 期限）|
| **玩家方（party）** | 3~7 人视为**一个政治实体**（"你们"），态度以方为单位 |
| **属地（domain）** | 被玩家夺下的原都城，登记在册（v1 只登记 + 界面可见）|

---

## 四、架构

三层，依赖单向向下。**本 mod 内部再分两个模块，但同一个 jar / 同一个仓库**（两者共用建筑-村民框架，拆开反而要维护跨 mod API）：

```
core（厚，纯 Java，零 Minecraft 依赖）
├─ politics/  NationState · WorldState · SettlementEngine · DiplomacyMachine
│             LetterFactory · EventFactory · 文本模板 · 地名池 · seed 随机
└─ civic/     BuildingDef · 环境判定规则 · 幂等决策 · 村民定义与绑定状态
             （判定规则在 core；世界读数由 integration 递进来 → core 仍可 JUnit）

integration（薄）
├─ EraProvider · 世界写入(锚点/村民/守军) · 环境快照采集 · HYW 适配(可缺)
├─ 图纸链路对接（Schematicannon / Portable Blueprints）
└─ OPAC 适配(后续) · The Hordes 联动(可选)

presentation（薄）
├─ 建筑工 NPC（交易界面）· 村民对话（原版界面）
├─ 情报册物品 · 周报成书 · 图纸授权物品
└─ 自定义界面（M5，且很可能不需要）
```

**为什么这样切**：
- 全部规则（时代结算、国家战争、吸收与疲劳、外交状态机、**环境判定与幂等决策**）都在 `core`，用 JUnit 就能跑完几个时代。
- `integration` / `presentation` 只把状态翻译成世界里的东西。
- 玩家侧的界面几乎全部**复用原版**（村民交易/对话），所以 UI 成本极低——这是"村民承载功能"这个设计最大的红利。

---

## 五、数据模型

### Nation

| 字段 | 说明 |
|---|---|
| `id` / `name` / `culture` | 标识、中文国名、文化（决定外观与兵种档位）|
| `capital` | 都城坐标 + 维度 |
| `size` 1~10 / `development` 0~100 | 规模 / 发展度（**反比**）|
| `stance` −100~100 | 政治倾向（主和 ↔ 主战）|
| `traits` | 尚武 / 富庶 / 记仇 / 开放（0~100）|
| `military` / `treasury` | 抽象军力 / 国库 |
| `status` | `alive` / `dead` |
| `met` | 玩家是否已接触（未接触只显示"传闻"）|
| `attitudeToParty` | 对玩家方的态度 |
| `partyStatus` | 对玩家方的外交状态：`NEUTRAL` / `WAR` / `ALLIANCE` / `TRUCE`。**实现更正**：原文只有一个布尔 `warOnParty`，但 §11.2 还要表达"同盟"与"停战/不侵犯"，加两个布尔就拦不住"既同盟又交战"这种非法组合；枚举让四种状态先天互斥 |
| `partyStatusUntilSeq` | 停战 / 不侵犯承诺的期限（只有 `TRUCE` 才有，其余状态恒为 0）|
| `partyWarScore` | 与玩家方的战果，正数 = 玩家占优（原文 `warOnParty` 那句"（含战果）"落在这里）|
| `fatigue` | **扩张疲劳**累积（影响军力恢复与被围攻概率）|
| `absorbedCount` | 吸收过几个国家（用于叙事与疲劳）|
| `traits` | ⏸ **刻意未实现**：§八/§十二 里没有任何规则读它；等有规则用它时再加（那时正好走一次存档迁移）|

### Relation（A ↔ B）
`{ attitude, state(peace/war/alliance/truce), warScore, lastEvent, truceUntilSeq }`
**实现更正**：另加了 `losingStreak`（§八.4 的"连输 ≥3 步"要有地方记，而原字段表里没有）；
`lastEvent` 没有实现——事件本身已经按序存在 `events[]` 里，再存一个"最后事件"是重复，等真有规则读它再加。

### Building（civic）

| 字段 | 说明 |
|---|---|
| `id` / `type` | 标识 / 建筑类型（`intel_station` / `warehouse` / …）|
| `anchor` | 锚点方块坐标 |
| `level` | 建筑等级（升级件提升）|
| `staffUUID` | 绑定的村民（可空）|
| `materializedAt` | 上次实体化序号（**幂等**）|
| `staffDeadAt` | 村民死亡/感染的时间戳（用于冷却与"重新雇用"）|

**实现说明（2026-09-20）**：`buildings[]` 已进 `WorldState`；锚点拆成嵌套的 `Building.Anchor{dimension,x,y,z}`；
另外**多一个字段** `materializedAtHours` —— 因为 §十二 的节流是"两次间隔 ≥5 分钟"，而 `materializedAt`（序号）一步就是 2 小时，**用序号表达 5 分钟会差 24 倍**；
序号管"同一 `seq` 不重复生成"，小时管"间隔 ≥5 分钟"。也不用系统真实时间（那会让世界在没人玩时自己往前走，§七 禁止）。

### Event
`{ eraId, eraOrdinal, seq, type, parties[], text(中文摘要), visible, read }`
`type` ∈ `war_declared` / `battle` / `absorbed` / `alliance` / `truce` / `trade` /
`collapse` / `raid` / `letter` / `player_action` / `siege_started` / `city_taken` /
`staff_lost` / `era_summary`

### WorldState
`{ schemaVersion, seed, eraId, eraOrdinal, seq, nations[], relations{}, events[](保留 500), unread[](上限 200), letters[], buildings[], domains[], playerLedger{} }`

- **序列化**：core 侧编解码成**中性数据树**（`StateNode`）——core 零 MC 依赖，薄层只做
  `StateNode ↔ CompoundTag` 的翻译（`SavedData` 存 NBT）；`schemaVersion` 参与迁移。
  缺版本号按 **v0** 处理；版本比代码新、缺某一级迁移、或内容损坏 → **只重置国家系统并记警告，绝不报废世界**。
- **seed**：由世界种子派生，首建时写入。
- **随机**：取自 `hash(seed, seq, 用途标签)` → **同 seed 同输入必得同结果**。

---

## 六、开局生成

| 项 | 默认（走配置）|
|---|---|
| 国家数量 | **12**（可配 8~16）|
| 国家间距 | 国家之间**最小 1500 格**；**不判定与出生点的距离** |
| 出生点附近 | **从候选里挑** `stance ≤ 0`（主和）的国家放到最近处（**不改它的数值**）；该国开局对玩家中立 |
| 规模 / 发展度 | `size` 均匀 1~10；`dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))` |
| 政治倾向 | `stance = clamp(−100, 100, 60 − dev + rng(−30,+30))` → **落后更主战、先进更主和** |
| 都城实体化 | 选址检查（非海洋 / 坡度 / 无既有结构 / 无玩家建筑与领地）→ 不合格换点（最多 8 次）→ 仍失败标 `undecided`，下一结算重试；**先平台化再放模板** |
| 都城居民 | 平民村民 `3 + size`（上限 12），只提供人气 |
| 文化与地名 | 按文化分配外观；每国一组**虚构地名**（都城/边镇/关隘/渡口）供周报取词 |
| 国名 | **优先本国文化池**（随机起点绕圈挑未用过的）；本国池用尽才**按文化表顺序回退**到别的文化池，全局不重名。所有文化的名字**去重后**总数 < 国家数时，在生成**入口**就报错（改 `nationCount` 或加名字）|

---

## 七、时代与结算（**数据驱动，不写死数量**）

### 时代表：`data/statecraft/eras.json`

```jsonc
[
  { "id": "landing",  "name": "落地", "ordinal": 0, "durationHours": 24,
    "coefficient": 1.00, "unlocks": ["intel_book"] },
  { "id": "founding", "name": "立国", "ordinal": 1, "durationHours": 40,
    "coefficient": 1.15, "unlocks": [] },
  { "id": "infra",    "name": "基建", "ordinal": 2, "durationHours": 50,
    "coefficient": 1.30, "unlocks": ["intel_station", "declaration"] }
  // …数量、名称、时长、系数、解锁项全部是数据。代码里不出现 "6"
]
```

> **时长口径（2026-09-19 改定）**：整条曲线**翻倍** → 24 / 40 / 50 / 50 / 60 / 76 ≈ **300 小时累计在线**。
> 理由：推进按**全服累计在线时间**算，只要有人在线就烧全服预算 —— 3~7 人时 150 小时太快。
> 口径的原始定义在 `DESIGN.md` §9.1（包侧文档），这里的数据文件是 mod 侧的权威副本。

- 解锁一律按 **`unlocks` 声明的 era id** 判定。
- 存档与事件**同时记** `eraId`（表意）与 `eraOrdinal`（排序）；**未知 id 归到最近 ordinal + 一次警告** → 时代表改了旧存档不废。
- **测试**：时代表换成 3 / 6 / 9 条各跑一遍整套结算 → 证明没有写死。

### 三种触发

| 触发 | 频率 | 内容 |
|---|---|---|
| **时代推进**（主节拍） | 由 `eras.json` 决定 | 全体成长 + 国家间事件 1~3 条 + 兼并结算 + 对玩家态度与战事推进 + 一封「天下大势」|
| **周期小结算** | 每 **2 小时**累计在线时间 | 推进战争（国家↔国家、国家↔玩家）、停战冷却、态度回归、国书到期、扩张疲劳衰减 |
| **玩家动作** | 即时 | 外交 6 动作、发起夺城 |

**时间口径**：按**累计在线时间**。**不在线不推进**；回来时看未读队列。

---

## 八、国家之间的战争、灭亡与吸收

1. **开战**：由 `stance`、`attitude`、邻接关系决定概率，受停战冷却限制。
2. **每步**：`战力 = military × (1 + stance/200) × fatiguePenalty × rng(0.85, 1.15)`。
3. **事件**：`battle`，中文摘要取**虚构地名池**（例："赤沙国攻破白岩国边镇『石口』"）。
4. **灭亡**（`collapse`）：`warScore` 落后超阈值 且 `development < 25` 且连输 ≥3 步 → `status = dead`。
   ⚠️ **实现更正（2026-09-19，实测）**：`development < 25` 在本节的生成公式下**不可达** ——
   `size = 10` 时 `dev = 41 + 抖动`，抖动 ±8 → 规模到顶的国家发展度恒在 **[33,49]**，
   而吸收只让规模变大、规模封顶后发展度不再降。所以全世界的 dev 下限是 **33**，
   阈值取 25 会让"灭亡"整条分支变成死代码（探针实测 20 seed × 6 时代灭亡 0 次）。
   实现取 **40**（"弱国"= 已膨胀到规模 9~10 的国家）。另有硬约束：
   **灭亡阈值必须小于吸收阈值**，否则被打垮的弱国在够到灭亡线前先被吸收掉。
5. **吸收**（`absorbed`）：胜者 `size += ceil(败者size × 0.5)`（上限 10）→ **`dev` 按反比公式重算**（于是"打赢了反而变落后"）；`fatigue += 1`、`absorbedCount += 1`。
6. **扩张疲劳**：`fatigue` 每点使军力恢复速率 ×0.85、被其他国家宣战概率 ×1.15；每小结算衰减 0.1，最低 0。
7. **零实体**：不生成军队、不改世界方块。这一切只出现在数字与周报里。
8. **灭亡或被吸收的都城留在世界里，但成为废墟**：它的居民与守军都不再生成，
   且**不能再被玩家吞并**（已经没有国家可吞了）。玩家走到那儿只会看到一座空城——
   这是设计要的效果（"旧世崩了"的调性），不是遗漏。

---

## 九、国家与玩家：压力、使节、国书

### 9.1 国家可以对玩家方宣战
`attitude ≤ −40` 且 `stance ≥ +40` 的国家最容易宣战（还要看发展度差与你们最近的扩张速度）。宣战 → 全服公告 + `war_declared` 事件。

### 9.2 宣战之后（v1 只做"袭击"）
每个结算周期按概率发动一次 `raid`：
- **实体化一支小队**（HYW 兵种，规模 `2 + floor(military/25)`，**上限 24**）
- 落点：**你们最近的活动区**（或你们的属地附近），**提前预警**（聊天 + 事件）
- 可选联动：若 `The Hordes` 在场且开启，可配成一次 horde 事件
- **HYW 缺席时降级**：不出实体，只出事件与损失

### 9.3 打赢 / 打输
| 情况 | 后果 |
|---|---|
| 你们守住 | 该国 `military`/`treasury` 受损、态度回升、可能递**求和国书** |
| 你们被压制 | 损失资源与建筑；该国态度更硬、要价更高 |
| **v1 不做"玩家被灭"** | 只做压力与损失（朋友服被 AI 灭国是挫败设计）|
| 你们反推、打到"可占领" | 见第十一节 夺城 |

### 9.4 国书
国家主动递**可交互事件**（条件 + 期限）："停止在边境建造""共同讨伐 X 国""朝贡 N 资源换互不侵犯"。
**v1 形态**：情报册/情报站里的可回复条目（接受 / 拒绝 / 忽略=拒绝）。

> ✅ **触发侧已实现**（2026-09-20）：`letter/LetterIssuing`。原来只有「递出之后怎么走」（`LetterMachine`）
> 而**没有任何地方会调用它** —— §9.4 的国书在游戏里永远不会出现。四档判断（顺序即优先级）：
> 「在打仗且它占优 → 索贡」「不在打仗但恨你 → 停止建造」「有个仇家又不讨厌你 → 共同讨伐」
> 「其余 → 不递」；带同国冷却、已有待回的不再递、每拍最多 1 封（确定性：按 `nations[]` 顺序取）。
>
> ⚠️ **门槛值不能与惩罚值同值**：递书门槛原本写成 −30，而宣战惩罚恰好也是 −30 ——
> 宣战完态度正好踩在门槛上，**同一拍里的"态度回归"又把它拉回 −28/−29**，于是边界永远
> 观察不到、一封国书都递不出来（真机实测：宣战→求和→连推三拍，`letters total=0`）。
> 现在门槛是 **−20**（明显松于 −30）。这和 `SettlementEngine.willDeclareWarOnPlayer`
> 那条边界注释是同一个坑，写在这里免得下次再踩。
**不在 v1**：实体使节走到你面前。

---

## 十、建筑与村民（图纸 → 图纸加农炮 → 绑定村民）

> 这一节把 `DESIGN.md` §三的"**图纸是壳，模块是核**"落地：**模块 = 一个绑定建筑的村民**。

### 10.1 链路

```
建筑工 NPC（出售「图纸授权」）
   ↓ 玩家买到授权
玩家在自己领地内用 Create 的图纸加农炮打出建筑外壳（真实材料消耗，已有机制）
   ↓ 锚点方块就位 + 环境条件成立
mod 幂等生成一个【绑定村民】= 这座建筑的功能接口 + 城里的人气
```

> ✅ **core 侧已实现**（2026-09-20）：`blueprint/BlueprintRules` 出「买授权 / 能不能动工 / 货架上还剩什么」，
> 价钱与「要不要授权」全部来自 `buildings.json`（`requiredBlueprint` + `blueprintPrice`）。
> **不需要授权的建筑永远通过** —— 门禁由数据决定，代码里不写死「所有建筑都要授权」。
> 玩家侧的「买过哪些」「钱包剩多少」**不在 core 里**（和外交那套同一个理由），由调用方递进来、core 只报出要扣多少。

**村民的边界（写死）**：只做**人气 + 界面**。不盖房、不生产、不搬运、不参战。
→ 本 mod **不写任何劳动 AI**；建造仍由玩家的图纸加农炮完成，
所以 `DESIGN.md` 的"**建造不外包 / 每一块砖都是玩家自己放的**"这条原则**没有被动过**。

### 10.2 村民的实现

- **继承原版 Villager** 的自研实体：白拿模型 / 动画 / 寻路，覆写交互
- 禁繁殖、**锁定职业**、游荡限制在建筑半径内、持久化不被自然清除
- **有名字**（与包里 `villager-names`、`more-villagers` 的语汇一致：这个世界本来就有"有职业、有名字的村民"）
- 交互：对话 → 该建筑的功能（情报站 = 情报成书 + 外交动作）。**复用原版人物交互界面**，不写自定义 GUI

### 10.3 幂等（关键，不做对会生出三个文书）

- `WorldState.buildings[]` 记 `{ id, type, anchor, staffUUID, materializedAt }`
- 区块重载 / 服务器重启后：校验 `staffUUID` 存在、是本职村民、且 `buildingId` 匹配
  → 匹配则**不生成**；不匹配则按冷却重生成
- **同一 `seq` 不重复生成**

> ✅ **core 侧已实现**（2026-09-20）：`building/IdempotencyRules` 出 `SKIP / RESPAWN / WAIT_COOLDOWN / MARK_INVALID`，
> 判定顺序是"**锚点先判** → 三件校验全过就 SKIP → 再看同一 seq 与冷却"；两种冷却分开（村民死了用重雇冷却，村民在但职业不对用实体化间隔）。
>
> ⚠️ **真机已知问题（2026-09-20）**：**没有玩家在线时**，绑定的村民在重启后不进世界 ——
> 盘上有（`tools/mca_probe.py` 能看到它的标签），但 `execute if entity @e` 是 0 个，
> 连原版 `/summon` 的村民也一样；方块则照常装回来。最可能是原版行为（实体要区块进入
> entity-ticking 才装，而那需要玩家在附近）。见 `DEFERRED.md` §J，第一步就是**带个客户端重跑**。
>
> ✅ **接线已实现**（2026-09-20）：`staff/StaffBinding`。mod 层只报告**原始事实**（那个实体是什么职业、身上记着哪个 buildingId、锚点还在不在），
> 「它是本职村民吗」「绑定对不对」在 core 判 —— 不然 §10.3 那条规则就跑进 mc 层了，而它恰恰是"不做对会生出三个文书"的那条。

### 10.4 村民的寿命与损失

| 情形 | 后果 |
|---|---|
| 村民死亡 / 被感染（The Hordes） | 建筑**停摆** + `staff_lost` 事件 + 冷却后可**重新雇用**（花资源） |
| 建筑被拆（锚点没了） | 建筑登记为失效；村民保留（可按登记重新安放） |
| 夺城期间 | 城内百姓（含绑定村民）**隐藏 + 休眠 + 无敌**，事件后原样恢复 |

### 10.5 环境条件（**软性判定，不做形状检测**）

- 条件：**有屋顶 / 封闭度 / 最小体积 / 在领地内** → 决定"能不能用 / 到哪一档"
- **规则（阈值）在 `core`**，世界读数由 `integration` 提供"环境快照" → core 保持纯逻辑、可 JUnit
- **为什么不做硬性形状检测**：脆（挪一块砖就失效）、跨区块误判、性能、且与"地标手建"的自由冲突

> ✅ **core 侧已实现**（2026-09-20）：`env/EnvironmentRules`。`EnvironmentSnapshot` **恰好只有四个读数**，没有任何方块/形状信息 ——
> 所以"不做形状检测"是**类型上的保证**，而且有一条用例把 record 的分量名钉死（谁想加"方块列表/长宽高"会先红）。

### 10.6 v1 只实现两处，其余是"加数据"

| v1 实现 | 内容层（后续，只加 `buildings.json` 数据） |
|---|---|
| **情报站**（文书村民）+ **建筑工 NPC** | 仓库管事 / 铁匠 / 商人 / 驿站马夫 / 农夫 … |

> ✅ **core 侧已实现**（2026-09-20）：`buildings.json` / `staff.json` 已接进 core（`building/BuildingDef` + `BuildingCatalog`、`staff/StaffDef` + `StaffCatalog`）。
> 档案里 v1 **只有 `intel_station` 一座**（"建筑工 NPC"是 mod 层实体，不是玩家自建的建筑，所以不进 buildings.json）；
> 「加一座建筑」现在只改两份数据：`buildings.json` 加一条 + `staff.json` 加一个职业。
> **能力挂在「档」上而不是建筑上**：§11.1 的一级站与升级站能力不同，挂在建筑上的话一级站也会声称自己会深挖。
> 两张数据表在**载入时交叉校验**（建筑要的职业必须在村民表里、村民说的建筑必须在建筑表里、村民的本事必须盖得住建筑满级开放的能力），对不上直接报错，不留到进游戏才发现。

---

## 十一、玩家侧：情报、外交、夺城

### 11.1 情报能力的三档

| 档 | 条件 | 能力 |
|---|---|---|
| **情报册**（物品） | 无（`eras.json` 解锁） | 看已接触国家的基本信息、读未读事件、回复国书 |
| **情报站**（建筑 + 文书村民） | 建筑条件成立 | 完整列表、**遣使**发起外交、**宣战/朝贡必须在此发起**（全服公告）|
| **情报站升级**（对锚点用升级件） | 建筑等级 +1 | 可追踪国家 +2 · 显示各国正在进行的战争与同盟 · 每结算可深挖 1 国详报 |

**接口是"人"**：去跟文书说话，而不是点一个方块。

> ⚠️ **原来整条玩家链是死的**（2026-09-20 接线时发现）：`met`（是否已接触）在生成时
> 一律 `false`，而**没有任何地方会把它改成 true**。后果不是"少一个字段"——
> 情报册只显示已接触国家、§11.2 的六个外交动作全都以"已接触"为前置、§9.4 的国书也一样。
> 也就是说 `met` 恒为 false 时，**情报 / 外交 / 国书全都不可达**（真机上实测：
> `/statecraft diplomacy n11 declare_war` 回的是"你还没接触过它"）。
> ✅ 已补 `intel/IntelContact`，两条接触路径：
> ① **走近** —— 到它都城 512 格内（【占位】；都城坐标一直在 `Nation.x/z` 里，不需要实体化）；
> ② **情报站建成** —— §11.1 写着情报站给"**完整列表**"，所以一座绑好村民的情报站
>    就等于"把天下各国的名号都记进册子"（这也让玩家不必跑遍地图就能开始外交）。

> ✅ **core 侧已实现**（2026-09-20）：`intel/IntelTier` + `intel/IntelBook`。
> 三档**按 `eras.json` 的 `unlocks` 判定**（`intel_book` / `intel_station` / `declaration` / `intel_station_2`），时代数量与解锁时机仍然全在数据里。
> 名单是**投影**（`NationBrief`）而不是把 `Nation` 交给界面：档位不够时那些字段**根本不在对象里** —— 
> 直接把完整状态给界面的话，§11.1 的三档就退化成"界面上少写几行"，一条注释就能绕过。
> 未接触（`met=false`）的国家**连名字都不给**；灭亡的国家**要给**并标注（§八.8 说它们的都城留着当废墟，情报册里突然消失反而像 bug）。
> 「追踪了哪两个」「这个结算深挖过没有」属于玩家侧状态（和 `playerLedger` 一样，计划 5 落盘），所以 core 只判上限与资格，清单由调用方递进来。

### 11.2 外交 6 动作

| 动作 | 前置 | 后果 |
|---|---|---|
| **宣战** | 已接触；非同盟；在情报站发起（全服公告） | 进入 `war`；态度大跌；可发起夺城；它也会来打你们 |
| **求和** | 处于 `war` | 按战果判定成败；成功转 `truce`（冷却 = 1 个结算周期）|
| **结盟** | 态度 ≥ +40；非战争 | 转 `alliance`；互相在战争中提升彼此战力 |
| **通商** | 态度 ≥ 0 | 双方 treasury 提升；解锁该国可交易商品（在都城交易）|
| **求援** | 处于 `alliance` | 按对方军力与态度决定是否驰援：抽象提升战力；**可选**派援军实体（复用 HYW 兵种）|
| **朝贡** | 你方发展度高于对方；在情报站发起（全服公告） | 支付资源换态度与"不侵犯"承诺 |

**实现说明（§11.2）**：

- **前置被违反一律拒绝，且状态一个字都不动**（§十五 的要求）。拒绝是正常结果（玩家会看到理由），
  只有"国家 id 不存在"这类调用方 bug 才抛异常。
- **"在情报站发起"由 mod 层把关**：core 没有世界读数，"你在不在情报站旁边"它判不了。
  core 只保证规则上的前置（已接触 / 非同盟 / 处于战争 / 态度门槛 / 发展度差）。
- **玩家侧的资源与战力不在 core 里**（`playerLedger` 属于计划 4/5）。所以通商/朝贡给玩家带来的国库变化、
  求援带来的战力加成，core 只**报出来**（`DiplomacyOutcome.playerTreasuryDelta / aidGranted`），
  由调用方去记账——core 不编一个玩家账本。
- **需要掷骰的动作**（求和、求援）用调用方递进来的 `nonce` 当随机序号：
  玩家动作是**外部输入**（和命令一样），只有把 nonce 记进事务日志，"同 seed 同输入必得同结果"才成立。
  同一串动作里 nonce 必须各不相同。
- **"不侵犯"承诺是真的会挡住宣战**（`SettlementEngine.willDeclareWarOnPlayer` 检查它），
  并且在结算序号到点时自动失效——一次结算里"承诺到期"排在"对玩家施压"之前，
  所以它一过期就可能立刻翻脸：买的是那段时间，不是它的态度。
- §十二 的外交成功率里 `attitude−50` 已定案为 `attitude`（理由见 `PLAN-statecraft-03-settle.md` §一）。

### 11.3 夺城（= 玩家吞并国家）

**平时：都城没有守军**，只有不参战的百姓。

**触发**：在情报站发起「吞并」。前置：该国已被打到**可占领**状态（`warScore` 压制 + 军力低于阈值）+ 付出代价（资源）。

**开场三件事**
1. **百姓隐藏 + 休眠 + 无敌**（不删除 → 绑定不丢、不会被误杀）
2. 临时刷出**守城军**（该国文化的 HYW 单位，上限 24~40）
3. 刷出**指挥官**（该文化现成的 `commander` / `boss` 实体）

**完成条件：两个都满足**
- **插旗**：走到城中心的**旗座**按住 N 秒（默认 8 秒，可配）
- **指挥官已死**

两者关系：**指挥官活着时守军周期性补员** → 实战上必须先解决他，否则打不完。
所以不写死顺序，玩家自由，但节奏天然是"先斩首、再插旗"。**指挥官不逃跑。**

**收场**
| 结果 | 处理 |
|---|---|
| 成功 | 生成**占领碑** + 该国 `status = dead` + 都城进**玩家属地清单** + `city_taken` 事件 + 守军清除 + 百姓恢复 |
| 失败（全员撤离 / 团灭 / 超时） | 守军撤走 + 百姓恢复 + 该国军力回一点 + 态度更硬 + **冷却 1~2 个结算周期**（防无限刷城）|

**性能**：实体只在事件期间存在，结束全清。

**依赖**：这一步挂在"**能不能刷出 HYW 单位、且它们会攻击玩家**"上——**这是第一个验证项**（第十五节）。

---

## 十二、数值与公式（默认值，全部走配置）

> **实现位置**：生成参数在 `StatecraftConfig`，结算参数在 `SettlementConfig`（计划 3 新增）。
> §十二 没给数的项在那里标成 **【占位】**（军力恢复速率、国家间开战概率、灭亡/吸收的阈值、
> 袭击概率、每结算事务上限、每步战果幅度），默认值是跑出来量过的保守值，不是随手写的。
> **实现更正**：外交成功率里的 `attitude−50` 已定案为 `attitude`（见 `PLAN-statecraft-03-settle.md` §一：
> §9.1 的 `attitude ≤ −40` 与 §11.2 的 `态度 ≥ +40` 都要求有符号态度）。

| 项 | 默认 |
|---|---|
| 发展度 | `dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))` |
| 政治倾向 | `stance = clamp(−100, 100, stanceBase − dev + rng(−30,+30))`，`stanceBase` 默认 60 → **落后更主战、先进更主和** |
| 外交成功率 | `p = clamp(0.05, 0.95, 0.5 + (attitude−50)/200 + (devYou−devThem)/200 + stance/400)` |
| 停战冷却 | 1 个结算周期（2 小时累计在线）|
| 态度回归 | 每小结算向 0 靠 1~2 点 |
| 玩家行为 → 态度 | 击杀其单位 −1/次（周期上限 −15）· 在都城 64 格内放置方块 −2/次（上限 −10）· 通商 +3 · 完成国书 +10 |
| 国家宣战门槛 | `attitude ≤ −40` 且 `stance ≥ +40` |
| 袭击规模 | `2 + floor(military/25)`，上限 **24** |
| **吸收** | 胜者 `size += ceil(败者size × 0.5)`（上限 10）→ `dev` 重算 |
| **可占领门槛**（夺城前置） | 与玩家方处于 `war` 且 `warScore` 占优 且 该国 `military < 10 × coefficient` |
| **扩张疲劳** | 每点：军力恢复 ×0.85、被宣战概率 ×1.15；每小结算 −0.1（最低 0）|
| 都城居民 | `3 + size`，上限 12 |
| 守军（夺城） | `6 + size × coefficient × 2`，上限 **40** |
| 插旗时长 | 8 秒 |
| 夺城冷却 | 1~2 个结算周期 |
| 建筑环境条件 | 有屋顶 + 封闭度 ≥60% + 内部体积 ≥12 格 + 在领地内 |
| 实体化节流 | 同一次结算最多 1 处；两次间隔 ≥5 分钟；其余排队 |
| 事件保留 / 未读上限 | 500 / 200（归档 ≠ 丢失）|

---

## 十三、失败与边界

| 情形 | 处理 |
|---|---|
| **存档 schema 变更** | `schemaVersion` 判定；缺版本号按 v0；不匹配走迁移；版本比代码新或**缺某一级迁移** → 重置国家系统 + 中文警告，**不让存档报废** |
| **时代表变更** | 未知 `eraId` → 归最近 ordinal + 一次警告；`eraId` 与 `eraOrdinal` 矛盾 → **以 eraId 为准**（id 是稳定标识，序号只用于排序）；不废档 |
| **结算途中崩溃** | 事务日志（结算序号 + 输入哈希）；确定性 + seed → 从上一个完整结算重放 |
| **外部输入异常** | 配置与命令的数值区间钳制 + 日志，绝不写入非法状态 |
| **国家灭亡 / 被吸收** | 事件与国史保留；关系矩阵清理；`dead` 不再参与结算 |
| **HYW 缺席（软依赖）** | 降级：不派实体、不刷守军；夺城不可发起（界面提示"缺少军事模块"），其余照常 |
| **都城 / 锚点放置失败** | 选址检查 → 换点（最多 8 次）→ 标 `undecided`，下一结算重试；**先平台化再放模板** |
| **夺城期间玩家掉线 / 服务器重启** | 事件视为**失败**（守军撤走、百姓恢复、进冷却），不留下"半开"状态 |
| **村民重复生成** | 幂等校验（第十节 10.3）；重复实体自动清除 |
| **中途移除 mod** | 按 MC 行为，方块变空气并记"丢失方块"日志（**不崩**）；`WorldState` 留在存档不读取；装回可继续 |
| **性能** | 关系矩阵 O(n²)（12 国 = 66 对，可忽略）；实体只在事件期间或玩家靠近时存在，并受节流约束 |

---

## 十四、已知风险（本机实测）

1. **能否刷出 HYW 单位、并让它们攻击玩家** —— **仍未验证：2026-09-19 两次真机尝试都被环境阻断，没拿到任何动态证据**。HYW 注册了 103 个实体、含两套敌对势力与攻城器械（静态证据充分：本次核对 `entity.hundred_years_war.*` 共 **103** 键，`bandit_soldier` / `desert_raider_commander` / `battering_ram` 三个 id 齐全），但"运行时刷出并作战"的活体验证没做成，两个阻断点都在环境侧：① **单机真客户端必崩在进世界那一步**（当日 5 次同签名：09:16 / 09:30 / 09:44 / 14:53 / 15:11；崩点是 `inventoryprofilesnext` 2.2.5 的类初始化环 `Features.ENABLE_PROFILES$delegate is null`，触发屏 `LevelLoadingScreen`，见 `crash-reports/crash-2026-09-19_14.53.30-client.txt`）——按 `tools/tests/hyw_units.txt` 跑两次都是 0/10、全部"无输出"，命令从未进到活着的游戏；② 退路（真客户端连专用服务端，即 3~7 人的真实部署形态）同样被阻断：服务端以自带的 `-Xmx4G` 起来后约 3 分钟即 `OutOfMemoryError: Java heap space`（崩在 WorldEdit 的 `PlatformReadyEvent` 派发中，`server/logs/latest.log` 15:24:49，**当时无玩家在线**），随后 OOM 自旋吃掉 8 核里的 7.2 核，客户端连 `Connecting to` 都到不了。**先修这两点（客户端换掉/升级 InventoryProfilesNext；服务端调大 `server/user_jvm_args.txt` 的堆）再重跑该脚本即可判定**；另注意本机 `options.txt` 是 `lang:zh_cn`，脚本里的英文断言（`Test passed`）很可能不匹配（回显为中文），届时以输出行人工判读为准。
   → **本 mod 的第一个验证项**；夺城环节完全挂在这上面。
   **2026-09-19 傍晚更新**：两个环境阻断点**都已经修掉**（C12 客户端进不了世界 / C14 BOM 编码），
   而且方案已经重新设计过 —— 见 `PLAN-m0-hyw.md` + `tools/tests/m0_hyw.txt`：
   判据改成"生存难度下玩家血量真的掉了"（不依赖客户端按键，因为玩家可能被打死、死亡屏会吞按键），
   通道全部走 RCON，跑在专用服务端 + 一个真客户端加入的形态上。
   **静态证据已经指向"会打"**：`hyw_main.json5` 里 `nullOwnerUnitsAggressive=true`、
   `nullOwnerUnitsFriendlyToEnemy=false`，而 `/summon` 出来的是无主单位 → 按配置应当攻击所有活物
   （`hostile_target_list` 那两组名单只在 `FriendlyToEnemy=true` 时才生效）。
   差的就是真机跑一次。
2. **RCON 命令通道不可靠**：自带 `server_ctl.py` 与手写裸客户端都能登录成功但只拿到空响应，服务端日志也无命令痕迹。
   → **所有自动化测试跑在 `core` 层**；命令只留人工调试。
3. **`Portable Blueprints` 的真实能力未核实**（是否支持"图纸即物品"、材料代扣的确切行为）。
4. **运行时放都城的地形融合观感**是 M4 的最大技术风险（第十二节给了缓解规则，但必须真机看一次）。

---

## 十五、测试与验收

### JUnit（主力，离线，天天跑）

| 用例 | 断言 |
|---|---|
| **N 时代快进** | 3 / 6 / 9 三套时代表各跑一遍整套结算，不抛异常、事件与周报数落在预期区间 |
| seed 复现 | 同 seed 同输入 → 两次运行状态逐字段相等 |
| **硬规则护栏** | 最近国主和只能靠**交换位置**满足：每国 `stance` 必须仍落在 `stanceBase − dev ± stanceJitter` 内（"事后改倾向"的实现会立刻红）。已用变异探针验证：把交换换成翻符号 → 只有这条断言失败，旧的"最近国主和"断言照样绿（2026-09-19）|
| 大小与发展度反比 | 生成 1000 次，相关系数显著为负 |
| 国家间战争 | 强打弱胜率高；灭亡边界（dev=24 vs 25、连输 2 vs 3）|
| **吸收与扩张疲劳** | `size`/`dev` 重算正确；连跑 6 个时代后**最大国不超过阈值**（防雪球）|
| 国家对玩家的压力 | 宣战门槛边界；袭击上限 24；守住/被压制分支 |
| 外交状态机 | 前置被违反**必须拒绝**；转移合法；停战冷却生效 |
| 玩家行为 → 态度 | 每周期上限不被突破；通商/完成国书加成正确 |
| 国书 | 到期、忽略=拒绝、完成后的态度与后续 |
| **建筑幂等** | 同一建筑重复触发只出一个村民；区块重载不重复 |
| **环境判定** | 有屋顶 / 无屋顶 / 被拆一半 / 跨区块 四种输入的判定结果 |
| **村民损失** | 死亡 → 停摆 → 冷却 → 重招；夺城期间隐藏且无敌 |
| **夺城** | 完成条件（插旗 + 指挥官死，缺一不成立）；补员速率；失败冷却；守军上限；百姓恢复 |
| schema 与时代表迁移 | 旧快照可迁；未知 `eraId` 归位不崩 |
| **结算重放（已实现）** | 同 seed + 日志里的输入 → 逐字段重放出一模一样的历史（8 个 seed × 18 步）；篡改任意一步都会被 `replay` 或 `verify` 抓住；支持"从上一个完整结算（快照）接着重放" |
| **外交前置（已实现）** | 6 个动作的前置被违反一律**拒绝且状态对象不变**；求和/求援两个分支都可达（骰子确定性，所以这不是碰运气）；朝贡的不侵犯承诺真的挡住宣战、到点自动失效 |
| **存档往返（已实现）** | 40 个 seed 的生成世界 `write → read` **逐字段相等**；world seed 取 2^53+1 也不丢精度（`long` 不许走 double）|
| **坏档与版本（已实现）** | 缺版本号按 v0 走迁移链；缺迁移 / 版本比代码新 / 内容损坏 → **重置国家系统 + 警告，不抛异常**；未知 `eraId` 归位；`eraId` 与序号矛盾以 id 为准 |
| 软依赖降级 | 关闭 HYW 分支时结算仍跑完、不产生实体、夺城被拒 |
| 周报文本 | 快照测试（模板不漏变量、不出现 `%s`、地名池不越界）|

### 游戏内（攒成一次，不零散占用时间）

1. **先验风险 #1**：刷 HYW 单位并确认它们会攻击玩家
2. 建筑工交易 → 打图纸 → 情报站起壳 → 文书出现且能对话
3. 夺城：发起 → 百姓消失 → 守军与指挥官 → 斩首 → 插旗 → 占领碑
4. 都城实体化的地形融合观感

### 交付物
- mod 源码（`mods/statecraft/`，NeoForge MDK + Gradle，Java 21）
- 数据包 / 资源包（第十六节的数据文件）
- README：**怎么加一个国家 / 一条条约 / 一个事件 / 一个时代 / 一座建筑与它的村民**
- 配置项清单与默认值说明

---

## 十六、数据文件清单（"怎么加内容"的入口）

| 文件 | 内容 |
|---|---|
| `eras.json` | 时代表：数量、名称、时长、系数、解锁项 |
| `nations.json` | 国名池（**按文化**：生成时优先取本国池，用尽后按文化表顺序回退；全局不重名）、规模/发展度/倾向取值范围 |
| `cultures.json` | 文化 → 外观与兵种档位（HYW 皮肤包/兵种 id）|
| `places.json` | 虚构地名池（都城/边镇/关隘/渡口），供周报取词 |
| `buildings.json` | **建筑定义**：类型、锚点方块、图纸、环境条件、绑定的村民职业与能力、所需图纸授权 |
| `staff.json` | **村民定义**：职业、名字池、外观、对话内容、能力绑定 |
| `events/*.json` | 事件模板（中文文本模板与变量）|
| `treaties.json` | 外交动作定义：前置、成功率修正、后果 —— **延后**，见 `DEFERRED.md` §I |
| `letters/*.json` | 国书模板（条件、期限、接受/拒绝后果）|
| `capital_templates.json` | 都城模板与档位（小/中/大）、旗座定义 |
| `schematics/` | 图纸 `.nbt`（随包分发）|

---

## 十七、里程碑（详细实现计划另行编写）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M0** | **验证风险 #1**：刷 HYW 单位并确认它们攻击玩家 | 真机确认（夺城的前置） |
| **M1** | core：数据模型 + 生成 + 结算 + 吸收与疲劳 + 外交状态机 + 国家对玩家压力 + **环境判定与幂等决策** + 文本模板 | 🟢 **只剩「事件文本/周报模板」没做**（要等地名池与情报册，属计划 4）：数据模型 / 生成 / 结算引擎 / 外交状态机 / 事务日志与重放 / **环境判定（§10.5）** / **幂等决策与实体化节流（§10.3/§10.4/§十二）** / 存档编解码与 schema 迁移全部完成 —— **226 个 JUnit 用例全绿**（2026-09-20，全程离线、一个游戏都没启动）|
| **M2** | 持久化（SavedData + schema/era 迁移 + 事务日志）+ 命令 | 🟡 **core + mod 薄层已完成，真机落盘已证**（`PLAN-statecraft-02-persist.md`）：`StateNode`/`StateCodec` 往返、`schemaVersion` 迁移链、`eraId` 归位；**版本太新 / 缺迁移 / 内容损坏一律只重置国家系统并给中文警告，绝不让存档报废**。mod 侧用 ModDevGradle 2.0.147 + NeoForge 21.1.250 构建，真机装载成功、生成 12 国并写出 `saves/<world>/data/statecraft.dat`（用 `tools/nbt_dump.py` 核对：`schemaVersion=1`、long 种子未丢精度、12 国名字/文化/坐标俱全）。**共 129 个 JUnit 用例全绿**。**未做**：事务日志（等计划 3 定义"结算输入"再写）、命令的按键通道验证（测试世界的死亡屏会吞按键，见 `CONFLICTS.md` T19）|
| **M3** | 建筑工 NPC + 图纸授权 + 情报站建筑 + **文书村民（生成/绑定/对话）** + 情报册 + 周报 + 国书 | 游戏内：买授权 → 打图纸 → 文书出现 → 读到真实情报 |
| **M4** | **夺城环节**（旗座 + 守军 + 指挥官 + 百姓隐藏/恢复 + 占领碑） | 完整链路真机跑通一次 |
| **M5** | 升级件 + 自定义界面（很可能不需要）+ 打磨 | 手感验收 |
| **后续** | 其余建筑的村民（内容层，加数据）· OPAC 接管 · 实体使节 · 附庸/朝贡深度 · 自造文化外观 · AI 史官接周报 | — |

---

## 十八、明确不在 v1 范围

- 世界内的扩张模拟（**按设计决策，永久不做**）
- **劳动 AI / 施工 AI**（建造永远是玩家用图纸加农炮打出来）
- **常驻守军**（只有夺城事件期间才有守军）
- **玩家被国家灭亡**（只做压力与损失）
- 实体使节走到玩家面前（v1 只有可交互国书）
- 除情报站、建筑工以外的建筑职员（内容层，后续加数据）
- 附庸国的内政与朝贡结算深度（v1 只有"灭亡 / 吸收 / 吞并"）
- 都城建筑的时代升级套件（v1 只做"平台化 + 放模板 + 居民 + 旗座"）
- 玩家属地的税收/产出（v1 只登记 + 界面可见）
- 接管进 OPAC 领地（需先核实 API）
- 多文化自造外观（v1 复用 HYW 现成的两套）
- 接入 AI 史官（v1 周报用模板生成）
