# Statecraft · 国家与邦交系统（设计文档 v1.1）

> **这个 mod 是什么**：《荒野建国》的**配套 mod**，负责"这个世界的政治"——
> 国家的成长与推演、国家之间的战争与结盟、各国怎么看待你们、你们怎么跟他们打交道、
> 以及怎么把他们收进来。
>
> **中文注解**：邦交（国与国之间的往来）。mod id 用 `statecraft`（MC 命名空间只允许
> 小写字母/数字/下划线）；**玩家看到的一切界面、周报、物品名全部是中文**。
>
> **状态**：设计定案（2026-09-19）。**尚未开始写代码**，下一步是写实现计划。

---

## 附：v1.0 → v1.1 自审修订清单

自审后改了 14 处。**第 1、2 条是结构性修改，第 4 条是补一个真漏洞**，其余是消歧义与补齐。

| # | 问题 | 处置 |
|---|---|---|
| 1 | **时代写死成 6 个**（系数表、解锁点、测试都硬编码"六时代"） | **改为数据驱动**：时代数量、时长、系数、解锁全部进 `eras.json`；代码里不出现 "6"；测试用 3/6/9 三套时代表各跑一遍（见第七节）|
| 2 | 解锁点用**序号**表达（"时代 3+ 可建望楼"）→ 时代一改就错 | 改用**稳定 era id** 表达解锁；存档与事件同时记 id（表意）与 ordinal（排序）；未知 id 归到最近 ordinal 并记一次警告（旧存档不废）|
| 3 | **"缩短情报延迟"含糊**（没有基线，无法测） | 删掉。望楼 II 改成明确的三条：+2 可追踪国家 · 显示各国正在进行的战争与同盟 · 每结算可深挖 1 国详细情报 |
| 4 | 🚨 **国家只会被你打，不会打你** —— "主战"属性等于没牙，"御敌时代"和这个系统没接上 | **新增第九节**：国家可对玩家方宣战并按结算周期派兵袭击；玩家守得住就削弱它；**v1 不做"玩家被灭"**（只做压力与损失，避免朋友服被 AI 灭国的挫败）|
| 5 | **3~7 人的多人语义没写**：态度是每人一份还是全队一份？谁能代表全队宣战？ | 态度以**玩家方**为单位（一个"你们"）；任何玩家可发起外交，但**宣战/朝贡必须在望楼前发起并全服公告**，避免一个人偷偷把全队拖进战争 |
| 6 | **玩家行为不影响态度**（只有外交动作能改） | 补"行为 → 态度"映射：击杀其单位 / 在其都城附近建造 / 通商 / 完成国书请求，各有数值与每周期上限（见第十一节）|
| 7 | 只有单向情报，**国家不会主动找你** | 新增**国书**：国家可主动递条件与期限（可交互事件）；v1 不做"实体使节走过来"（明确列为后续）|
| 8 | **规模（1~10）在世界里看不出来** | 补 `size → 实体化规模` 映射：哨所数 `floor(size/2)`、守军 `4 + size×系数`（上限 40）、都城模板按 size 选档 → "大而落后、小而先进"看得见 |
| 9 | **都城放置的可行性完全没验**（会放进海里/峭壁/已有结构/玩家建筑上吗？） | 新增放置前置检查 + 重试 + 平台化规则，并列为 M4 最大技术风险（见第十二、十三节）|
| 10 | 抽象战争却要写"攻破某处边镇"，**地名从哪来**没交代 | 新增**虚构地名池** `places.json`（每国一组：都城/边镇/关隘/渡口），周报才可信 |
| 11 | **HYW 缺席时会不会崩**没写 | 明确 **HYW 是软依赖**：缺失时降级为"纯推演 + 情报"（不派实体、不刷守军），界面提示"缺少军事模块" |
| 12 | **成功率没依据**、停战冷却没时长 | 给统一公式骨架与默认值（见第十一节）|
| 13 | **实体化没有冷却与并发上限**（玩家跑图可能连续触发） | 补：同一次结算最多实体化 1 处；两次之间 ≥5 分钟；同时未实体化的国家排队 |
| 14 | 出生点附近的国家**靠改数值**强制主和 → 与"发展度决定倾向"的公式打架 | 改成**从候选里挑**：优先把 `stance ≤ 0` 的国家放到离出生点最近处；没有就调换布点，而不是改它的数值 |

（另：补了**数据文件清单**（第十五节）与**中途移除 mod 的安全行为**，这两条是原来漏的。）

---

## 一、定位与边界

### 做

- 国家的**状态与推演**（规模、发展度、政治倾向、军力、国库、互相之间的态度）
- **信息流**：可建造的「望楼/情报站」+ 情报册，收各国动态，出周报
  （《文明》式的抽象表现，**不做世界内扩张模拟**）
- **外交**：6 个动作，状态机驱动
- **国家与玩家的战争**：国家会宣战、会派兵来打你们（第九节）
- **吞并**：打下一国的都城即吞并（唯一的"玩家主动攻城"实体化战场）
- **把状态落成世界里的东西**：都城、哨所、守军、可交易的商品

### 不做（明确交给谁）

| 不做 | 交给 |
|---|---|
| 战斗、兵种、攻城器械、成建制行军 | `hundred-years-warfare`（已装，**软依赖**）|
| 领地规则与权限 | `OPAC`（已装）|
| 尸潮 / 夜袭等威胁事件 | `The Hordes` + `Undead Nights`（已装，可选联动）|
| 建筑美术 | 复用已装建筑 mod 的积木 + 生成器组合（不手搭）|
| 世界生成期的结构注入 | 不做——国家都在**运行时**实体化，因此**不需要重建世界** |

---

## 二、与现有系统的接口

| 接口 | v1 实现 | 以后 |
|---|---|---|
| **读时代** | `EraProvider` 接口，数据来自 `eras.json`（见第七节）。v1：内部"累计在线时间"计数器 + 命令强制推进 | 日历/国力系统做出来后**换实现类**，`core` 不动 |
| **写世界** | 运行时放置结构、刷 HYW 兵种实体、开交易 | 都城升级套件 |
| **吞并落账** | 生成「占领碑」+ 事件入国史 + 都城登记进**玩家属地清单** | 接管进 OPAC 领地（**需先核实 OPAC 是否提供可调用 API**，v1 不依赖）|
| **HYW（软依赖）** | 只做"点名"：某国用哪套皮肤包 / 哪个兵种档位（I~IV） | 让 HYW 部队承接国家的出击 |
| **The Hordes（可选）** | 若能联动，把"国家来袭"配成一次可配的 horde 事件 | — |
| **命令通道** | 只用于人工调试 | 已知本机 RCON 通道不可靠（第十三节），故**一切自动化测试不依赖命令** |

---

## 三、术语

| 术语 | 含义 |
|---|---|
| **国家（Nation）** | 一个政治实体：国名、文化、都城、规模、发展度、政治倾向、军力、国库、态度、状态 |
| **规模（size）** | 1~10 的抽象值。**不进世界**，但**决定实体化规模**（哨所数、守军数、都城档位）|
| **发展度（development）** | 0~100。与**规模反比**：大国低、小国高 |
| **文化（culture）** | 决定外观与兵种档位，默认复用 HYW 现成势力（`bandit` / `desert_raider`）|
| **都城（capital）** | 每国一座，**必须实体化**（吞并要有一个真能打进去的地方）|
| **哨所（outpost）** | 由 size 推出的次要实体化点，让"大小"看得见 |
| **关系（Relation）** | 两国之间：态度、状态（和平/战争/同盟/停战冷却）、战果 |
| **事件（Event）** | 带时代戳的记录：谁对谁做了什么 + 中文摘要。**周报、望楼界面、国史共用同一个数据源** |
| **国书（Letter）** | 国家主动递给玩家的**可交互事件**（带条件与期限）|
| **未读队列** | 玩家还没看到的事件，按顺序呈现，不刷屏 |
| **玩家方（party）** | 3~7 人视为**一个政治实体**（"你们"）。态度以方为单位，不按人分 |
| **玩家属地（domain）** | 被玩家占领的原都城，登记在册（v1 只登记 + 界面可见，不做税收）|

---

## 四、架构

三层，**依赖方向单向向下**（上层可调下层，下层不许 import 上层）：

```
┌─ presentation（薄）─────────────────────────────────────┐
│  望楼方块 · 情报册物品 · 自定义界面(网络包) · 周报成书 · 国书交互  │
├─ integration（薄）──────────────────────────────────────┤
│  EraProvider · 世界写入(结构/守军/交易) · HYW 适配(可缺)         │
│  OPAC 适配(后续) · The Hordes 联动(可选)                      │
├─ core（厚，纯 Java，零 Minecraft 依赖）───────────────────┤
│  NationState · WorldState · SettlementEngine · DiplomacyMachine │
│  LetterFactory · EventFactory · 文本模板 · 地名池 · seed 随机     │
└────────────────────────────────────────────────────────┘
```

**为什么这样切**：
- `core` 装**全部规则**——时代结算、国家间战争、国家对玩家的压力、外交状态机、吞并判定、周报文本。
  不 import 任何 Minecraft 类 → **JUnit 就能把整套时代表跑完**。
- `integration` / `presentation` 只把状态翻译成世界里的东西。
- 换 MC 版本或加载器时，`core` 一行不动。

---

## 五、数据模型

### Nation

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 稳定标识，生成后不变 |
| `name` | string | 中文国名（名字池，按文化分池，全局唯一）|
| `culture` | string | 决定外观与兵种档位 |
| `capital` | 坐标 + 维度 | 都城位置 |
| `outposts` | 坐标数组 | 哨所位置（由 size 推出）|
| `size` | 1~10 | 规模 |
| `development` | 0~100 | 发展度，与 size 反比 |
| `stance` | −100~100 | 政治倾向（主和 ↔ 主战）|
| `traits` | 结构 | 尚武 / 富庶 / 记仇 / 开放（各 0~100）|
| `military` / `treasury` | float | 抽象军力 / 国库 |
| `status` | enum | `alive` / `dead`（v1 只用这两个）|
| `met` | bool | 玩家是否已接触（未接触的只显示"传闻"）|
| `attitudeToParty` | −100~100 | 对**玩家方**的态度 |
| `warOnParty` | bool + 战果 | 是否正与玩家方交战 |
| `materializedAt` | seq | 上次实体化的结算序号（防重复放置）|

### Relation（国家 A ↔ 国家 B）

`{ attitude, state(peace/war/alliance/truce), warScore, lastEvent, truceUntilSeq }`

### Event

`{ eraId, eraOrdinal, seq, type, parties[], text(中文摘要), visible, read }`

`type` ∈ `war_declared` / `battle` / `alliance` / `truce` / `trade` / `collapse` /
`raid`（国家袭击玩家）/ `letter`（国书）/ `player_action` / `era_summary`

### WorldState

`{ schemaVersion, seed, eraId, eraOrdinal, seq, nations[], relations{}, events[](保留 500), unread[](上限 200), letters[], domains[], playerLedger{} }`

- **序列化**：NeoForge `SavedData` 存 NBT；`schemaVersion` 参与迁移判定。
- **seed**：由世界种子派生，首建时写入（换世界 = 新的一套国家）。
- **随机**：全部取自 `hash(seed, seq, 用途标签)` → **同 seed 同输入必得同结果**。

---

## 六、开局生成

| 项 | 默认值（走配置）|
|---|---|
| 国家数量 | **12**（可配 8~16）|
| 国家间距 | 国家之间**最小 1500 格**（最小间距采样布点）；**不判定与出生点的距离** |
| 出生点附近 | **从候选里挑**：把 `stance ≤ 0`（主和）的国家优先放到离出生点最近处；**不改它的数值**（否则与发展度公式打架）；该国的开局态度为中立 |
| 规模 | 每国随机 1~10，分布均匀 |
| 发展度 | `dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))` |
| 政治倾向 | `stance = clamp(−100, 100, 60 − dev + rng(−30,+30))` → **落后更主战、先进更主和** |
| 文化 | 按 culture 池分配（默认只用 HYW 现成两套外观）|
| 名字 / 地名 | 中文名字池（按文化）+ **虚构地名池**（每国一组：都城/边镇/关隘/渡口）|

---

## 七、时代与结算（**数据驱动，不写死数量**）

### 时代表：`data/statecraft/eras.json`

```jsonc
[
  { "id": "landing",  "name": "落地", "ordinal": 0, "durationHours": 12,
    "coefficient": 1.00, "unlocks": ["intel_book"] },
  { "id": "founding", "name": "立国", "ordinal": 1, "durationHours": 20,
    "coefficient": 1.15, "unlocks": [] },
  { "id": "infra",    "name": "基建", "ordinal": 2, "durationHours": 25,
    "coefficient": 1.30, "unlocks": ["watchtower", "declaration"] }
  // …数量、名称、时长、系数、解锁项全部是数据。代码里不出现 "6"
]
```

**规则**：
- 解锁一律按 **`unlocks` 里声明的 era id** 判定，代码只问"当前时代是否已解锁 X"。
- 存档与事件同时记 `eraId`（表意）与 `eraOrdinal`（排序）；
  **时代表变了旧存档不废**：未知 `eraId` 归到最近 ordinal 并记一次警告。
- 望楼 / 望楼 II / 外交动作的子集都在 `unlocks` 里声明。
- **测试**：`eras.json` 换成 3 条、6 条、9 条各跑一遍整套结算 → 证明没有任何地方写死时代数量。

### 三种触发

| 触发 | 频率 | 内容 |
|---|---|---|
| **时代推进**（主节拍） | 由 `eras.json` 决定（默认 6 次） | 全体成长（development / military / treasury）+ 国家间事件 1~3 条 + 国家对玩家的态度与战事推进 + 出一封「天下大势」 |
| **周期小结算** | 每 **2 小时**累计在线时间 | 推进进行中的战争（国家↔国家、国家↔玩家）、停战冷却、态度向 0 回归、国书到期判定 |
| **玩家动作** | 即时 | 宣战/求和/结盟/通商/求援/朝贡 立刻结算并回应 |

**时间口径**：与全包一致——按**累计在线时间**，不跟世界时间、也不跟现实时间。
**不在线不推进**：玩家回来时取未读队列按顺序呈现。

### 时代系数的用法

系数 `coefficient` 作用于军力/国库增长与兵种档位（I~IV）。数值本身在数据里，不在代码里。

---

## 八、国家之间的战争（抽象、确定、不发生成实体）

1. **开战**：由 `stance`、`attitude`、邻接关系共同决定概率（主战国更容易宣战），受停战冷却限制。
2. **每步**（一次小结算）：`战力 = military × (1 + stance/200) × rng(0.85, 1.15)` 比较，胜者推进 `warScore`。
3. **产出事件**：`battle`，中文摘要用**虚构地名池**（例："赤沙国攻破白岩国边镇『石口』"）。
4. **灭亡**：`warScore` 落后超阈值 且 `development < 25` 且连输 ≥3 步 → `collapse` 事件，`status = dead`。
5. **不生成军队、不改世界方块。**

---

## 九、国家与玩家：压力、使节、国书

> 这一节是补 v1.0 的漏洞：没有它，"主战/主和"只是个数字，"御敌时代"和这个系统也接不上。

### 9.1 国家可以对玩家方宣战

- **判定**：`stance`、`attitudeToParty`、双方发展度差、以及**你们最近的扩张速度**（属地数、哨所附近的建造量）共同决定。
  默认：`attitude ≤ −40` 且 `stance ≥ +40` 的国家最容易宣战。
- **宣战表现**：全服公告（聊天）+ 一条 `war_declared` 事件 + 望楼/情报册里立刻可见。

### 9.2 宣战之后它会做什么（v1 只做"袭击"）

- 每个结算周期，交战国家按概率发动一次 `raid`：
  - **实体化一支小队**：兵种取该国的 HYW 档位，规模 `2 + floor(military/25)`，**上限 24 实体**
  - 落点：**你们最近的活动区**（或你们的属地附近），提前 N 秒给预警（聊天 + 事件），不是凭空糊脸
- **可选联动**：若 `The Hordes` 在场且开启，可把"国家来袭"配成一次 horde 事件（拆墙档位直接复用）
- **HYW 缺席时降级**：不出实体，只出事件与损失（见第十二节）

### 9.3 打赢 / 打输的后果

| 情况 | 后果 |
|---|---|
| 你们守住 | 该国 `military`/`treasury` 受损、态度回升一点、可能主动递**求和国书** |
| 你们被压制（领地内被拆若干次且未反击） | 你们损失资源与建筑；该国态度更硬、要价更高 |
| **v1 明确不做"玩家被灭"** | 只做压力与损失。朋友服被 AI 灭国是挫败设计，不是我们要的 |
| 你们反推、打下它的都城 | 触发第十节 10.4 的吞并 |

### 9.4 国书（国家主动找你）

国家会主动递**可交互事件**，带条件与期限，例如：
- "停止在边境继续建造，否则视同挑衅"
- "与我们共同讨伐 X 国"
- "朝贡 N 资源，换 X 个结算周期的互不侵犯"

- **v1 形态**：在望楼/情报册里作为**可回复条目**（接受/拒绝/忽略，忽略视为拒绝）。
- **明确不在 v1**：实体使节走到你面前（列为后续）。

---

## 十、玩家侧

### 10.1 信息站升级路径

| 阶段 | 载体 | 能力 | 解锁依据 |
|---|---|---|---|
| 早 | **情报册**（物品） | 看已接触国家的基本信息、读未读事件、回复国书 | `eras.json` 的 `intel_book` |
| 中 | **可建造的「望楼」**（方块） | 更细情报（军力/态度/正在进行的战争）+ **遣使**发起外交 + **宣战/朝贡必须在此发起** | `eras.json` 的 `watchtower` |
| 后 | **望楼 II** | +2 可追踪国家 · 显示各国正在进行的战争与同盟 · 每结算可深挖 1 国详细情报 | `eras.json` 的 `watchtower_2` |

**望楼的实体形态（避免 M3 返工，先钉死）**：**单方块**，升级 = 用升级件对其使用（方块状态变体 + 外观变化），**不做多方块结构**。

### 10.2 界面

列表（国家 + 状态徽记）→ 详情（属性、对玩家方态度、最近 5 条事件、可发起的外交动作、国书回复）→ 未读队列。

**实现顺序**：v1 先用**成书**把数据与规则跑通 → 再补自定义界面（网络包）。**规则与数据先对。**

### 10.3 外交 6 动作

| 动作 | 前置 | 后果 |
|---|---|---|
| **宣战** | 已接触；非同盟；望楼前发起（全服公告） | 进入 `war`；态度大跌；可攻打其都城；它也会来打你们 |
| **求和** | 处于 `war` | 按战果判定成败；成功转 `truce`（冷却 = **1 个结算周期**）|
| **结盟** | 态度 ≥ +40；非战争 | 转 `alliance`；互相在战争中提升彼此战力 |
| **通商** | 态度 ≥ 0 | 双方 treasury 提升；解锁该国可交易商品（在都城交易）|
| **求援** | 处于 `alliance` | 按对方军力与态度决定是否驰援：抽象提升你方战力；**可选**派出援军实体（复用 HYW 兵种）|
| **朝贡** | 你方发展度高于对方；望楼前发起（全服公告） | 支付资源换态度与"不侵犯"承诺（v1 只做态度与承诺）|

### 10.4 吞并（玩家主动攻城的唯一实体化战场）

对某国宣战 → **打它的都城**（战斗交给 HYW）→ 守军被清空 → 触发占领：

1. 生成「占领碑」方块
2. 该国 `status = dead`，从名单消失（关系矩阵清行/列）
3. 都城登记进**玩家属地清单**（望楼界面可见；v1 不做税收）
4. `collapse` 事件入国史；该国原哨所转为废墟（不再实体化）

---

## 十一、数值与公式（默认值，全部走配置）

| 项 | 默认 |
|---|---|
| 发展度 | `dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))` |
| 政治倾向 | `stance = clamp(−100, 100, 60 − dev + rng(−30,+30))` |
| 外交动作成功率 | `p = clamp(0.05, 0.95, 0.5 + (attitude−50)/200 + (devYou−devThem)/200 + stance/400)` |
| 停战冷却 | 1 个结算周期（2 小时累计在线时间）|
| 态度回归 | 每小结算向 0 靠 1~2 点 |
| **玩家行为 → 态度** | 击杀其单位 −1/次（每周期上限 −15）· 在都城 64 格内放置方块 −2/次（上限 −10）· 通商 +3 · 完成国书请求 +10 |
| 国家宣战门槛 | `attitude ≤ −40` 且 `stance ≥ +40`（两者同时满足才进入候选）|
| 袭击规模 | `2 + floor(military/25)`，**上限 24 实体** |
| 实体化（哨所数） | `floor(size/2)`（0~5）|
| 实体化（守军） | `4 + size × coefficient`，**上限 40 实体** |
| 实体化节流 | 同一次结算最多 1 处；两次间隔 ≥5 分钟；其余排队 |
| 事件保留 / 未读上限 | 500 / 200（归档 ≠ 丢失）|

---

## 十二、失败与边界

| 情形 | 处理 |
|---|---|
| **存档 schema 变更** | `schemaVersion` 判定；不匹配走迁移；最坏"重置国家系统"而**不让存档报废** |
| **时代表变更** | 未知 `eraId` → 归到最近 ordinal + 记一次警告；不废档 |
| **结算途中崩溃** | 写事务日志（结算序号 + 输入哈希）；因确定性 + seed，重启后从上一个完整结算重放 |
| **外部输入异常** | 配置与命令的所有数值区间钳制 + 日志，绝不写入非法状态 |
| **国家灭亡 / 玩家占领** | 事件与国史保留；关系矩阵清理；`dead` 不再参与结算 |
| **HYW 缺席（软依赖）** | 降级为**纯推演 + 情报**：不派实体、不刷守军、袭击只出事件与损失；望楼界面提示"缺少军事模块" |
| **都城 / 哨所放置失败** | 选址检查（非海洋、坡度阈值、无已生成结构、无玩家建筑与领地）→ 不合格换候选点（最多 8 次）→ 仍失败则标 `undecided`，下一结算重试；放置方式为**先平台化再放模板**，避免悬空/淹水 |
| **中途移除 mod** | 按 MC 自身行为，望楼/占领碑会变成空气并记一条"丢失方块"日志（**不崩**）；`WorldState` 留在存档中不被读取；装回后可继续 |
| **性能** | 关系矩阵 O(n²)：12 国 = 66 对，可忽略；实体化只在玩家靠近时发生并受第十一节节流约束 |

---

## 十三、已知风险（本机实测）

1. **RCON 命令通道不可靠**：项目自带 `server_ctl.py` 与手写裸 RCON 客户端都能登录成功但只拿到空响应，服务端日志也无命令痕迹。
   → **所有自动化测试都跑在 `core` 层**；命令只留人工调试。
2. **HYW 的实体能力是静态证据充分、活体验证未完成**（103 个注册实体、两套敌对势力、攻城器械、群体寻路）。
   → 列为这个 mod 的**第一个验证项**。
3. **运行时放置都城的观感与地形融合**是 M4 的最大技术风险（第九/十二条给了缓解规则，但必须真机看一次）。

---

## 十四、测试与验收

### JUnit（主力，离线，天天跑）

| 用例 | 断言 |
|---|---|
| **N 时代快进** | `eras.json` 用 3 / 6 / 9 三套各跑一遍整套结算，均不抛异常、事件数与周报数落在预期区间 |
| seed 复现 | 同 seed 同输入 → 两次运行状态逐字段相等 |
| 大小与发展度反比 | 生成 1000 次，相关系数显著为负 |
| 国家间战争 | 强打弱胜率高；灭亡边界（dev=24 vs 25、连输 2 步 vs 3 步）|
| **国家对玩家的压力** | 宣战门槛（attitude/stance 边界）；袭击规模上限 24；守住/被压制的后果分支 |
| 外交状态机 | 每个动作前置被违反时**必须拒绝**；状态转移合法；停战冷却生效 |
| 玩家行为 → 态度 | 每周期上限不被突破；通商/完成国书的正确加成 |
| 国书 | 到期判定、忽略视为拒绝、完成后的态度与后续 |
| 态度漂移 | 回归速率与事件加成符合表 |
| 未读与归档 | 500 / 200 上限与折叠行为，归档不丢事件 |
| schema 与时代表迁移 | 旧快照可迁；未知 eraId 归位不崩 |
| 软依赖降级 | 关闭 HYW 分支时，结算仍能跑完且不产生实体 |
| 周报文本 | 快照测试（中文模板不漏变量、不出现 `%s`、地名池不越界）|

### 游戏内（攒成一次，不零散占用时间）

1. 望楼建造、升级与界面手感
2. 靠近都城/哨所时实体化的观感（含地形融合）
3. 一次完整链路：宣战 → 扛住它的袭击 → 反推打都城 → 占领

### 交付物

- mod 源码（`mods/statecraft/`，NeoForge MDK + Gradle，Java 21）
- 数据包 / 资源包（第十五节的数据文件）
- README：**怎么加一个国家 / 一条条约 / 一个事件 / 一个时代**
- 配置项清单与默认值说明

---

## 十五、数据文件清单（这就是"怎么加内容"的入口）

| 文件 | 内容 |
|---|---|
| `eras.json` | 时代表：数量、名称、时长、系数、解锁项 |
| `nations.json` | 国名池（按文化）、规模/发展度/倾向的取值范围 |
| `cultures.json` | 文化 → 外观与兵种档位（HYW 皮肤包/兵种 id）映射 |
| `places.json` | 虚构地名池（都城/边镇/关隘/渡口），供周报取词 |
| `events/*.json` | 事件模板（含中文文本模板与变量）|
| `treaties.json` | 外交动作定义：前置、成功率修正、后果 |
| `letters/*.json` | 国书模板（条件、期限、接受/拒绝后果）|
| `capital_templates.json` | 都城模板与档位（小/中/大）、哨所模板 |

---

## 十六、里程碑（详细实现计划另行编写）

| 里程碑 | 内容 | 验收 |
|---|---|---|
| **M1** | `core` 骨架：数据模型 + 生成 + 结算器 + 外交状态机 + 国家对玩家的压力 + 文本模板 | JUnit 全绿（含 3/6/9 时代表快进）|
| **M2** | 持久化（SavedData + schema/era 迁移 + 事务日志）+ 命令 | 存档往返一致；崩溃重放；时代表变更不废档 |
| **M3** | 情报册 + 望楼 + 周报 + 国书（成书形态） | 游戏内可读，内容来自真实状态 |
| **M4** | 都城/哨所实体化 + 守军 + 袭击 + 占领 → 吞并 | 完整链路真机跑通一次 |
| **M5** | 自定义界面（网络包）+ 望楼 II + 打磨 | 手感验收 |
| **后续** | OPAC 接管、实体使节、附庸/朝贡深度、自造文化外观、AI 史官接周报 | — |

---

## 十七、明确不在 v1 范围

- 世界内的扩张模拟（**按设计决策，永久不做**——用信息流表现）
- **玩家被国家灭亡**（v1 只做压力与损失）
- 实体使节走到玩家面前（v1 只有可交互国书）
- 附庸国的内政与朝贡结算深度（v1 只有"灭亡"）
- 都城建筑的时代升级套件（v1 只做"平台化 + 放模板 + 刷守军"）
- 玩家属地的税收/产出（v1 只登记 + 界面可见）
- 接管进 OPAC 领地（需先核实 API）
- 多文化自造外观（v1 复用 HYW 现成的两套）
- 接入 AI 史官（v1 周报用模板生成）
