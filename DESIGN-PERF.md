# 性能优化方案（加载速度 + 帧数）

> **这份文档回答什么**：怎么让这个包"启动/进世界更快、跑起来更稳"。
> 日期起点 2026-09-23 · 所有数字都来自 `tools/perf_report.py` 从**真机日志**里读出来的事实
>
> 规矩（本轮定的）：**先量基线，再动手；每个改动都要有前后对比**。
> 这条不是形式主义 —— 我在任务书排版那件事上因为"优化一个没校准过的指标"连错三次。

---

## 一、现状基线（2026-09-23 实测）

### 1.1 玩家自己那次会话（`logs/2026-09-22-1.log.gz`，23:45 起）

| 阶段 | 时刻 | 耗时 |
|---|---|---|
| JVM 启动（ModLauncher） | 23:45:19 | — |
| 资源管理器重载 | 23:46:11 | **+52 s** |
| 加载配方（进世界） | 23:52:36 | （中间 388 s 是**玩家在主菜单的操作**，不算加载） |
| 集成服务端起 | 23:52:39 | — |
| **世界就绪** | 23:52:42 | **+6 s**（`Time elapsed: 453 ms`） |

**卡顿**：4 次 `Can't keep up!`，累计落后 **29.8 秒**，最严重 **17.9 s / 357 ticks**。
（00:04 那次备份期间还有一次 8.8 s。）

### 1.2 受控启动（`tools/perf_baseline.py`，quickPlay 进 `scdev`，无人工 idle）

第一次跑出来 277 s 启动 + 卡顿 433 s —— **但那次的日志被污染了**：
同时有两个客户端在跑，两段会话交错写进同一个 `latest.log`。
现在 `perf_report.py` 会检测时间戳回退并**拒绝出数**（退 2），
`perf_baseline.py` 也会在启动前**数 java 进程**，有别的实例就不启动。

> **为什么要写这条**：两个客户端把 15.8 GB 内存顶满（可用一度只剩 **1.4 GB**），
> 机器被拖到假死 —— 这正是玩家说的"似乎卡住了"。
> **测性能时只能有一个客户端**，这不是洁癖，是测量前提。

### 1.3 GC（`-Xlog:gc*`，8 GB 堆，119 秒窗口）

| 指标 | 实测 |
|---|---|
| 堆上限 | **8192 MB**（确认 `-Xmx8G` 生效） |
| 暂停次数 | **137 次**（≈ **1.53 次/秒**） |
| 累计暂停 | **5.30 秒** |
| **最大单次** | **448 ms**（Mixed GC，堆 6050M → 2498M） |
| ≥100 ms 的暂停 | 多次（448 / 258 / 240 / 227 / 158 …） |

**⇒ 150~450 ms 的 Mixed GC 反复出现，是"顿挫"的实锤之一。**
（它解释不了 17.9 s 那种巨停 —— 那是另一类原因，见 §三。）

---

## 二、已经能确定的两件事

### 2.1 客户端堆开得太大（12 GB / 机器 15.8 GB）

证据：崩溃报告里读到 `up to 12884901888 bytes (12288 MiB)`；机器
`Win32_OperatingSystem` = **15.8 GB**。
12 G 堆 + 非堆（LWJGL / 纹理 / 直接缓冲 2~4 G）+ 系统 ≈ 顶满 →
Windows 会压缩/换页，表现就是**又卡又慢**。
（实测量：跑着一个客户端时可用内存一度只剩 **1.4 GB**。）

### 2.2 预生成只解决一部分

Chunky 已在包里。按区块数算（`DESIGN-PREGEN.md` §5.1）：

| 半径 | 区块数 | 纯生成耗时（估） |
|---|---:|---:|
| 1024 方块（r=64） | 16,641 | ~0.2 h |
| 2048 方块（r=128） | 66,049 | ~0.9 h |
| 8192 方块（r=512） | 1,050,625 | ~15 h |

**"整片预生成"不现实**；现实做法是**先把常用区域铺好**（r=64~128），
让"边走边生成"的卡顿不再出现在基地附近。

---

## 三、还没定位的那一类卡顿（**下一步要查的**）

17.9 s / 433 s 这种巨停，GC 解释不了。候选原因（按可能性排）：

1. **同时跑两个客户端**（已证实发生过一次）—— 内存顶满 → 换页 → 假死
2. **区块生成 + 结构生成**（Terralith / YUNG's / Towns&Towers / Repurposed /
   dungeons_arise / Tidal Towns 全是世界生成重户）在同一批区块上叠加
3. **SimpleBackups 同步备份**：实测一次 **7.389 s**，紧随其后就是 8.8 s 的 `Can't keep up`
4. **自动保存**（`Saving chunks for level ...` 之后常跟巨停）
5. 某个 mod 的 tick 逻辑（需要 spark 才能定位到具体 mod）

**定位手段**：`spark`（包里已装）。
- `/spark tps` → 服务端 tick 健康
- `/spark profiler start --timeout 60` → 采样出**具体是哪个方法在吃 CPU**
- 结合 `debug.log`（7.2 MB，比 latest.log 详细得多）

---

## 四、实验设计（**同时只跑一个客户端**）

| 编号 | 目的 | 变量 | 状态 |
|---|---|---|---|
| E1 | 基线 | `-Xmx8G`，JDK 默认 G1 | ✅ 世界就绪 **+130.5 s**（854×480）|
| E2 | GC 调参 | `-Xmx8G` + Aikar's flags | ✅ **否决**（最坏暂停 319→3101 ms）|
| E3 | 堆大小 | `-Xmx12G` 对比 E1 | ⬜ 未跑（本机仅 15.8 GB，12 G 堆会换页）|
| E4 | 预生成 | Chunky 预生成 432 方块见方 → 再跳进该区域 | ✅ **实测有效**：探索巨停 **14.8 s → 3.4 s（−77%）**；但治不了"进世界卡顿"（§11.4）。见 §十二 |
| E5 | 定位巨停 | spark / MC 自带 `/debug` 采样器 | 🟡 已具名两个来源（WorldEdit 状态表 5787 ms、RoadWeaver 673 结构）；剩余 2~8 s 未具名 |
| E6 | 客户端画质 vs 帧数 | `graphicsMode` / `particles` / `renderClouds` / `entityDistanceScaling` | ✅ **四项合计 +20%**（Fancy 单独 +10%、另三项 +9%），见 §8.4 |
| E7 | Every Compat | `dynamic_assets_generation_mode` ALWAYS→CACHED_ZIPPED | ❌ **热缓存打平、冷缓存 +35 s，已回滚**，见 §七 |
| E8 | 移走 `chipped`/`rechiseled` | mods（占全部注册项 41%）| ⬜ **需玩家拍板**（改内容）。**现在有机制证据**：尾段的服务端热点就是方块状态的属性查表/哈希（§11.1）⇒ 砍变体会直接削掉它 |
| E9 | ModernFix `dynamic_resources=true` | config（默认关闭）| ❌ **实测负优化（+155.1 s），已回滚**，见 §8.5 |
| E10 | 窗口分辨率 | 854×480 vs 1920×1080 | ✅ 1080p 慢 **10~15 s（+8~11%）** |
| E11 | 具名元凶 `farm_and_charm:mincer` | 13332 个方块状态 → WorldEdit 建表 5787 ms | ✅ 已定位（处置待定：报作者 / 换版本 / 接受）|
| E12 | 进世界后头 90 秒的巨停 | WorldEdit 状态表 + RoadWeaver 结构发现 | ✅ **已归因**（JFR，§十一）：尾段 = 状态空间查表/哈希 + jar/zip 解码 + 剔除；**配置层已无可压缩的开关** |
| E13 | JFR 采样（新仪器） | `-XX:StartFlightRecording` + `tools/jfr_hotspots.py` | ✅ **可用**（唯一能覆盖尾段的手段；注意它本身拖慢启动约 18%）|


每条都记 `data/perf/<label>.json`，同一套口径比对。

---

## 四之三、启动耗时：三次对照 + 两次**更正**（2026-09-23）

### 三次同口径对照（都是 8G、同一存档、150 秒驻留、每次只一个客户端）

> ⚠️ **这张表改过一次。** 第一版引用的是 ModernFix 的三句话
> （`Game took 158.6 s` / `Time from main menu to in-game 60.2 s` / 合计 218.8 s）。
> 2026-09-23 逐条对时间戳后发现**那三句话不是加载阶段**（§四之四），
> 现在换成「日志时间戳」口径 —— 任何人都能拿日志自己复算：

| 对照 | → 资源重载 | → 集成服务端起 | **→ 世界就绪** |
|---|---|---|---|
| base（JDK 默认 G1） | +49.6 s | +124.3 s | **+130.5 s** |
| aikar（Aikar's flags） | +48.2 s | +120.1 s | **+125.3 s** |
| novc（关掉 NeoForge 版本检查） | +49.9 s | +125.9 s | **+131.9 s** |

三者合计差 ≤6.6 s（5%），**与重复跑自身的噪声同量级** —— 见 §四之四 的重复性证据。

**两个假设都被否掉**（都在噪声范围内，±3%）：
- **Aikar's flags**：不仅没改善加载，还把最坏 GC 暂停从 319 ms 放大到 **3101 ms** → 否决
- **关掉版本检查**：启动没有变化 → **已回滚**（规矩：只留实测有效的改动）

### 为什么"关掉版本检查"没用（**我第一版归因错了**）

`tools/perf_gaps.py` 第一版量的是**所有日志行**的相邻时间差，于是报出
"29 个空档合计 155.9 秒，绝大多数是网络更新检查超时"。
**这个结论是错的**：那些检查跑在**独立线程**（`[NeoForge Version Check/...]`），
它们的 5~15 秒超时是**和主线程的 mod 加载并行**的，不在关键路径上。

**修法**：`perf_gaps.py` 加了 `--thread` —— 只看**同一个线程**的相邻行，
换线程就断链。修正后 main 线程只有 **3 个 ≥1s 的空档、合计 5.7 秒**。

> **硬规则 23**：跨线程的日志空档**不能**当成串行成本。
> 静态日志分析只能提出**假设**，验证必须靠 A/B（改一个变量、各跑一次）。
> 本轮就是"静态分析提了个漂亮假设 → A/B 直接否掉"的完整例子。

### 那么那些秒数花在哪（**这一小节的第一版算错了，见下**）

> ⚠️ 原文这里写"`Reloading ResourceManager` 在 +44 s、游戏可玩在 +158 s ⇒ 资源重载约 114 秒"。
> **这两个数字本身就是错口径的产物**（见 §四之四）：按日志时间戳，
> 资源重载在 **+55 s** 左右，世界就绪在 **+136~155 s**，两者之间的 **79~95 秒**才是
> "资源重载剩余部分 + 主菜单 + 世界数据包/配方加载"。
> 正确的构成表在 **§七**，全部 11 次受控跑在 **§十**。

注册项统计（`tools/item_index.py --stats`）仍然有效，它指向"模型为什么这么多"：

| 命名空间 | 注册项 | 性质 |
|---|---:|---|
| **chipped** | **6,993** | 纯方块样式变体 |
| **rechiseled** | **3,628** | 纯雕刻方块变体 |
| minecraft | 1,952 | — |
| railways | 1,951 | — |
| create | 901 | — |
| **合计** | **26,024** | 前两个占 **41%** |

**⇒ `chipped` + `rechiseled` 是"注册项/模型数量"上的最大单块**：10,621 个注册项
（= 同样数量的模型要烘焙），却**不提供任何玩法**，只是装饰变体。

代价也明确：建筑/装饰两章有引用它们的任务，删了要改；而且**已存在的存档**里
如果放过这些方块，移除 mod 会留下未知方块（`scdev` 是测试档，用户的档要单独评估）。
**这是要玩家拍板的事** —— 我给数字，不替他决定。

### 还没跑的

| 编号 | 目的 | 为什么值得 |
|---|---|---|
| E7 | Every Compat 改 `CACHED_ZIPPED` | 它每次会话重算 **436（客户端）+ 438（服务端）** 个动态任务，是"资源重载→进世界"那 79 秒里最大的一块可动项。**仍未跑** |
| E8 | 临时移走 `chipped` 并测启动 | 直接验证"41% 注册项"值多少秒（需玩家同意，且要新存档测） |


## 四之二、E1 / E2 的实测结果（2026-09-23）

同一天、同一台机器、同一个存档（`scdev`）、同样 150 秒驻留、**每次只有一个客户端**：

| 对照 | GC 暂停 | 累计暂停 | **最大单次** | ≥100ms | → 资源重载 | **→ 世界就绪** |
|---|---|---|---|---|---|---|
| **E1 base-8G**（JDK 默认 G1） | 262 | 10.50 s | **319 ms** | 31 | +49.6 s | **+130.5 s** |
| **E2 aikar-8G**（Aikar's flags） | 254 | 12.78 s | **3101 ms** | 22 | +48.2 s | **+125.3 s** |

**结论：Aikar's flags 在本机是负优化 —— 否决，保留 JDK 默认 G1。**

- 最坏暂停 **319 ms → 3101 ms**（放大 10 倍）—— 这一条就足以否决：玩家感觉到的顿挫
  是"最坏那几次"，不是平均数。**这条依据来自 GC 日志，不受口径更正影响。**
- 累计暂停 10.5 s → 12.8 s（更差）。
- 加载没有可辨别的改善：+130.5 s → +125.3 s（差 5 s = 4%，与重复跑噪声同量级）。

> **为什么要把否定结论写下来**：Aikar's flags 是社区里被反复推荐的"标配"，
> 下一个人（或下一个我）很可能"顺手加上"。**没有这条实测记录，就会重犯。**
> 顺带一个方法论：`-XX:MaxGCPauseMillis` 是**目标值不是保证值**，
> 它会让 G1 用更大的年轻代去换频率 —— 于是平均更稳、最坏更糟。

**两次都测得 `Can't keep up!` = 0 次**（一个客户端、挂机 150 秒）。
而玩家那次真实会话有 **4 次 / 累计 29.8 秒**（日志已确认没被污染）。
⇒ **卡顿跟"有没有在动"强相关**：探索生成区块、自动保存、备份才会触发。
所以 E4/E5 必须**在有活动量的场景下测**，挂机基线测不出来。


---

## 四之四、口径更正：ModernFix 那三句话**不是**加载阶段（2026-09-23）

### 我错在哪

第一版把 `Game took X` 当成"启动到主菜单"、`Time from main menu to in-game Y` 当成
"主菜单到进世界"，于是 base = 158.6 + 60.2 = 218.8 s。**这三句话不能这么读。**

**证据（novc 那次会话，`logs/2026-09-23-6.log.gz`，4176 行、时间戳单调）：**

| 行 | 时刻 |
|---|---|
| `ModLauncher running`（JVM 第一行） | 08:48:01.586 |
| `Reloading ResourceManager` | 08:48:51.489（**+49.9 s**）|
| `Starting integrated minecraft server` | 08:50:07.456（+125.9 s）|
| `Time elapsed: 3080 ms`（世界就绪） | 08:50:13.453（**+131.9 s**）|
| `Game took 159.575 seconds to start` ← **印在这里** | 08:50:39.267 |
| `Total time ... 221.26004 seconds` ← 印在这里 | 08:51:08.951 |

两条推论，都能自己验算：

1. **`Game took` 的零点是"进程启动"，终点是"它被打印的那一刻"**，而打印发生在
   **世界就绪之后 26 秒**（08:50:39 − 08:50:13）。反推零点 = 08:50:39.267 − 159.575 =
   **08:47:59.69**，比 JVM 第一行还早 2 秒（日志系统初始化前的那点时间）。
   ⇒ 它量的是"JVM 运行了多久"，**里面混进了世界就绪之后的那段空转**。
2. **`Total` = `Game took` + `main menu to in-game`**（159.575 + 61.685 = 221.260 ✓，
   小数完全对上），它不是独立的第三个数，而是前两个的和。

**后果**：同一台机器、同一份配置，只要"打印时机"落在世界就绪之后 26 秒还是 35 秒，
这两个数就差 9 秒 —— 拿它们做 A/B 是在量打印时机，不是在量加载。

### 换成的口径（自己能复算）

`perf_report.py` 一直在印这张表，只是我以前没看它：

> JVM 第一行 → `Reloading ResourceManager` → `Starting integrated minecraft server`
> → `Time elapsed:`（世界就绪）

全是日志时间戳，逐行可查。以后**所有启动数字都用这一套**。

### 顺带发现：同配置重复跑的噪声有多大

| 会话（2026-09-23） | 配置 | → 资源重载 | → 世界就绪 |
|---|---|---|---|
| 08:35 base-8G | 854×480，默认 G1 | +49.6 s | +130.5 s |
| 08:40 aikar-8G | + Aikar's flags | +48.2 s | +125.3 s |
| 08:48 novc-8G | 版本检查关 | +49.9 s | +131.9 s |
| **09:39 手工 FPS 会话**（`autotest.py`） | **同一个包** | **+93.6 s** | **+194.8 s** |
| 09:51 r1-1080p | 1920×1080 | +59.0 s | +148.7 s |

前三次（同一工具、同一分辨率）**极稳**：世界就绪 125.3 / 130.5 / 131.9 s，极差 6.6 s（5%）。
而 09:39 那次慢了 **63 秒**，且 main 线程空档 **28.9 s vs 10.4 s** ——
**每个 I/O 重的步骤都一起变慢**（mixin 初始化 7.8 s vs <0.5 s），不是某一步的锅。
那次是为拿 FPS 用 `autotest.py` 跑的（少了 GC 日志参数、多了 options 自动化），
**不能与受控跑混在一张表里比**。

> **规矩（补进硬规则）**：不同**启动方式**（工具 / 参数 / 分辨率）的会话不进同一张对照表；
> 单次跑不出结论，**至少两次重复**，先看重复之间的极差，再谈改动效果。
> 09:51 那次（1080p）比三次 854×480 慢约 18 s，提示**窗口分辨率本身影响启动耗时**
> —— 所以启动耗时对照与 FPS 对照**都要固定分辨率**。

### 两个工具 bug（都是"假绿"）

| 症状 | 真因 | 修法 |
|---|---|---|
| 保存的 `.md` 报告里"加载各阶段"**永远是空的** | `perf_baseline.py` 调 `perf_report.py` 时只传 `--gc-log`、**没传 `--log`** → 报告把 GC 日志当主日志解析 | 补 `--log logs/latest.log` |
| `perf_gaps.py` 对 `.gz` 报"**0 个空档**" | 直接 `read_text()` 读 gz = 把压缩字节按 utf-8 解码 → 4176 行的会话只解析出 2200 行乱码 | 加 gz 支持（`gzip.open`）|

第二个尤其危险：它当时"证明"了 novc 那次没有空档，我差点据此得出"慢与 I/O 无关"。
**检查器口径错了，绿灯比红灯更贵。**

---

## 五、候选"加装 mod"（**先验证再装**，不许凭印象）

按 `mod-selection` 的规矩：先确认 loader/版本/依赖窗口，再进候选表。

| 候选 | 想解决 | 待验证 |
|---|---|---|
| `Exordium` | 客户端渲染批处理 → 帧数 | 有没有 1.21.1 NeoForge 版 |
| `ThreadTweak` | 服务端/客户端线程优先级 → 抖动 | 同上 |
| `FastSuite` / `RecipeEssentials` | 16472 条配方的查找开销 | 同上 |
| `Chunky` | 预生成 | **已在包里** |

**不装的**：C2ME / Krypton / Sodium（都是 Fabric 侧；NeoForge 对应物是 Embeddium+Lithium，
已在包里）。

---

## 六、玩家侧只需要做三件事（我改不了你的启动器与客户端设置）

### 6.1 客户端内存 12 G → 8 G（**最重要**）

- 证据：崩溃报告读到 `up to 12884901888 bytes (12288 MiB)`；机器
  `Win32_OperatingSystem` = **15.8 GB**。
- 12 G 堆 + 非堆（LWJGL/纹理/直接缓冲 2~4 G）+ 系统 → 顶满 → 换页 → 又卡又慢。
- 实测：跑一个客户端时可用内存一度只剩 **1.4 GB**；两个客户端直接假死。
- 8 G 是实测跑得动 231 个 mod 的值（E1/E2 两次都在 8 G 下跑完）。

### 6.2 客户端画面设置（`options.txt`）—— **已实测，不再是推测**

下面每一项的"值"都是 2026-09-23 在 1920×1080、正午、同位置、`enableVsync:false` 下
实测量出来的（方法见 §8；原始采样图在 `data/perf/shots/`）：

| 项 | 玩家原值 | 现值 | 实测效果 | 视觉代价 |
|---|---|---|---|---|
| `graphicsMode` | 2（Fabulous） | **1（Fancy）** | **+10%**（129.5→143 中位数） | 几乎没有（Fabulous 只多一层透明/实体阴影 pass）|
| `particles` | 0（全部） | **2（最少）** | 与下两项合计 **+9%** | 明显：大部分粒子不再画 |
| `renderClouds` | `"true"` | **`"fast"`** | 同上 | 明显：云从 3D 变扁平 |
| `entityDistanceScaling` | 1.0 | **0.5** | 同上 | 远处生物不渲染 |
| `enableVsync` / `maxFps` | true / 120 | **true / 120（已还原）** | — | 关掉才能量出上限，所以只在实验期间关 |

**合计 +20%**（119 → 143 fps 中位数）。但注意 §8.3：正午这种轻场景你本来就顶在 120 上限，
**换画质看不到差别**；这 +20% 买到的是重场景（夜晚、怪多、基地大）的余量。

**想逐项回退**（例如舍不得云和粒子）：
```
python tools/options_set.py 'renderClouds="true"'          # 云恢复高品质
python tools/options_set.py particles=0                     # 粒子恢复全部
python tools/options_set.py entityDistanceScaling=1.0       # 实体距离恢复
python tools/options_set.py graphicsMode=2                  # 画质恢复 Fabulous（-10%）
python tools/options_set.py --restore                       # 或者整份还原到 2026-09-23 之前
```


### 6.3 测性能时只开一个客户端

不是洁癖，是测量前提：两个客户端会互相污染日志并把内存顶满（这就是一次"卡住"的成因）。

### 6.4 剩下那几秒的巨停怎么继续查（**不需要你操作**）

已经具名两个来源（见 §九）：WorldEdit 给 `farm_and_charm:mincer`（13332 个方块状态）
建表 **5787 ms**、RoadWeaver 扫 **673 个结构**。
剩下进世界后 40~120 秒里的 2~8 秒卡顿还没定到方法上 —— 但**我可以自己跑**
（在测试存档里用自动化敲 `/spark profiler start --timeout 60` 或 MC 自带 `/debug` 采样器），
不需要玩家出手。已列入 `NEXT.md` 的性能待办。

---

## 七、启动耗时的构成（口径已更正，见 §四之四）

以 r1（1920×1080）为例，全部相对 **JVM 第一行**：

| 里程碑 | 相对启动 | 说明 |
|---|---|---|
| `ModLauncher running` | +0 s | JVM 第一行 |
| ModernFix 配置 / `max.bg.threads=5` | +25 s | |
| `Reloading ResourceManager` | **+59 s** | 资源重载开始 |
| Every Compat 动态资源任务 | +64 s | **436 个（分批 55）** |
| 配方加载完成（16472 条） | +136 s | 世界数据包 |
| `Starting integrated minecraft server` | +138 s | |
| 世界就绪（`Time elapsed:`） | **+149 s** | |

⇒ **大头是"资源重载开始 → 集成服务端起"这一段（约 79 秒）**：资源重载的剩余部分
+ 主菜单 + 世界数据包与配方加载。
（§四之三 早先那版写"资源重载约 114 秒"是引用错口径的产物，**已作废**。）

可动的地方（按性价比）：
1. **Every Compat**：`ALWAYS` 每次会话重算 **436 个**动态任务（还有 438 个服务端任务），
   是上面这 79 秒里最大的一块可动项 → **E7 候选**（改 `CACHED_ZIPPED` 对比启动耗时，**尚未跑**）。
2. **资源重载**：包里那个 5.2 MB 的中文补全资源包 + `serilum_mod_translations`
   都参与每次重载（要不要留由玩家决定，我不擅自删）。
3. **mod 数量**：231 个是根本成本；其中 `chipped` + `rechiseled` 占全部注册项 **41%**
   （10,621/26,024）却不提供玩法 → **E8**（需玩家拍板）。
4. ~~ModernFix `dynamic_resources`~~：**实测负优化，已回滚**（§8.5）。

### 七之二、玩家真正"能玩"是在**再加 58 秒**之后（2026-09-23 新增口径）

`Time elapsed:` 只是**服务端**把出生区块准备好了。客户端这时还在显示 **"加载地形中…"**，
真正能玩要等它把世界接过来 —— 这个时刻在日志里有明确一行：
`net.minecraft.advancements.AdvancementTree … Loaded N advancements`（**第二次**出现的那次）。

| 跑次 | → 世界就绪 | **→ 客户端可玩** | 尾段 |
|---|---|---|---|
| r10 | +136 s | **+195 s** | +59 s |
| r11 | +142 s | **+196 s** | +58 s |
| r12 | +138 s | **+200 s** | +58 s |
| r13 | +140 s | **+198 s** | +58 s |

**尾段非常稳定（58~59 秒）**，而且**卡顿就发生在这一段里**：
r13 最严重的一次 `Can't keep up! 2156 ms` 正好落在 11:26:20 = 客户端可玩的那一刻；
§9.1 那个 WorldEdit **5787 ms** 也在这段窗口内。

⇒ **"加载速度"要按 195~200 秒来谈，不是 136~142 秒。**
（口径已修进 `perf_report.py`：新增"客户端可玩"阶段，并把它作为总耗时的那一行。）

⚠️ 这个 logger 会打**两次**：启动时装数据包那次（`Loaded 9803 advancements`，
在"世界就绪"**之前**）和真正加入世界那次（`Loaded 253 advancements`）。
取第一个会算出**负的间隔** —— 我第一次没限定、第二次限定错了线程，连着踩了两次。
工具里用 `("客户端可玩", pattern, "last")` 显式声明"取最后一次"。

⚠️ **它还可能被"第三次"触发**：r15 里出现 **+290 秒**的假值（那次世界数据被中途重新加载过）。
⇒ 这个阶段是**辅助口径**，数值明显异常时（比如超过 +80 秒）以屏幕证据/其它阶段为准，
不要拿它当铁证。

### E7 实测结果：`CACHED_ZIPPED` **热缓存打平、冷缓存更差 → 已回滚**

Every Compat 的 `dynamic_assets_generation_mode`（客户端）+ `server_assets_generation_mode`（服务端）
一起从 `ALWAYS` 改成 `CACHED_ZIPPED`，其余一切不动：

| 跑次 | 状态 | → 资源重载 | → 集成服务端起 | **→ 世界就绪** | 卡顿 |
|---|---|---|---|---|---|
| ALWAYS 基线（8 次，1080p） | 每次会话重算 436 + 438 个动态任务 | +52~59 s | +130~148 s | **136.2~155.1 s**（中位 ~144）| 0~3 次 |
| **r9** | `CACHED_ZIPPED` **第一次**（建立缓存） | +65 s | +167 s | **+176.0 s** | 3 次 / **19.7 s** / 最坏 14.1 s |
| **r10** | `CACHED_ZIPPED` **第二次**（读缓存） | +53 s | +129 s | **+136.0 s** | 3 次 / 11.2 s |

**判读**

- 缓存命中的 r10 = **136.0 s**，只比基线最快那次（r7 136.2 s）快 **0.2 秒**
  ⇒ **落在噪声带里，不算收益**。
- 冷缓存（改过 mod / 资源包之后的第一次）要多付 **约 35 秒**，卡顿也翻倍（19.7 s，最坏 14.1 s）。
- 生效性验证过：r9 日志里有 `Starting dynamic resources generation tasks: 436 …`，
  **r10 里这行消失**（= 在读缓存）；两个缓存目录也确实在 r9 期间被写入（10:55:04 / 10:56:31）。
- ⇒ **按硬规则 24 已回滚**：两份配置都从备份整份还原为 `ALWAYS`。
  这与 mod 作者自己的注释一致（CACHED 模式"任何磁盘访问都慢"）——
  本机重新生成（纯内存）并不慢，**上游默认是对的**。
- 遗留：两个缓存目录（`dynamic-resource-pack-cache` 846 文件 / 32.4 MB、
  `dynamic-data-pack-cache` 41 文件 / 3.1 MB）在 ALWAYS 下不再被使用。
  它们**本来就在**（9/22 与 9/23 就有），不是我这轮新建的；要清可以自己删。

> **方法论收获（补进硬规则 26 的注脚）**：这是第一个被量出"**冷启动代价**"的改动。
> 凡是带缓存的优化，**只测第二次跑是不够的 —— 必须把第一次（建立缓存）也记下来**，
> 否则会把"第一次多付的 35 秒"藏起来。

### 那 76 秒里到底有没有"卡在某一处"

把 r10 的"资源重载开始 → 集成服务端起"整段（11:01:54 → 11:03:20）按线程切开：

| 线程 | ≥1.5 s 的空档 |
|---|---|
| `Render thread` | **只有 1 个：3.2 s**（`Shader position_tex_blur_vertical …` → `Loaded 0 entity animations`）|
| `Server thread` | **0 个** |
| `Worker-ResourceReload-*` | 每个都不大 —— 这 76 秒是**几十个 worker 线程并行干活的合计** |

**⇒ 这一段没有"某一处卡住"可以摘出来**：它是贴图拼接 + 模型烘焙这类**并行聚合**成本。
想再往下拆只能上采样器（spark / MC `/debug`），或者用"拿掉东西再测"的办法（那就是 E8）。

顺带一条**否定证据**：框架层的动态资源生成其实很便宜 ——
`Finished runtime resources generation for 4 packs in a total of 86.39 ms`。
Every Compat 那"436 个任务"是它自己的内存生成，不走这条框架路径
（这也解释了 E7 为什么改了没效果）。

> ⚠️ **我自己在这里踩了一次硬规则 23**：先用一段临时脚本按时间排了个"多线程混排"的时间线，
> 看到 `Render thread` 与下一行之间有个 **13.9 秒**空档，差点当成"渲染线程卡了 14 秒"。
> 那两行**根本不是同一个线程**（下一行是 `Worker-ResourceReload-0`）。
> 用 `perf_gaps.py --thread "Render thread"` 重算，同一窗口只有 3.2 s。
> **跨线程的空档不是串行成本** —— 这条规矩连自己写的临时脚本也要遵守。



---

## 八、帧数（FPS）：怎么测 + 第一次实测（2026-09-23）

### 8.1 为什么只能"读屏幕"

FPS **不写日志**。它只出现在游戏内 F3 覆盖层上（本包的排版来自 BetterF3），
所以只能：固定窗口分辨率 → 按 F3 → 截图 → 读那一行 `NN fps / 120 fps 垂直同步`。

- 试过 `windows_computer_use_ocr`：中文界面下它把 `90 / 120 fps` 读成 `f / 12@ fps`，
  **数字不可用**（而且一次 OCR 输出 25 KB，比截图贵）。读数一律靠截图人眼核对。
- 规矩：**同一分辨率、同一位置与朝向、同一世界时间、同一天气**；
  每次取多次采样看**中位数**，不看单次（实测同场景 20 秒内能从 74 跳到 106）。

### 8.2 实测采样

| 分辨率 | 场景 | FPS 采样 | GPU 占用 |
|---|---|---|---|
| 854×480 | 夜晚、基地内 | 84 | 33% |
| 1920×1080 | 夜晚、同一位置 | 73 / 90 | 83% / 44% |
| 1920×1080 | **正午**（`/time set 6000` + 冻结昼夜） | 74 / 106 / 119 | 55% / 35% / 34% |

（正午那组的第一个 74 是**改时间后 20 秒**取的，光照更新波还没走完；
后两个是稳定值 —— 这也说明"改完场景要等，别立刻采样"。）

### 8.3 关键发现：正午场景撞在 120 fps 上限上，而且是 **CPU 瓶颈**

`119 / 120 fps 垂直同步` 配 `GPU 使用率 34%` ⇒ 帧数被 vsync/上限卡住，**GPU 还闲着一大半**。
即在这个场景里：

- 渲染管线（Fabulous 的额外透明度/阴影 pass）**不是**瓶颈；
- 瓶颈在**主线程 CPU**（约 8.4~9.4 ms/帧）；
- 想在这个场景涨帧，改画质选项基本没用；要减的是**主线程工作量**
  （区块数/渲染距离、实体与方块实体、粒子、动态光照）。

> **这条修正了我自己在 §6.2 给出的建议。** 那一版把 `graphicsMode 2→1`
> （Fabulous→Fancy）写成"单项最大的帧数开销"—— 那是**没测之前的推测**。
> 实测正午 GPU 只占 34%，Fabulous 的额外开销在这里根本不显形。
> 夜间 GPU 会冲到 83%、FPS 掉到 73，但同一分钟内又回到 90/44%：
> **夜间场景本身不稳定**（夜行怪、动态光照、月色事件都在变），拿它做 A/B 要多采样。

### 8.4 对照实验（`enableVsync:false` + `maxFps:260`，正午，1920×1080，同位置同朝向）

> **为什么要关 vsync 测**：不关它，两边都顶在 119/120，**量不出差别**。
> 这属于"测量用配置"，只在实验期间生效；玩家的 `options.txt` 有整份备份
> （`data/perf/options.txt.original-2026-09-23`）。
> 每次都把世界时间冻在正午（`/time set 6000` + `gamerule doDaylightCycle false`）、天气设晴
> —— 否则昼夜与下雨会污染对照（实测夜间同场景能在 20 秒内从 73 跳到 90）。

| 跑次 | 画面设置 | FPS 采样（6~8 次） | **中位数** | 相对 r4 |
|---|---|---|---|---|
| r4 | Fabulous + 粒子全部 + 云高品质 + 实体距离 1.0（**= 玩家原设置**） | 89, 100, 118, 120, 125, 134 | **119** | — |
| r6 | **Fabulous** + 粒子最少 + 云 fast + 实体距离 0.5 | 92, 112, 122, 129, 130, 138, 147, 164 | **129.5** | **+9%** |
| r5 | **Fancy** + 粒子最少 + 云 fast + 实体距离 0.5 | 102, 129, 130, 140, 146, 154, 157, 174 | **143** | **+20%** |

**结论**

1. 三项便宜设置合起来约 **+9%**（粒子最少 / 云 fast / 实体距离减半）。
2. 单把 Fabulous 换成 Fancy 再约 **+10%**（129.5 → 143）。
3. 1.10 × 1.09 = 1.20，与"四项一起改"实测的 **+20%** 吻合。
4. 组内离散度很大（±10~25%），所以 ±10% 这一档的归因只能说"方向一致"；
   **整体 +20% 是站得住的**（r5 的 8 个采样里有 7 个高于 r4 的中位数）。

**⚠️ 但你实际设置下它的含义不同**：你开着 vsync（上限 120）。正午这个场景
r4 就已经顶在 `119 / 120` ⇒ **在这个场景里换画质你看不到任何变化**。
这 +20% 买到的是**余量**：帧数掉到 120 以下的场景（夜里、怪物多、基地大）
会按同样比例抬上去（例如某处 80 → 96）。

**采样方法（可复现）**：`tools/fps_shot.py` —— 只截 F3 覆盖层左上角 560×120，
采完拼成一张长图。对比过另外两条路：
MCP 的整窗截图会**强制 GL 帧缓冲回读**（采样动作本身拖慢被测帧）且每次带回一整棵 UIA 树；
`windows_computer_use_ocr` 在中文界面下把 `90 / 120 fps` 读成 `f / 12@`，数字不可用。

**这套设置如何回滚**：`python tools/options_set.py --restore`（从 `options.txt.perfbackup` 整份还原）。

> **测试存档 `scdev` 现在的状态**（下次做性能对照前要知道）：世界时间被冻在正午
> （`doDaylightCycle=false`、时间 6000）、天气设成长期晴朗。
> 这是**故意**的 —— 不冻住昼夜和天气就没法比帧数。要恢复正常游玩请在世界里敲
> `/gamerule doDaylightCycle true`。**玩家的其它存档没有被碰过。**


### 8.5 E9：ModernFix `dynamic_resources` —— **实测负优化，已回滚**

`config/modernfix-mixins.properties` 里的 `mixin.perf.dynamic_resources` **默认就是 false**
（"用到才烘焙模型"，社区常推荐打开）。本机实测：

| 跑次 | 配置 | → 资源重载 | → 集成服务端起 | **→ 世界就绪** |
|---|---|---|---|---|
| r5 / r6 / r7（开它之前、同配置三次） | Fabulous + 三项便宜设置 | +54~56 s | +130~139 s | **+137.0 / +145.3 / +136.2 s** |
| **r8** | 同上 **+ `dynamic_resources=true`** | +52 s | **+147.7 s** | **+155.1 s** |

- **生效性先验证过**：日志从 `0 override(s) found` 变成 **`1 override(s) found`**，
  且 `debug.log` 里不再出现 `Skipping mixin perf.dynamic_resources.… disabled by configuration`。
  > 第一次跑（r7）我**改了配置但没生效**，差点把它当成"无效果"记下来：
  > 我的检查脚本用 `in` 匹配，命中了文件头里**被注释掉的示例行**
  > `#   mixin.perf.dynamic_resources=true`，于是打印"已经开过了"就跳过了写入。
  > **那次结果作废**（教训与硬规则 22 同族：判据写错，绿灯比红灯贵）。
- 变慢的位置在**资源重载 → 集成服务端**这一段：+78~83 s → **+95 s**。
- 结论：**没有收益，而且落在噪声带最慢端**（1080p 基线带 137~148.7 s）。
  按硬规则 24 **已回滚**：配置已从 `data/perf/modernfix-mixins.properties.original-2026-09-23`
  整份还原，非注释行重新为空（已复验）。

> **为什么"延迟烘焙"没省下时间**：这个存档周围方块种类极多，该烘焙的模型在世界加载阶段
> 基本都会被走到；延迟只是把成本挪了个位置，还多出查表开销。**上游默认值是有理由的。**

---



---

## 九、进世界后的"巨停"：已抓到两个**具名**来源（2026-09-23）

玩家说的"卡住"= 进世界后 1~2 分钟内的多次 `Can't keep up!`。
**挂机 idel 测不出来**，必须在"刚进世界"那段窗口看。受控跑抓到的：

| 会话 | 卡顿次数 | 累计 | 最严重 |
|---|---|---|---|
| r2（1080p、正午、240 s 驻留） | 3 | 13.2 s | **8239 ms**（10:01:02）|
| 09:39 手工会话（300 s 驻留） | 4 | 18.0 s | **10103 ms** |
| r1（1080p、夜晚、180 s 驻留） | 0 | — | — |

### 9.1 具名来源一：WorldEdit 的方块状态表 —— **5787 ms，每次进世界都发生**

```
[10:07:19.326] [Server thread/WARN] [com.sk89q.worldedit.WorldEdit/]:
Took more than 5 seconds to generate complete state map for farm_and_charm:mincer.
This block is likely improperly using properties. State count: 13332. 5787ms elapsed.
```

- **证据**：r2（09:59:58.900）与 r3（10:07:19.326）**两次都出现**、措辞一致；
  即 `farm_and_charm:mincer` 一个方块有 **13332 个方块状态**，WorldEdit 给它建表花 **5787 ms**。
- 它发生在**世界就绪之后**（r3：世界就绪 10:06:57.750，这行 10:07:19.326）
  ⇒ 玩家"已经进世界了"却还要接着卡：正是这一类。
- r2 的 Server thread 在那段有 **20.5 秒空档**（09:59:38 → 09:59:58）；
  WorldEdit 这 5.8 s 是其中**已具名**的一部分，其余仍未具名。
- WorldEdit 自己的判词是"这个方块的属性用法有问题"
  （`likely improperly using properties`）⇒ **责任在 `farm_and_charm` 的方块定义，不在 WorldEdit**。
- 但 `config/worldedit/worldedit.properties` 里**没有**关闭状态表生成的开关
  （只有命令权限、半径、历史条数等），所以只能：报给 mod 作者 / 换版本 / 接受这 5.8 秒。

### 9.2 具名来源二：RoadWeaver 结构发现

`发现 673 个结构和 146 个标签` —— r1/r2/r3 **每次**都有，落在世界就绪后 30~35 秒。
它同时解释了 HUD 上那个"100%"进度框：RoadWeaver 在扫全世界的结构。

### 9.3 仍未具名：进世界后 40~120 秒的 2~8 秒卡顿

同一时段 **GC 最大只有 341 ms** ⇒ **GC 解释不了 8 秒的停**。
剩余候选（按可能性）：区块流式加载/生成、自动保存、SimpleBackups 同步备份（玩家那次实测 7.389 s）。

**两次尝试与结论（2026-09-23，别重复踩）**

| 手段 | 结果 |
|---|---|
| MC 自带 `/debug start`（r11/r12） | 聊天栏**确认**了「已开始刻分析」，但 `/debug stop` 在实例里**没留下任何文件**（`debug/` 里只有联网协议错误的 `disconnect-*.txt`）。客户端在 dwell 到点时退出，`stop` 的聊天回复没截到 ⇒ **这条路的产物位置没验证，先别再用** |
| `spark`（r13） | jar 里确认支持 `--save-to-file`（`.sparkprofile`），命令形态 `/spark profiler start --timeout 120 --save-to-file`；**这是下一次该用的** |

**卡顿计数不能跨跑次直接比**（见 §十 的警告）：今天 12 次跑里卡顿从 3~4 次降到 1~2 次，
但世界区块越跑越熟（热身效应），所以这个下降**不能记账给任何配置改动**。
要证明"预生成能消掉进世界卡顿"，得在一个**新存档**上做前后对照（`E4`）。

> **教训**：`/debug` 那次我**先发了命令、60 秒后才截图**，聊天栏的确认早就滚掉了，
> 于是"命令到底有没有生效"变成猜。**凡是靠聊天栏确认的操作，必须在 5 秒内截图验证**
> （r12 就是这么做的，立刻看到「已开始刻分析」）。

---

## 十、启动耗时总表（2026-09-23 全部受控跑，口径 = 日志时间戳）

| 跑次 | 分辨率 | 配置 | → 资源重载 | → 集成服务端起 | **→ 世界就绪** | 卡顿 |
|---|---|---|---|---|---|---|
| base-8G | 854×480 | 默认 G1 | +49.6 | +124.3 | **+130.5** | 0 |
| aikar-8G | 854×480 | Aikar's flags | +48.2 | +120.1 | **+125.3** | 0 |
| novc-8G | 854×480 | 版本检查关 | +49.9 | +125.9 | **+131.9** | 0 |
| r1 | 1920×1080 | 玩家原画质 | +59.0 | +138.3 | **+148.7** | 0 |
| r2 | 1920×1080 | 玩家原画质（正午） | +56.8 | +135.3 | **+142.1** | 3 / 13.2 s |
| r3 | 1920×1080 | 玩家原画质 | +54.8 | +134.0 | **+140.5** | 3 / 12.7 s |
| r4 | 1920×1080 | 玩家原画质 + vsync 关 | +57.4 | +139.6 | **+148.5** | 3 / 7.4 s |
| r5 | 1920×1080 | 四项画质包 | +54.1 | +131.0 | **+137.0** | 2 / 6.9 s |
| r6 | 1920×1080 | Fabulous + 三项 | +55.9 | +138.3 | **+145.3** | 1 / 2.0 s |
| r7 | 1920×1080 | Fabulous + 三项 | +54.1 | +130.0 | **+136.2** | — |
| r8 | 1920×1080 | 同上 + `dynamic_resources` | +52.0 | +147.7 | **+155.1** | 3 / 13.2 s |
| r9 | 1920×1080 | Every Compat `CACHED_ZIPPED`（**冷缓存**） | +65.0 | +167.0 | **+176.0** | 3 / 19.7 s |
| r10 | 1920×1080 | Every Compat `CACHED_ZIPPED`（**读缓存**） | +53.2 | +128.6 | **+136.0** | 3 / 11.2 s |
| r11 | 1920×1080 | **最终交付配置**（Fancy + 三项 + vsync 开） | +55.0 | +133.7 | **+142.0** | 2 / 5.3 s |
| r12 | 1920×1080 | 同上（跑 `/debug` 采样） | +53.0 | +131.0 | **+138.0** | 1 / 2.4 s |
| r13 | 1920×1080 | 同上（跑 spark 采样） | +54.0 | +133.0 | **+140.0** | 3 / 6.3 s |
| r14 | 1920×1080 | 同上 + JFR 录制 | +63.0 | +153.0 | **+164.0** | 2 / 9.3 s |
| r15 | 1920×1080 | 探索新地形（tp 进未生成区）+ 预生成 B 区 | +55.0 | +133.0 | **+139.0** | 9 / **38.8 s**（最坏 14.8 s）|
| r16 | 1920×1080 | tp 进**已预生成**区 | — | — | — | 5 / 10.7 s（最坏 3.4 s）|

> **上表是"服务端就绪"，不是"玩家能玩"**：小口径见 §七之二 ——
> r10/r11/r12/r13 的**客户端可玩**分别是 **+195 / +196 / +200 / +198 秒**
> （尾段稳定 +58~59 s）。谈"加载速度"请用后者。

**读数规矩**：同一分辨率的重复跑极差约 **5~13%**
（854×480：125.3~131.9；1080p：136.0~176.0，去掉冷缓存那次是 136.0~155.1）
⇒ **小于 ~10% 的差异不算有效**。1080p 比 854×480 慢 **10~15 秒（+8~11%）**。

⚠️ **"卡顿"这一列不能跨跑次直接比**：今天的跑次是按时间顺序跑的，世界区块**越跑越熟**
（第一次要生成/载入的区块，后面都已在存档和系统缓存里），
所以"卡顿 3 次 → 1 次"里混着热身效应，不能记在画质设置头上。
要比卡顿，必须**控制冷/热状态**各跑两次。

⚠️ **这一版的总表也提醒了一件事**：1080p 这 8 次的极差（136.2~155.1 = **19 秒**）
比我一开始估计的 5% 更大 —— 所以**单次跑不足以判定 10~20 秒级的启动改动**，
至少得两边各两次。E7/E8 的测法必须照这个来。

---

## 十一、用 JFR 把"尾段 58 秒"拆开（2026-09-23 新增）

**手段**：JVM 级 Flight Recorder，从**进程启动那一刻**就在录，所以连"启动阶段 + 进世界 + 尾段"
全都能采到 —— 这是**唯一**能覆盖尾段的采样方式（尾段里客户端还卡在"加载地形中…"屏幕，
**聊天命令根本发不进去**，spark / `/debug` 都够不着）。

```bash
# 录制（r14）：加到 perf_baseline 的额外 JVM 参数里
python tools/perf_baseline.py --label r14-jfr-8G --xmx 8G --seconds 200 \
  --width 1920 --height 1080 \
  --extra="-XX:StartFlightRecording=filename=logs/r14.jfr,settings=default,dumponexit=true"
# 分析（JDK 21 没有 `jfr view hot-methods`，用自带的聚合器）
python tools/jfr_hotspots.py logs/r14.jfr --window 164 300        # 尾段窗口
python tools/jfr_hotspots.py logs/r14.jfr --window 164 300 --thread "Server thread"
```

> ⚠️ **JFR 会拖慢启动**：r14 是 +164 s 世界就绪 / **+237 s 客户端可玩**，
> 比同期基线（+140 / +198）慢约 18%。⇒ **只拿它做归因，不要拿它当性能基线。**
> （r14 仍抓到 2 次卡顿、累计 9.3 s、最坏 7215 ms，说明窗口覆盖到了。）

### 11.1 尾段（世界就绪 → 之后约 2 分钟）到底在算什么

`Server thread` 的 **self** 热点（2107 个采样，占全场采样 21%）：

| 排名 | 方法 | 占比 |
|---|---|---:|
| 1 | `Maps.equalsImpl` | 7.5% |
| 2 | `RegularImmutableMap.get` | 5.8% |
| 3 | `HashMap$TreeNode.find` | 4.5% |
| 4 | `ImmutableMapEntrySet.isHashCodeFast` | 4.4% |
| 5 | `RegularImmutableMap.size` | 3.4% |
| 6 | `RegularImmutableMap.fromEntryArrayCheckingBucketOverflow` | 2.3% |
| 7 | `HashMap.putVal` | 2.3% |
| 8 | **`AbstractProperty.equals`** | **2.3%** |
| 9 | *（其后）* `BlockState.withValue` | 1.0% |

**读法**：`AbstractProperty.equals` + `BlockState.withValue` + 一堆 `ImmutableMap`
（`fromEntryArrayCheckingBucketOverflow` 就是 Guava **在建 ImmutableMap**）
⇒ 服务端在**按属性值查方块状态**上烧时间。**这一项的成本随"方块状态空间"增长** ——
而这个包的注册项是 26,024（`chipped` 6,993 + `rechiseled` 3,628 = 41%），
再加上 `farm_and_charm:mincer` 一个方块就 **13,332 个状态**（§9.1）。

⇒ **这是把 §9 的"具名元凶"和 E8 连起来的机制证据**：
E8（砍装饰变体）不只是"少几个方块"，它会直接削掉这条热点。

### 11.2 同一窗口里其它线程在干什么

| 线程 | self 热点 | 含义 |
|---|---|---|
| `Worker-ResourceReload-1` | `JavaUtilJarAccessImpl.entryFor` **36%**、`String.newStringUTF8NoRepl` **24%**、`ZipFile.getZipEntry` 14% | **从 jar 里读条目 + UTF-8 解码字符串**（231 个 mod + 那个 5.2 MB 中文语言包）—— 这是"资源重载"那 79 秒的实质 |
| `Worker-ResourceReload-4` | `Objects.equals` **14%**、`HashMap.putVal` 6% | 解析/去重 |
| `CullThread` | `Arrays.fill` **55%**、`Provider.isOpaqueFullCube` 17% | Embeddium 的遮挡剔除在**清数组**（这是剔除本身的开销，不是配置问题）|
| `Chunk Render Task Executor #0/#1` | `copycats$customOcclusion` **8%**、`SimpleBakedModel.getQuads` 8%、`ChunkBuilderMeshingTask.execute` 7% | 客户端区块重建；其中 8% 花在 **Copycats 的方块状态遮挡钩子**上（又一个"状态空间"税）|
| `main` | `HashMap.getNode` 4%、`Holder$Reference.value` 3% | 注册表查表 |

### 11.3 结论（**这一轮的答案**）

尾段的 58 秒**不是某个 mod 的 bug，也不是配置错**，而是：

1. **方块状态空间的查表/哈希**（服务端），以及同源的 Copycats 遮挡钩子（客户端）；
2. **从 231 个 jar 里读条目 + 解码 UTF-8**（重载 worker）；
3. Embeddium 剔除的固定开销。

⇒ 在"config / JVM 参数 / 预生成 / 脚本"这四层里**已经没有能显著压缩它的开关了**；
能真正压缩它的只有**内容规模**（E8 砍装饰变体、或整体减 mod 数），
那是**要玩家拍板的内容决定**。这是本轮最重要的结论：
**继续在配置层找"省几十秒"的开关是徒劳的 —— 已经用采样证据证明了。**

### 11.4 顺带否掉"预生成能治进世界卡顿"（E4 的范围被收窄）

尾段窗口 top 60 里**没有任何区块生成帧**（没有 `ChunkGenerator` / `NoiseBasedChunkGenerator` /
`Structure` / `Feature`）。出现的是：

| 类别 | 代表帧 |
|---|---|
| 取区块 | `ServerChunkCache.getChunk` 1.0% |
| 光照 | `IBlockStateExtension.getLightEmission` 0.7%、`DynamicLightingEngine.getDynamicLightLevel` 0.5%、`ArrayLightDataCache.get` 0.5% |
| 区块重建 | `ChunkBuilderMeshingTask.execute` 0.6%、`ChunkMeshBufferBuilder.push` 0.4% |
| 碰撞 | `ChunkAwareBlockCollisionSweeper.computeNext` 0.4% |

⇒ **进世界那一分钟的卡顿是"载入 + 光照 + 重建"，不是"生成"**（`scdev` 出生区早就生成过了）。
所以 **E4 预生成治不了"进世界卡顿"**，它治的是**探索新地形时**的生成卡顿
（玩家那次 17.9 秒的巨停可能属于后者 —— 但需要另测）。
**别为了"进世界卡顿"去跑几小时预生成。**

---

## 十二、预生成实测：探索新地形的巨停从 **14.8 秒 → 3.4 秒**（2026-09-23）

**这一节回答**：预生成（`E4`）到底值不值 —— 它是"预生成"这条唯一还没实测的杠杆。

### 12.1 先复现症状

把玩家瞬移进 `scdev` **从未生成过**的区域（x≈3000 / z≈3000，此前连 region 文件都不存在）：

| 时刻 | 事件 |
|---|---|
| 11:53:00 | `/tp @s 3000 100 3000`（从出生点跳 4,200 方块）|
| **11:53:21** | **`Can't keep up! Running 14849ms or 296 ticks behind`** |
| 之后 | 该区域才出现 region 文件（`r.5.5.mca` 等）|

**14.8 秒的巨停**，与玩家自己那次 **17.9 秒**同级 ⇒ **探索新地形的巨停 = 边走边生成的代价**。
（它只解释"探索"类巨停；"进世界头一分钟"那类不是生成，见 §11.4。）

### 12.2 预生成后再跳同一类位置

在**另一片**同样未生成过的区域（中心 -3100/3000）用 Chunky 预生成：

```
/chunky center -3100 3000
/chunky radius 200      ← 单位是**方块**：日志报 729 区块（27×27 = 432 方块见方）
/chunky start           ← 1 分 34 秒跑完（均值 ≈8 区块/秒，日志瞬时 Rate 12.1 cps）
```
然后**退出世界让它落盘**（4 个 region 文件共 ~18 MB，已确认）。

新会话里再跳进这片区域（`/tp @s -3100 100 3000`，同样从远处跳进来）：

| 跑次 | 目标区域 | tp 之后的卡顿 | **最严重** |
|---|---|---|---|
| r15 | **未预生成**（新地形）| 2789 / … / 3077 ms | **14,849 ms** |
| r16 | **已预生成**（432 方块见方）| 2670 / 3443 / 2370 ms | **3,443 ms** |

⇒ **最严重那次 14.8 秒 → 3.4 秒（−77%）**；两次的"进世界尾段"卡顿（2.0 / 2.2 秒）几乎没变
⇒ 改掉的确实是"探索生成"，不是别的东西。

### 12.3 为什么还剩 3.4 秒 / 怎么更干净

预生成只覆盖 **432 方块见方**，而渲染 12 区块（192 方块）+ 模拟 8 区块让视野**超出**这片范围
⇒ 边缘仍在生成（r16 里确实看到一批新 region 文件被创建）。
按实测速率换算：**半径 1024 方块（16,641 区块 ≈ 2,048 方块见方）约 23 分钟**，
那才是"跳进去完全不生成"的量级 —— 见 `DESIGN-PREGEN.md` §5.1。

> ⚠️ 顺带更正了那份文档的一个**单位错误**：`/chunky radius` 的数是**方块**、不是区块。
> 旧版写 `/chunky radius 128` 想做"2048 方块"的覆盖，实际只会生成 289 区块（**差 99.5%**）。

### 12.4 结论（可直接给玩家的建议）

* **预生成确实能治"探索新地形"的巨停**：实测 −77%，代价 ≈ **1.6 分钟 / 729 区块**（8~12 区块/秒）。
* **它治不了"进世界后头一分钟"的卡顿**（那段时间没有生成帧，§11.4）。
* 所以建议很具体：想消探索卡顿 → `/chunky center <基地x> <基地z>` +
  `/chunky radius 1024`（≈23 分钟）把常用区域预生成；
  **不要指望它让"进世界"变快。**





