# 需要你做的事（其余我自己搞定）

> 这份清单**只放"只有你能做/必须你拍板"的事**。能我自己跑、自己验的都在
> `DEFERRED.md`（押后验收）与 `NEXT.md`（进度与下一步）里，不用你操心。
> 最后更新：2026-09-20。

---

## 一、现在挡着我的（**最优先**，都是"操作"不是"决定"）

### 1. 跑客户端批次时，腾一点内存出来

**事实**：这台机器总内存 **15.8G**。我实测过一次：

| 进程 | 占用 |
|---|---|
| 开发服务端（`-Xmx3G`）| ~3.4G |
| `node`（DSH Web 那个进程）| ~3.0G |
| DSH Desktop | ~0.6G |
| **League 客户端**（`LeagueClientUxRender`）| ~0.13G（还有其它 LoL 相关进程）|
| **Oopz**（语音）| ~0.17G |
| 剩余可用 | 4~7G |

而这个整合包**客户端要 5~8G**（232 个 mod）。客户端我起过两次：
第一次 420 秒没进世界、第二次 900 秒也没进（都在 `Minecraft: NeoForge Loading...` 那一屏）。
**这大概率就是内存不够 + 加载慢**，不是包坏了。

**要你做的**（任选，按省事排序）：

- [ ] **关掉 League 客户端和 Oopz**（如果你现在不用）—— 立刻多出 1~2G 且少了后台 IO；
- [ ] 或者**告诉我可以关**（我不想擅自关你的东西）；
- [ ] 或者**让我把客户端堆降到 5G 再试**（我下次会这么做，但如果还是进不去，就需要上面两条）。

### 2.（可选，但很省我的事）重启一次 DSH，让电脑控制插件上线

`dsh-computer-use-win` v0.1.2 已经装好、我也验证过它能用（22 个工具、截图直接返回图片），
但**它要 DSH 重启后才会出现**。有它之后"看画面"这类验收（村民外观、任务书界面、
石碑排版）我能自己截图判断，不用你描述给我听。

- [ ] 重启 DSH（会中断当前会话；重启后我从 `NEXT.md` 第三节接着干）

---

## 二、需要你拍板的设计（`OPEN-DESIGN.md` 里那 5 条，**不挡当前开发**）

这 5 条我在代码里都留了接缝，你什么时候定都能接上：

- [ ] **一国一城 vs 多城**（含"B-lite"：数据上多城、只实体化都城）
- [ ] **国力怎么合成**（城内兵力 + 国土面积 + 城内资源/设施的权重）
- [ ] **城内资源分类与上限公式**（食物 vs 生产资源，你之前定过"必须分类"）
- [ ] **边镇要不要有实体**（还是只在地图上与周报里存在）
- [ ] **终局**（毕业工程之后进"无尽纪元"的横向目标清单）

---

## 三、文字与语气（我铺了结构，腔调是你的事）

- [ ] **`TASKBOOK-300H.md`**：我按 `DESIGN.md` §十五 的密度口径（1 任务 ≈ 1.5 小时）
      把 27 个任务扩到 **199 个**（六个时代 16/27/33/33/40/50）。现在是
      `任务名（达成判据）` 的结构稿，**成书时每个任务还要一句叙述**（碑文腔/方志腔）——
      那是你的文字，我不替你定。
- [ ] 要不要把 `DESIGN.md` §十五 那张 27 行的表改成一行引用（指到 `TASKBOOK-300H.md`）。
- [ ] `NATIONS.md` / `DESIGN.md` 里我新加的实现备注（✅/⚠️ 那些块）措辞是否合适。

---

## 四、只有真人/联机能验的（`DEFERRED.md` 各组，我都写好了跑法）

这些**脚本都写好了、我也尽量自动化了**，但物理上需要两个客户端或人眼：

- [ ] **A 组（联机）**：跨领地保护、领地里的机械、队伍共有领地、
      **A4 图纸 × 领地（项目关键路径）**、领地界面、OPAC 服务端配置落盘
- [ ] **B 组（人眼）**：Create 的 Ponder 中文排版、OPAC 中文界面观感、图纸放出来的观感、中文字体
- [ ] **C 组（不用游戏，但只有你能决定风格）**：给 OPAC 补 613 条 `zh_cn`
- [ ] **D 组（性能）**：并发压测、`save-all` 时那个 90 ticks behind、袭击场景 ≥15 TPS
- [ ] **F 组（军事 8 条）**：三套袭击会不会打架、指挥手感、队伍概念三套、性能红线、
      工人系统与 Create 重叠、供养机制、攻打据点循环、攻城性能
- [ ] **M0（HYW 会不会主动打玩家）**：**至少要一个真人客户端在线当靶子** ——
      它是**夺城（计划 5）的唯一前置**。脚本在 `tools/tests/m0_hyw.txt`。

---

## 五、我已经替你做完的（列出来免得你重复操心）

- ✅ v1→v2 存档迁移：拿 `saves/b5v3` 里**真实的 v1 档**在真服务端上跑过
  （`已迁移 schemaVersion 1 → 2` / seed 一致 / 新字段补空 / `roundtrip=OK` / 迁移后能正常结算）
- ✅ 结算调度（§七）：空转 65 秒证明"不在线不推进"
- ✅ §10.1 门禁、§11.1 情报册三档、§9.4 国书全生命周期、§11.2 六个外交动作、
  §十二 玩家行为→态度 —— 都在真服务端上端到端跑过
- ✅ 世界会自己清空的平衡问题：量了 12 seed × 6 时代，按实测表调了参数
  （末代平均存活 1.0 → 6.2 国）
- ✅ 300h 任务书骨架（199 条，结构稿）
