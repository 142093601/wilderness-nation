# PLAN-questbook：任务书设计（FTB Quests · 六时代 + 邦交 + 技艺 + 类目）

> **这份文档是什么**：`TASKBOOK-300H.md` 的 199 条是按"1 任务 ≈ 1.5 小时"这个**内容量公式**
> 倒推出来的草稿。用户否掉了那个思路：**任务书首先是玩家的引导，不是内容量的填充物。**
> 这里把任务书**重新设计**一遍，定下架构、判据、文字、奖励、验收，然后才动手写数据。
>
> 设计日期：2026-09-20 · 状态：**已与用户逐段确认**（七段全部点头后才落笔成文）

---

## 〇、根本约束（用户原话，一切设计服从它）

> **"一定不能让任务悬于游戏之上，任务是游戏的导向这个根本目标不能变。"**

翻译成四条可执行的硬规则：

1. **判据必须落在游戏事实上** —— 做出某样东西 / 放下某个方块 / 到达某维度 / 达成某进度。
   **不用"自觉打勾"**；只在"游戏里确实没有可检测信号"时才允许 `checkmark`，且逐条写明理由（见 §四）。
2. **不做任务专属内容** —— 没有"只为任务存在"的去处、物品或步骤。任务指向的都是玩家本来就会做的事。
3. **任务不当地图外的门闸** —— 除"提前推进时代"这一把钥匙外，不做任务也能把整个包玩下去。
4. **不重复 mod 自己的教学** —— Create 有 Ponder（本包 10 个 mod 自带）、物品有 JEI/EMI。
   任务书只负责"指路 + 给节奏"，不负责把 mod 说明书抄一遍。

**由此产生的一条结论**：任务总数**不设配额**，由引导需要决定 —— 不再有"199 条"这个指标。

---

## 一、现状与参照（都是实测，不是印象）

### 1.1 现状

- FTB Quests **2101.1.36**（NeoForge 1.21.1）已装；`config/ftbquests/quests/` **一个文件都没有** —— 任务书在游戏里是空的。
- 配套模组：FTB Quests Lang Splitter（**正文可与结构分离**）、FTB XMod Compat、Quest Enhance、Extra Quests、FTB Quests Optimizer。
- `TASKBOOK-300H.md`：199 条任务（16/27/33/33/40/50）只有 `任务名（判据）` 的结构稿，**未进游戏**。
- `DESIGN.md` §十五 的定位语：任务链"只提供**方向与节奏**，不是内容主体"；内容主体是建造与自动化。

### 1.2 参照物 ATM-10 的实测（只作结构参照）

拉取 `AllTheMods/ATM-10` 的 `config/ftbquests/quests/` 做量化：

| 项 | 数值 |
|---|---|
| 章节数 / 总体积 | **66 章 / 2.6 MB**（单章 2.6 KB ~ 142 KB） |
| 抽样 12 个最大章 | 1,935 个任务 · 4,006 条判据 · 2,956 条依赖 · 2,510 条奖励 · **0 条 description** · **472 张图片** |
| 平均 | 每任务 2.1 条判据 · 1.3 条奖励 |

三条结论：

1. **ATM 的引导几乎不靠文字**：靠物品/机器图标 + 依赖箭头排路线 + 章节内示意图（0 条 description、472 张图）。
2. **它不给每个 mod 开章**：66 章 = mod 章（大件，单章 90~260 任务，大 mod 按档位拆章）+ **系统章**（`basic_tools`/`storage`/`food_and_farming`…，同类边缘 mod 被吸收）+ 主线章。
3. **正文与结构分离**：文字在 `quests/lang/<语言>/`，键形如 `chapter.<id>.title`、`chapter.<id>.chapter_subtitle`。

⚠️ **法律边界**：ATM-10 的 quests 目录是 **All Rights Reserved**（其 README 明确禁止在任何公开发布的整合包中再分发）。
我们的仓库是**公开**的 —— 因此**只作结构参照，一个字都不抄**，全部自写。

### 1.3 我们自己版本的能力清单（从 jar 实测，不照抄 ATM）

- **判据类型**：`item` · `dimension` · `biome` · `structure` · `advancement` · `observation` · `stat` · `kill` · `location` · `fluid` · `energy` · `xp` · `stage` · `custom` · `checkmark`
- **奖励类型**：`xp` · `xplevels` · `item` · `loot` · `random` · `choice` · `alltable` · **`command`** · `toast` · `stage` · `advancement` · `currency` · `custom`

（`command` 奖励存在，是"时代钥匙"能落地的技术前提。）

---

## 二、章节架构（**40 章** · 2026-09-22 修订）

> **这次修订的起因**：初版定 22 章，依据是"40~60 行是左侧一堵墙"这个**没有数据的判断**。
> 用户提出质疑后，我实测了本机三个成熟包（`tools/reference_questbook_style.py`，只统计结构、不抄文字；
> ATM10 的任务书是 ARR，我们仓库公开）：
>
> | 包 | 章 | 任务 | 每章中位 | 最大章 | 带依赖比例 | 判据类型数 |
> |---|---|---|---|---|---|---|
> | All the Mods 10 | 63 | 4528 | **56** | 253 | 88% | 17 |
> | 天空蜂巢 | 35 | 2901 | **66** | 336 | 54% | 13 |
> | 龙之冒险 | 14 | 1566 | **58** | 492 | 38% | 12 |
> | **本包（修订前）** | 23 | 437 | **14** | 68 | 93% | 7 |
>
> 结论：**"章多了是一堵墙"这个顾虑不成立**（最流行的 ATM10 就是 63 章）。
> 真正的问题是**每章中位 14 条 vs 别人 56 条** —— 我们的章是空壳，不是墙。
> 新基线：**章 40 上下 · 每章中位 40~56 · 总数 1600 上下**。
>
> 另一条要学的结构事实：ATM10 只有 **20%** 的任务显式写 `icon`（其余靠"第一条判据的物品"自动当图标），
> 形状用了 10 种（`diamond` 535 / `rsquare` 466 / `hexagon` 299 / `square` 298 / `circle` 213 /
> `octagon` 96 / `gear` 90 / `pentagon` 79 / `none` 62）；我们此前只有 4 种形状。
> **形状是免费的语义**（层级、里程碑、可选），要用起来。
>
> 章名与 mod 家族的对应、每章内容清单、目标条数 → **`QUESTBOOK-ROSTER.md`（单一真源）**。

### 2.1 四栏分组（40 章）

| 分组 | 章数 | 装什么 |
|---|---|---|
| **编年** | 12 | 序 · 生存第一课 · 落地 · 立国 · 立约 · 基建 · 出关 · 御敌 · 公共工程 · 文明 · 无尽纪元 · 碑林与史册 |
| **邦交** | 3 | 接触与情报 · 外交与条约 · 图纸与夺城 |
| **技艺** | 8 | 材料与工业 · 机械动力 · 机械动力·物流 · 机械动力·建材 · 蒸汽与铁路 · 图纸与工程 · 领地与队伍 · 防御与尸潮 |
| **类目** | 17 | 每个 mod 家族一章（构件 / 装饰 / 家具 / 生活 / 食物 / 农牧 / 仓储 / 贸易 / 装备 / 实用 / 探索 / 结构 / 战斗 / 尸潮 / 性能 / 界面 / 音画） |

**编年 vs 技艺 vs 类目的分界**（防止同一主题被写两遍）：
- **编年** = 时代主线，回答"这个时代要达成什么"（国策判据落在游戏事实上）
- **技艺** = 有**生产链**的系统，回答"这条产线怎么搭起来"（依赖箭头 = 工序图）
- **类目** = mod 家族**覆盖面**，回答"这个 mod 有什么、什么时候用它"（消费视角，不铺产线）

### 2.2 时代章内部布局

```
        [国策主线 4~6 条]  ← 横向主轴，左→右，rsquare / pentagon，尺寸区分主次
              │
   ┌──────────┼──────────┬──────────┬──────────┐
 [建造簇]  [自动化簇]  [探索簇]  [防御簇]  [国家线簇]   ← 支线挂在对应主轴节点上
              │
        [终局大任务]  ← hexagon，最大尺寸；下方 link 指向下一时代
```

- **支线类别占比**（沿用 `TASKBOOK-300H.md` 的口径）：建造 30% · 自动化 30% · 探索 15% · 防御 15% · 国家线 10%。
- **终局大任务** = 该时代的"彩排"（`DESIGN.md` §十五 的定义），同时是拿到**时代钥匙**的判定点。

### 2.3 技艺章（mod 章）布局

章副标题写一句"这章怎么走"；章内第一条是"从哪开始"的锚点任务；之后按**真实的机器/物品先后关系**铺树
（依赖箭头就是路线图）。图标注：`Framed X` 之类的命名一律跟社区中文（有 `data/i18n-style` 基准就用它）。

**密度基线（实测别人）**：ATM10 的 `productive_bees` 一章 **253 条 / 251 条带依赖** —— 一个 mod 的
完整产线摊开就是一两百条。我们定 **每章 30~56 条**，其中：
- **主轴**（`rsquare`/`pentagon`，尺寸 1.75~2.0）= 不可跳过的工序节点，8~14 条
- **支线**（`circle`/`diamond`，1.2~1.5）= 同一工序的变体、并行产物、可选升级，其余条数
- **里程碑**（`hexagon`/`octagon`，2.5~3.0）= 每 10 条左右设一个，给"我走到哪了"的路标

### 2.4 类目章（系统章）布局

同类 mod 各占一个**簇**，**簇内 8~20 条**（初版写 3~8 条，那是"敷衍量"的同义词）；
簇之间用留白与形状区分，簇头用 `pentagon` + 2.0，簇内 `circle` 1.2。
一个簇 = 一个 mod 家族；**每个有玩法的 mod 都必须在某个簇里出现至少一条**。

### 2.5 形状与尺寸的语义（**全包统一，不要随意用**）

| 形状 | 尺寸 | 语义 |
|---|---|---|
| `pentagon` | 2.0 | **簇头 / 主轴起点**："这一类从这里开始" |
| `rsquare` | 1.75 | **主轴工序节点**：链上的必经步骤 |
| `square` | 1.5 | 主轴上的**批量节点**（同一件事做够数量） |
| `diamond` | 1.5 | **分叉点**：从这里起有两条以上并行路线 |
| `circle` | 1.2 | **支线 / 变体**（默认值，最常见） |
| `octagon` | 1.75 | **章内里程碑**："这一阶段完了" |
| `hexagon` | 2.5 | **章终局**（一章最多一个） |
| `gear` | 1.75 | **自动化 / 产线**专用标记 |
| `none` | — | 纯结构节点（只用来接线，玩家看不见） |

### 2.6 排版（**位置就是引导** · 2026-09-22 重做）

FTB Quests 的画布是二维的，**位置本身就是一种说明**：左右=先后，上下=分支，
留白=分组，形状+尺寸=层级。所以排版的问题不是"排得下"，而是"**别让排版撒谎**"——
把 25 条互不相干的同深度任务摊成一行，等于告诉玩家"这 25 件是并列的"。

**算法：整齐树（tidy tree）** —— 零交叉是**结构性保证**，不是调出来的：

1. **深度**：迭代式不动点算层号（不用递归，有环也不会把生成器打挂）
2. **主父节点**：多前置的节点挂在**最深的前置**下（语义 = 最后一个前提做完就能做它）
3. **叶子按 DFS 序排槽** ← **最关键的一步**。按 TOML 顺序排槽会让两个兄弟子树的
   叶子交错（A1 B1 A2 B2），A 的连线必然穿过 B —— 几何上就注定了。
   DFS 序（A1 A2 B1 B2）让每个子树占**一条连续的横向带**，两条边相交不再可能。
4. **父节点 x = 子节点中点**
5. **同层消重叠**：带动整棵子树平移，保证带与带不交错
6. **间距按尺寸算**：中心距 = `(s₁+s₂)/2 × grid_scale + gap`
   （`gap=0.18` · `unit=0.5` 取自 `data.snbt` 的 `grid_scale`）

**这些数字是从参照包量出来的，不是拍的**（`tools/reference_layout_calibration.py`）：

| 包 | 章数 | 中位 宽×高 | 中位面积 | 依赖线交叉 |
|---|---|---|---|---|
| ATM10 | 61 | 18.5 × 15.0 | 240 | **0** |
| 天空蜂巢 | 31 | 19.5 × 13.4 | 255 | **0** |
| 本包（改前：全局分层 + 固定步长 1.5） | 40 | 26 × 24（最大 **65 × 7**） | 408 | **31 ~ 342** |
| 本包（改后：整齐树） | 40 | 26 × 10（最大 42 × 15） | — | **5 ~ 136** |

- ATM10 的 `productive_bees`：**253 条只占 21 × 29.5，零交叉**
- 间距公式的来处：ATM10 邻接中心距 0.5，而它 `size=1.0`、`grid_scale=0.5`
  → 正好 `(1+1)/2 × 0.5`，也就是"两个图标刚好不重叠"。
  **我们改前比这松 3~5 倍** —— 这才是画布铺得开的真正原因（不是内容多）。

**还没做到的**（诚实记录，不许当成已完成）：本包最大的两章仍有 **136 / 100** 条交叉，
ATM10 是 0。两个原因都不是算法还能救的：
① 我们的节点比它大 ~1.4 倍（我们用 `size` 表达层级，它几乎只用 `shape`）；
② 我们的章是 **DAG**（跨簇依赖实测 495 条），ATM10 的章基本是纯树。
要继续压只有两个方向：**缩小节点尺寸**，或**把最大的几章拆开**。

**复盘**：这一轮我在交叉线上迭代了三版、还一度把它从 238 弄到 375，
根因是**从没量过参照物**。量完之后方向立刻清楚了（ATM10 零交叉 ⇒ 必然是整齐树）。
**"优化一个没校准过的指标"比不优化更浪费时间** —— 这条已记进 `INCIDENTS.md`。


---

## 三、主线的"钥匙"机制

- 每个时代的主线 = 一个**国策任务组**（4~6 条）。整组做完 → 拿到该时代的**时代钥匙**。
- 拿到钥匙后，任务书里点亮一条隐藏任务「**提前推进时代**」；它的奖励是一条 **`command` 奖励**。
- **不做也不会被卡**：时间到了照样推进（现有 `SettlementClock` 的累计在线小时机制**不动**）。
- **低耦合**：判定"主线做完"完全交给 FTB Quests 自己；mod 侧只需要一个"接受钥匙"的命令入口 ——
  **不需要 mod 读任务数据，也不改 core 的确定性模型**（同 seed 同输入仍必得同结果）。
- **防重复**：`command` 奖励天然只能领一次；Statecraft 侧再记一次"本时代钥匙已用"，避免重复推进。

### 需要新增的一小块（mod 侧）

**可被命令触发的"提前推进"入口**。现状：只有按累计在线小时推进 + 一个开发用的
`/statecraft advance <hours>`（它走的是小结算，不是时代推进）。

- 新增：一个明确的"提前进入下一时代"入口（命令或方块交互），由任务书的 `command` 奖励调用。
- 约束：仍走 core 纯逻辑 + JUnit + 真机脚本那套验证；**必须保留**"时间到自动推进"这条老路径。
- 这一块单列成一个实现任务，不混在任务书数据里做。

---

## 四、判据规范

### 4.1 映射规则（把中文判据翻成可检测事实）

| 中文判据形态 | 用什么判据 | 例 |
|---|---|---|
| 建造 / 放置 | `item`（持有该方块，可带数量） | "第一间避难所" → 床 + 门 |
| 数量 / 存料 | `item` + count | "公共仓库存 1000 单位" |
| 探索 | `structure` / `biome` / `dimension` / `stat` | "走出生点 500 格" → 行走统计 |
| 防御 | `kill` | "守住一次袭击" → 击杀 N 个 |
| 进度 | `advancement` | 原版/模组进度 |
| 到过某地 | `location` / `observation` | 到达/看见某结构 |

### 4.2 `checkmark` 例外清单（**初版，逐条给理由**）

| # | 任务 | 为什么没有可检测信号 |
|---|---|---|
| 1 | 活着过第一晚 | 无状态信号（天亮时是否在附近需要自定义存档数据） |
| 2 | 定下约定（一页约定进议事厅） | 书的**内容**无法检测（放下书架可检测，但"约定了"不能） |
| 3 | 地图墙覆盖核心区 | "覆盖"是主观覆盖率 |
| 4 | 终局碑的刻字 | 碑文内容无法检测 |

**其余一律强行找可检测替身。** 例："国史成书" → 做出成书物品（`item`，可检测）；
"插下第一根界碑" → 持有界碑方块并放置（`item` + `observation`）。

### 4.3 id 与命名规范

- 章 id / 任务 id：**16 位十六进制**，由"稳定名字 + 序号"哈希生成 → **可复现、可回归**
  （改一处不会让别处的 id 漂移，也不怕重复跑生成器）。
- 文件名：`chapters/<章 key>.snbt`；正文 `lang/zh_cn/chapters/<章 key>.snbt`。

### 4.4 图标、形状、依赖

- **图标语义**：任务图标 = 这件事要做的那个物品/机器（扫一眼就知道要干嘛 —— 这是 ATM 最有效的引导手段）。
- **形状语义**：主轴 `rsquare`/`pentagon` · 支线 `circle` · 终局 `hexagon`（超大）· 可选任务 `optional`。
- **依赖**：主轴串联成链；支线挂在对应主轴节点上；**跨章只用 `quest_links`（软链接），不做硬前置** ——
  这样"主线导向 mod 章"不会变成作业。

---

## 五、引导文字：放哪、写多长、谁写

- **位置**：`config/ftbquests/quests/lang/zh_cn/`（Lang Splitter 按章分文件）。**结构与文字彻底分离**。
- **键**：`chapter.<id>.title` · `chapter.<id>.chapter_subtitle`（章首一句）· `quest.<id>.title` ·
  `quest.<id>.subtitle` · `quest.<id>.description`（正文）。
- **长度**：章首引导 ≤ 1 句（30 字内）；**主线任务** description 2~4 句（为什么做 / 卡在哪 / 怎么做，≤120 字）；
  **支线与 mod 章任务默认不写 description**，只有标题（避免文案海洋）。
- **分工**：**我写白话版**（保证可玩、可验证），**用户替换成碑文/方志腔**。语言键、结构与占位符不动，只换字。

---

## 六、奖励规范

- **原则：奖励是游戏里的东西，不是任务的终点。** 不发放"只有任务才用得上"的物品。
- **克制**：ATM 平均 1.3 条/任务；我们**大多数任务不给奖励**，只在三处给：① 主线节点 ② 章节里程碑 ③ 终局大任务。
- **类型分配**：主线完成 → `command`（时代钥匙）+ `toast`（世界内叙事反馈）；支线 → 少量 `xp`；
  **卡壳点起步材料** → 一次性 `item`，只在"该 mod 的第一步需要玩家在当下不可能拥有的材料"时才发
  （例：刚进 Create 却还没有铜），只发基础材料、只发一次；**章节里程碑** = 该时代主线的完成节点（终局大任务）→ `loot` 表。
- **红线**：不发稀有矿物/机器成品，不破坏经济，不让玩家"为了奖励"去做本来不会做的事。

---

## 七、数据、工具与目录

```
pack/config/ftbquests/quests/          ← 仓库里的源（packwiz 会把它装到实例的 config/ftbquests/quests/）
├── chapter_groups.snbt        # 四栏分组（编年/邦交/技艺/类目）
├── data.snbt                  # 全局默认（progression_mode=flexible 等）
├── chapters/<章 key>.snbt     # 22 章结构（生成器产出）
└── lang/zh_cn/                # 正文（用户改写的地方）
    ├── chapter.snbt  chapter_group.snbt  chapters/<章 key>.snbt  …
```

- **生成器**：`tools/questbook.py`（读任务书定义 → 产出 `chapters/*.snbt` 与 lang 骨架）。
  任务定义用一个**可读的中间文件**（TOML，进 git：`tools/questbook/*.toml`），不直接手写 SNBT ——
  SNBT 由工具生成，保证 id 稳定、依赖不写错。
- **校验器**：`tools/questbook_check.py`（离线，见 §八）。
- **分发**：`pack/index.toml` 里逐个登记 `config/ftbquests/…` 下的文件（packwiz 装到实例）。

---

## 八、分期落地与验收

### 期 1（现在就做）

生成器 + 校验器骨架，先出 **五章**：**序 · 落地（E0）· 邦交 · 机械动力 · 防御与尸潮**
（结构 + 白话文字 + 图标与依赖），跑服务端加载验证。

### 样板评审（需要用户：开着游戏看一眼，不需要操作）

用户进游戏看这五章的手感与腔调 → 定稿模板（章节布局、依赖密度、文字长度、图标选择）。

### 期 2 / 期 3

- 期 2：按模板铺满其余 17 章。
- 期 3：客户端批次时补 ATM 那套**图片引导** + 微调布局。

### 离线验收（我自己能做的，全部自动化）

`tools/questbook_check.py` 检查：

1. **服务端加载零错误** —— FTB Quests 服务端也加载任务数据，解析失败会写日志（真机证据）。
2. **id 唯一**，且由稳定名字哈希得到（重跑生成器不漂移）。
3. **依赖无环、无悬空引用**；跨章 `quest_links` 双向可解析。
4. **判据类型在白名单内**（§1.3 实测清单）。
5. **每个任务都有对应 lang 键**；lang 里没有孤儿键。
6. **`checkmark` 只出现在例外清单**里（清单外出现即报错）。

### 人工验收（需要用户或客户端）

图书排版是否清楚、指路是否真看得懂、图标是否一眼能认出、正文腔调是否对。

---

## 九、明确不做（YAGNI）

- ❌ 不做"任务专属"内容（不为任务凭空造地方、造物品、造步骤）。
- ❌ 不做硬门闸（除时代钥匙外，任何任务都不阻断玩法）。
- ❌ 不锁死任务总数（199 是旧公式的产物，已废）。
- ❌ 不按"每个 mod 2~4 条"敷衍边缘 mod（同类归入类目章，簇内给足 3~8 条）。
- ❌ 不在期 1 做图片引导（需要客户端截图，放到期 3）。
- ❌ 不抄 ATM-10 的任何文字（ARR，且我们仓库公开）。
- ❌ 不重复 mod 自带教学（Ponder / JEI / EMI）。

---

## 十、开放问题（已给默认值；实现时若发现不对再改，改之前先说一声）

1. **「邦交」章是否要拆成两章**（情报与外交 / 图纸与夺城）—— **默认：不拆**；实际任务数 >40 条时才拆。
2. **时代钥匙的命令语义** —— **默认：`/statecraft accelerate`，不取代价**。理由：钥匙本身就是"做完主线"的回报，
   再收一次费等于对做完的人二次收费。可逆（后面要加代价只需改一处参数）。
3. **是否用 `stage` 隐藏未到时代的任务** —— **默认：不用**。任务书是引导手册，藏起来反而不引导；
   内容的时代闸门由玩法本身（配方、材料、维度）决定，不由任务书决定。
4. **`progression_mode`** —— **默认：全局 `flexible`**（ATM 同款）。时代章主轴内部靠**依赖链**表达先后，
   额外用 `linear` 会把"还没轮到"的任务整个藏掉，与"引导看得见"冲突。
5. **22 章的具体归属**：每个类目章里到底收哪些 mod —— 期 2 时按 `pack/mods` 清单逐个归位（届时逐章过一遍）。

---

## 十一、证据索引

- ATM-10 结构统计：`AllTheMods/ATM-10` 的 `config/ftbquests/quests/`（66 章 / 2.6 MB；抽样 12 章 1,935 任务 /
  4,006 判据 / 2,956 依赖 / 0 description / 472 图片）。该目录 **All Rights Reserved**，本仓库不复用其内容。
- 我们版本的判据与奖励类型：从 `ftb-quests-neoforge-2101.1.36.jar` 的
  `dev/ftb/mods/ftbquests/quest/{task,reward}/*.class` 实测列出。
- 时代机制现状：`mods/statecraft/core/.../settle/SettlementClock.java`（`ERA_ADVANCE` 由累计在线小时触发）。
- 定位语与内容量口径：`DESIGN.md` §十五；`README.md`「时代推进靠时间到或做完并主动加速，取先到者」。
- 旧草稿：`TASKBOOK-300H.md`（199 条结构稿，保留作参考，不作为配额）。
