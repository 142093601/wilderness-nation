# 计划 3：结算引擎 + 外交状态机 + 国家对玩家压力（core 侧）

> 目标（M1 的剩下部分）：**时代推进 / 周期小结算**两套节拍跑起来，
> 国家之间的战争→灭亡/吸收、扩张疲劳、态度回归、外交状态机、国家对玩家的压力全部落地，
> 并且**全程离线可验**（纯 Java，不开游戏）。
> 前置：计划 1（生成骨架）、计划 2（存档与迁移）已完成，共 129 个 JUnit 用例全绿。

---

## 一、必须先把设计里的一处矛盾定案（否则算出来的成功率是错的）

`NATIONS.md` §十二 给的**外交成功率**是：

```
p = clamp(0.05, 0.95, 0.5 + (attitude−50)/200 + (devYou−devThem)/200 + stance/400)
```

`attitude−50` 只有在 **态度是 0~100** 时才是"以中性为中心"。但同一份文档里：
- §9.1 国家宣战门槛：**`attitude ≤ −40`**
- §11.2 结盟前置：**`态度 ≥ +40`**；通商前置：**`态度 ≥ 0`**

这些阈值**必须**用有符号的态度才讲得通。所以 §十二 里的 `−50` 是**照 0~100 标度写的遗留**。

**定案（本计划采用，并改文档）**：

```
p = clamp(0.05, 0.95, 0.5 + attitude/200 + (devYou−devThem)/200 + stance/400)
```

理由：`attitude=0` 是中性（+0.5 基座）、`stance=±100` 给 ±0.25、发展度差 ±100 给 ±0.5；
`attitude ∈ [−100,100]` 时整体落在 [0.05, 0.95] 的合理区间里。
（`Nation.attitudeToParty` 在计划 1 就已经是 `−100..100` 并有测试钉住，不需要改模型。）

---

## 二、设计**没给**的数，一律做成显式"占位参数"

规则要照设计写，但下面这些**设计没有给数值**。绝不在代码里偷偷写一个魔数，
一律进 `StatecraftConfig`，在 javadoc 与 `NATIONS.md` §十二 里标成 **占位（待平衡）**：

| 缺的数 | 本计划的占位默认 | 影响 |
|---|---|---|
| 军力恢复基础速率 | 每小结算 `+ceil(size × 0.5)`（疲劳按 ×0.85^fatigue 削） | 战争节奏 |
| 国家之间开战的基础概率 | 每小结算 `0.06 × (1 + stance/100) × 1.15^fatigue` | 世界有多乱 |
| 灭亡的 `warScore` 落后阈值 | `−60` | 灭亡快慢 |
| 国家对玩家宣战的额外修正 | 发展度每落后 10 点 `×1.1`（上限 ×2） | 压力强度 |
| 袭击发动概率（已宣战） | 每小结算 `0.5` | 被打的频率 |
| 内部事务上限（每小结算最多开战/停战几对） | 2 | 性能与观感 |

**为什么这样做**：§十二 自己写着"默认值全部走配置"。这些数**必然会**在真机试玩里改，
写死在公式里就得改代码重编译；放进配置则改一次 json 就能试。

## 三、明确**不做**（避免写没人用的东西）

| 不做 | 为什么 |
|---|---|
| `traits`（尚武/富庶/记仇/开放） | §八/§十二 里**没有任何规则读它**。加了就是"没人用的字段"，等有规则用它再加（那时正好走一次迁移）|
| 事件的中文文本渲染、周报、`places.json` 地名池 | 属于 M3 的情报册/周报。本计划的事件**带结构化 `textKey` + `params`**，渲染在计划 4 接上——这样文本改动不会牵动规则 |
| `letters`（国书）、`buildings`、`domains`、`playerLedger` | 计划 4/5 才用得上 |
| 袭击的实体化（刷 HYW 兵种） | 需要 MC 与 HYW，属于 mod 层；本计划只算出"打了一次、规模 N、损失 M" |
| 夺城环节本身 | 计划 5；本计划只提供 `可占领` 判定（§十二 的门槛公式）|

---

## 四、任务

### Task 1 · 模型扩展（`schemaVersion 1 → 2`）
- `Nation` 增：`status`(`alive`/`dead`)、`warOnParty`、`fatigue`、`absorbedCount`（`traits` 见上，不做）
- 新增 `Relation`：`{ attitude, state(peace/war/alliance/truce), warScore, truceUntilSeq }`
- 新增 `Event`：`{ seq, type, parties, textKey, params, visible, read }`
- `WorldState` 增：`elapsedOnlineHours`、`relations`、`events`、`unread`
- **正好用上计划 2 的迁移链**：`StateMigration(1, …)` 把 v1 存档补上这些字段的默认值，
  并加"v1 老档能迁到 v2 且内容不变"的测试（这是迁移框架第一次接真实迁移）

### Task 2 · 结算引擎（`settle/SettlementEngine`）
| 步骤 | 依据 |
|---|---|
| 累计在线小时 → 时代推进判定 | §七（`EraTable.currentFor`）|
| 军力恢复 + 扩张疲劳衰减 | §八.6、§十二 |
| 态度回归（向 0 靠 1~2 点） | §十二 |
| 国家 ↔ 国家：开战 → 逐步推进 → 灭亡 / 吸收 | §八.1~5 |
| 国家对玩家压力：宣战门槛、袭击规模与胜败后果 | §9.1~9.3、§十二 |
| 事件写进 `events`，按 500 保留 / 200 未读上限裁 | §十二 |
| `seq` 自增；**每步随机都按 `(seed, seq, 用途标签)` 键控** | 计划 1 的确定性纪律 |

**吸收的语义（设计里最容易被写错的一条）**：胜者 `size` 变大 → 由"反比公式"重算 `dev` →
**它发展度会掉**。这不是 bug，是设计要的"打赢了反而变落后"。

### Task 3 · 外交状态机（`diplomacy/DiplomacyMachine`）
§11.2 的 6 个动作，**前置被违反必须拒绝**（§十五 明确要求），
转移合法（`peace↔war↔truce`、`alliance`）与停战冷却（1 个周期）都要有测试。

### Task 4 · 事务日志与重放（补计划 2 欠下的那一项）
现在"结算输入"终于定义清楚了：
- `SettlementLog` 记每条 `{ seq, trigger, elapsedHoursDelta, inputHash }`
- **重放**：从 `seq=0` 用同样的 seed 重跑一遍，逐步比对状态哈希
- 测试：跑 40 个 seed × 6 个时代的结算 → 重放结果**逐字段相等**（这是"崩溃后不废档"的地基）

### Task 5 · 文档同步
`NATIONS.md`（§五 字段、§十二 加"占位"列、§十三/§十五 更新、§十七 M1 行）、
`mods/statecraft/README.md`、本计划追加执行记录。

---

## 五、验收（全部离线，`gradle :core:test`）

| 用例 | 断言 |
|---|---|
| 同 seed 同输入可重放 | 40 seed × 6 时代，重放后状态逐字段相等 |
| 吸收与发展度反比 | 吸收后 `size` 上限 10、`dev` 按公式重算且**确实下降** |
| 防雪球 | 连跑 6 个时代后**最大国不超过阈值**（`fatigue` 生效）|
| 灭亡边界 | `dev=24 vs 25`、连输 `2 vs 3` 两侧分别不灭/灭 |
| 外交前置 | 每个动作的非法前置**必须抛/拒绝**；停战冷却生效 |
| 袭击规模 | 上限 24；`2 + floor(military/25)` |
| 态度回归与周期上限 | 每周期向 0 靠 1~2；玩家行为扣分不越周期上限 |
| 事件保留 | 超过 500 条时丢最旧；未读上限 200 |
| 迁移 | v1 老档迁到 v2 后内容不变、只多出默认字段 |

---

## 执行记录

### Task 1 ✅（`fde43ac`）

模型扩展 + schema v1→v2。产出见那次提交的说明。要点：`traits` 与事件文本渲染**刻意不做**；
v1 是真实发布过的格式（测试世界的 `.dat`），所以 v1→v2 不是死代码。

### Task 2 ✅（结算引擎）

产出：`settle/SettlementConfig`、`SettlementInput`、`SettlementTrigger`、`SettlementEngine`，
以及 `Relation.losingStreak`（§八.4 要"连输 ≥3 步"，§五 没这个字段，**有意追加**；
当时 v2 还没写出过任何存档，所以直接并进 v2，不必再加一级迁移）。

`SettlementEngineTest` 24 条用例，覆盖：时钟与时代推进、全体成长、态度回归、疲劳衰减与恢复惩罚、
吸收（规模上限 + **发展度确实下降**）、灭亡三条件边界、战果与连败累计、宣战门槛边界、袭击规模与上限、
灭亡国不再参战、开战概率 0 时世界和平、**40 个 seed 的重放一致性**、长跑不炸、以及两条绊线。

#### 测量推翻了我自己的两个假设（这是本轮最值钱的部分）

写引擎时我用"跑一遍量一量"代替"想当然"，量出两件事：

**① §八.4 的 `development < 25` 在原公式下不可达 → 灭亡分支是死代码。**
§六 的生成公式是 `dev = clamp(20, 95, 95 − 6×(size−1) + rng(−8,+8))`。代入 `size = 10` 得 `41 + 抖动`，
而抖动 ±8 → **规模到 10 的国家发展度恒在 [33,49]**；吸收只让规模变大、发展度按规模差下降，
规模封顶 10 之后就不再降。所以全世界的 dev 下限就是 **33**，"dev < 25" 永远不成立。
调参探针实测（20 seed × 6 时代）：**灭亡 0 次**。
→ 实现把阈值抬到 **40**（"弱国"= 已经膨胀到规模 9~10 的国家，正好是设计要的那句"旧世崩了"），
并留了一条用例把这个 33 的下限钉住（改 `devBase`/`devPerSize` 时会先红）。

**② 灭亡阈值必须严格小于吸收阈值，否则灭亡永远被吸收抢先。**
原来的占位值是灭亡 60 / 吸收 50：战果一到 50 就先走了吸收，战争结束，再也够不到 60。
→ 现在 `SettlementConfig` 的构造器**强制** `collapseWarScore < absorptionWarScore`，
默认改成 40 / 50，并有一条用例断言"默认参数下 20 seed × 6 时代里灭亡次数必须 > 0"（绊线）。

**③ 吸收后的发展度不能重摇抖动。**
§八.5 只说"dev 按反比公式重算"。照 §六 重摇一次抖动的话，"打赢了反而变落后"会被抖动推翻 ——
规模 +4 只让基准掉 24 点，而抖动范围 ±8，重摇完全可能把发展度摇**高**（规模已到 10 时尤其明显：
规模一点都不变、发展度却上下跳）。实现改成"**沿用该国原有抖动，只按规模差扣**"，
于是"规模变大 ⇒ 发展度必然下降"变成结构性事实，而且这一段不再消耗随机数、重放更稳。

#### 顺带确认了设计的一个意图被满足

§七 要求时代推进出"国家间事件 1~3 条"。实测默认参数（开战概率 0.06、每结算最多 2 笔事务）:
**时代事件 avg 3.0、min 2、max 3**（其中 1 条是「天下大势」本身），落在设计给的区间里。
所以这两个占位值就按测量结果定下来，不再凭感觉改。

### Task 3 ✅（外交状态机）

产出：`diplomacy/DiplomacyAction`、`DiplomacyConfig`、`DiplomacyRequest`、`DiplomacyOutcome`、
`DiplomacyResult`、`DiplomacyMachine`，以及 `DiplomacyMachineTest` 21 条用例。用例 168 → **188 全绿**。

覆盖：6 个动作的前置逐条拒绝（且**被拒绝时状态对象必须是同一个**——这是"拒绝不改状态"最硬的写法）、
宣战/结盟/通商/朝贡的效果、求和与求援**两个分支都可达**（骰子确定性，所以"两种结果都出现过"是可以断言的，
不是碰运气）、公式边界与钳制、战果上报、朝贡承诺真的挡住宣战并在到点后失效、停战冷却按结算周期算、
同 seed 同 nonce 复现 / 换 seed 结果会变。

#### 建模上的两个更正（都是"照着 §五 写会写出坏模型"）

**① §五 的 `warOnParty` 布尔 → `PartyStatus` 枚举 + 期限 + 战果。**
§11.2 除了交战还要表达**同盟**（结盟）与**停战/不侵犯**（求和、朝贡）。
再补两个布尔，`warOnParty=true` 且 `alliedWithParty=true` 这种组合在类型上就拦不住。
枚举让四种状态先天互斥。§五 那句"（含战果）"落在 `partyWarScore` 上。

**② 玩家动作的随机序号是"外部输入"，不该塞进世界状态。**
我先按"给 WorldState 加一个 `actionSeq`"写，写到一半发现那是错的：
玩家动作与命令同类，属于外部输入；把计数器藏进世界状态，会让
"同 seed 同输入必得同结果"悄悄变成"同 seed 同输入还得看它内部数到几"，
而且事务日志（Task 4）也就没法把它记下来重放。
→ 改成 `DiplomacyRequest.nonce`（调用方给，通常用世界刻），**并撤掉了已经写进 WorldState 的字段**。
好处顺带抵消了一批测试改动。

**③ 玩家账本不编。** 通商/朝贡给玩家的国库变化、求援的战力加成，core 只**报出来**
（`playerTreasuryDelta` / `aidGranted`），由调用方记账；"你方发展度"也由调用方递进来。
§十二 公式里的 `(devYou − devThem)` 因此现在是"调用方给 0 就等于这项不参与"，而不是假装有个账本。

### Task 4 ✅（事务日志与重放）
产出 `persist/StateHash`（64 位 FNV-1a，作用就是日志里的"输入哈希"）、
`settle/SettlementLogEntry`、`settle/SettlementLog`、`settle/ReplayResult`，
以及 `SettlementLogTest` 10 条用例。用例 188 → **199 全绿**。

- 日志记的是**输入**（结算序号 + 触发方式 + 小时数 + **动手前状态的哈希**），不是结果 ——
  结果由输入 + seed 重算得出来，那正是确定性的意义。
- `replay` 逐步校验"日志记的输入哈希 == 我手上这个状态的哈希"，对不上就**立刻停下**并报第几步分歧，
  而不是继续算出一段看起来合理、实际是假的历史。
- **只说 `replay` 不够**：篡改的要是正好最后一条，中间全对得上。所以另有 `verify` 核对最终结果 ——
  两种失败都被用例钉住了（一个报第 7 步分歧、一个由 verify 抓到最后一条）。
- `since(seq)` 支持"从上一个完整结算（快照）接着重放"，这条也单独测了。
- 日志**刻意不放进 WorldState**：§五 的字段表里没有它，而且它记的是操作历史、不是当前状态，
  混进状态会让每次存档都跟着变长。它由 mod 层和状态存在一起（同一个 `.dat` 两个字段）。
- **哈希算法被用例钉住了一个具体值**：改了它，存档里所有 `inputHash` 就全失效，
  这条会红并提醒"必须有意为之 + 写进迁移说明"。

### Task 5 ✅（文档同步）
`NATIONS.md`（§五 Nation/Relation 字段更正、§八.4 灭亡阈值更正、§11.2 实现说明、§十二 实现位置与占位、
§十三/§十五 补实现现状、§十七 M1 行）、`mods/statecraft/README.md`、本计划的执行记录。

---

## 计划 3 收尾状态

| Task | 状态 |
|---|---|
| 1 模型 + schema v1→v2 | ✅ `fde43ac` |
| 2 结算引擎 | ✅ `a0ce8db` |
| 3 外交状态机 | ✅ `6c1ab7f` |
| 4 事务日志与重放 | ✅ 本次提交 |
| 5 文档同步 | ✅ 本次提交 |

**仍然不在计划 3 里的**（写清楚，免得以后误以为漏了）：
事件中文文本渲染 / 周报 / 地名池（计划 4）、`letters`/`domains`/`playerLedger`（计划 4/5）、
袭击的实体化（mod 层 + HYW）、夺城（计划 5）、
以及"结算/外交参数接数据文件"（等真的调平衡时再做，现在多一层没人读的解析没意义）。

**M1 剩下的两块另开一份计划**：环境判定与幂等决策是纯规则、同样离线可测，
放在 `PLAN-m1-env-idempotency.md`（插在计划 4 之前 —— 它们正是计划 4 的核心机制）。
