# 押后验收清单（一次性跑完，不要零散地做）

> **原则**：需要"开服务端 / 开两个客户端 / 人眼看画面"的项目，**全部攒到一次做**，
> 不要挤在日常改包的流程里。日常改动只需要跑**离线 + 单机自动套件**（都是秒级或分钟级、无需人参与）。

---

## 现在就能自动跑（不需要你，任何时候）

| 套件 | 命令 | 上次结果 |
|---|---|---|
| 装载冒烟（L0） | `python autotest.py --seconds 45` | 通过 |
| 单机功能回归（L3） | `python game_agent.py --script tests\opac_v0.txt --launch` | 6/6 |
| 服务端回归（L1） | `python server_ctl.py --start` + `--script tests\server_v0.txt` | 22/22（连跑两次一致） |
| 联机链路 | `python game_agent.py --server 127.0.0.1:25565 --script tests\client_server_v0.txt` | 9/9 |
| **图纸校验（离线）** | `python blueprint_check.py --dir <图纸目录>` | 10/10 结构 · 12/12 方块 ID |
| **中文本地化审计（离线）** | `python lang_audit.py --mods <实例mods>` | 19 个 jar：1 个缺中文 |
| mod 端侧分类（离线） | `python side_check.py --mods <实例mods> --write-lists lists` | 15 服务端 / 4 纯客户端 |

---

## A. 联机功能验收（需要：服务端 + 2 个客户端同时开）

> 为什么必须后置：跨玩家权限**物理上需要两个真实玩家**；而且两个客户端 + 服务端大概要 5~6GB 内存，
> 和日常改包无关，不该零散占用。

- [ ] **A1 跨领地保护**：玩家 B 进玩家 A 的领地 → 尝试破坏 / 放置 / 开箱子，**应被拒绝**
- [ ] **A2 领地内机械**：玩家 B 在 A 的领地里操作 Create 机械（最典型是 **Deployer**，它用假玩家交互，是最容易被误拦的一类）
- [ ] **A3 队伍共有领地**：开 `partyOwnedClaims` 后，同队玩家能否共同操作（对应设计里的"国家领地"）
- [ ] **A4 图纸 × 领地**（**项目关键路径**）：在**已被圈占的领地内**用 Schematicannon 放一张图纸 —— 如果被领地拦下，整套图纸流水线在有国界的区域就废了
- [ ] **A5 领地界面**：按 `'` 打开 OPAC 界面，在联机下是否正常（单机已验）
- [ ] **A6 OPAC 的服务端配置落盘**：`serverconfig/` 里的领地配置改完后是否随世界保存

**工具已就绪**：`server_ctl.py`（起服/RCON/断言）、`game_agent.py --server`（客户端连服）。
两个客户端只要用不同的 `--username` / `--uuid` 起两次即可，不需要新写工具。

---

## B. 界面与视觉（需要人眼，单机即可）

- [ ] **B1** Create 的 **Ponder 教学界面**（中文？）——静态审计已知 Create 99% 有中文，但**排版/换行只能看**
- [ ] **B2** OPAC 领地界面在中文客户端下的实际观感（英文界面混在中文 UI 里有多违和）
- [ ] **B3** 图纸放出来后的**观感**（美化程度）—— 属于主观项，只能人判
- [ ] **B4** 目标字体：中文字体是否清晰（`zh_cn` 默认字体在 mod 界面上有没有缺字）

---

## C. 中文补全（不需要游戏，可以现在做）

- [ ] **C1** 给 **OPAC 补 613 条 zh_cn**（`assets/openpartiesandclaims/lang/zh_cn.json`）→ 做成资源包。
      这是唯一的大缺口；文本大多是模板句（"Successfully claimed the chunk at %s"），适合批量翻译 + 人工抽查。
- [ ] **C2** 补小缺口：Embeddium（74 键仅 6 条中文）、modernfix（86%）、YBD（80%）、moreculling（89%）
- [ ] **C3** 复核完成后跑 `lang_audit.py` 确认覆盖率，并把资源包加进整合包

---

## D. 性能（等你的服务器就绪）

- [ ] **D1** 并发压测：N 个客户端下的 TPS/MSPT（本机内存只够 2~3 个，真实人数请在你那台机器上测）
- [ ] **D2** 复现排查 `Can't keep up! Running 4503ms or 90 ticks behind`（出现在 `save-all` 同步存档时，稳态无此问题）
- [ ] **D3** 设计稿 §12 的其余指标：袭击场景 ≥15 TPS、实体 ≤200、启动 ≤3 分钟

---

## E. 可选

- [ ] **E1** 组0 纯原版基线（没有它就无法回答"性能组到底省了多少"）——现在能自动跑
- [ ] **E2** 图纸生成器扩到 5 个模块（公共仓库 / 农田 / 熔炉阵列 + 功能占位）
