# 押后验收清单（一次性跑完，不要零散地做）

> **原则**：需要"开服务端 / 开两个客户端 / 人眼看画面"的项目，**全部攒到一次做**，
> 不要挤在日常改包的流程里。日常改动只需要跑**离线 + 单机自动套件**（都是秒级或分钟级、无需人参与）。

---

## 现在就能自动跑（不需要你，任何时候）

| 套件 | 命令 | 上次结果 |
|---|---|---|
| **电脑控制插件自检** | `python wcu_probe.py` | 22 工具 / health 全绿（2026-09-20，用法见 `tools/COMPUTER-USE.md`）|
| 装载冒烟（L0） | `python autotest.py --seconds 45` | 通过 |
| 单机功能回归（L3） | `python game_agent.py --script tests\opac_v0.txt --launch` | 6/6 |
| 服务端回归（L1） | `python server_ctl.py --start` + `--script tests\server_v0.txt` | 22/22（连跑两次一致） |
| 联机链路 | `python game_agent.py --server 127.0.0.1:25565 --script tests\client_server_v0.txt` | 9/9 |
| **图纸校验（离线）** | `python blueprint_check.py --dir <图纸目录>` | 10/10 结构 · 12/12 方块 ID |
| **中文本地化审计（离线）** | `python lang_audit.py --mods <实例mods>` | 19 个 jar：1 个缺中文 |
| mod 端侧分类（离线） | `python side_check.py --mods <实例mods> --write-lists lists` | 15 服务端 / 4 纯客户端 |

---

## G. M0：夺城的前置验证（2026-09-19 新增，**唯一挡住计划 5 的事**）

> 详细方案与脚本见 `PLAN-m0-hyw.md` + `tools/tests/m0_hyw.txt`。
> 为什么必须开游戏：要一个**真玩家**当靶子，而且怪得真的走过来动手。

- [ ] **G1 HYW 单位会不会主动攻击玩家**（判据：生存难度下玩家血量真的掉了 +
      `M0_RESULT=ATTACKS_PLAYER` 印记落进服务端日志）
      - 静态证据已指向"会"：`hyw_main.json5` 里 `nullOwnerUnitsAggressive=true` 且
        `nullOwnerUnitsFriendlyToEnemy=false`，而 `/summon` 出来的是**无主**单位 → 按配置应当攻击所有活物
      - 跑法：`server_ctl.py --setup --start` → `game_agent.py --server 127.0.0.1:25565 --script tests\m0_hyw.txt`
      - **判据刻意不依赖客户端按键**（玩家很可能被打死，死亡屏会吞按键；RCON 是另一条通道）
- [ ] **G2（G1 失败时才做）** 把 `nullOwnerUnitsFriendlyToEnemy` 改成 `true` 再跑一次 ——
      那会打开 `hostile_target_list` 那条路径。两次结果都要留证；这决定夺城是"改设计"还是"改配置"
- [ ] **G3（顺带）** `enableSupplySystem=true` 下，长期无人管的守军会不会因缺补给变弱
      （直接影响夺城"临时守军"的可行性）

**M0 未验成之前不要开始计划 5** —— 夺城整套（旗座/守军/指挥官/百姓隐藏）都建立在
"能按我们的意思刷出并指挥 HYW 单位"之上。

---

## H. 300 小时口径带来的内容量重算（**离线可做，不用开游戏**）

> 2026-09-19 时代时长整体翻倍（12/20/25/25/30/38 → 24/40/50/50/60/76 ≈ 300h）。
> 这动的不是"一个数字"，而是**内容量要求**——凡是按 150h 算出来的结论都要重算。

- [ ] **H1 任务书按 300h 重算**：`DESIGN.md` §15 的结论是"27 个任务只够撑约 1/8"，
      粗算需要 **~180~220** 个任务（原稿按 150h 算的是 ~90~110）。
      与"任务书正文"一起做（载体已定 FTB Quests，已装）
- [ ] **H2 探索内容够不够探**：探索时长从 ~6h 涨到 **~50h**（原稿按 25h 判断"够"），
      `DESIGN.md` §15 明确要求**实战验证后再决定加不加结构 mod**，不要提前塞
- [ ] **H3 前两个时代的内容密度**：24h + 40h = **64 小时**都在"选址、活下来、圈地、仓库"阶段，
      原稿已警告"撑不住"，现在要求更高（`DESIGN.md` §9.1 的连带硬要求 ②）

**为什么单列一组**：这三条都是**纯文档/内容规划**，不需要开游戏，可以在等 mod 侧推进的空隙里做。

---

## I. 计划 4 里**有意没做**的两件事（2026-09-20 记）

> 两条都是"现在做只会做出半截东西"，所以先记账、不动手。

- [ ] **I1 `treaties.json`（§十六 列的"外交动作定义：前置、成功率修正、后果"）**
      —— 数据化的收益是把前置与成功率挪进数据；代价是 `DiplomacyMachine` 现在每个动作的前置拒绝理由
      都是**针对那个动作写的中文**（"还在打仗，谈不了结盟"、"它还不够信你（态度 −12，需要 40）"），
      改成数据表只能给出通用模板，玩家看到的提示会变差。
      而且 `PLAN-statecraft-03-settle.md` 已经定过："结算/外交参数接数据文件"**等真的调平衡时再做**，
      现在多一层没人读的解析没有意义。**触发条件**：开始调外交平衡（改 6 个动作的前置或成功率）时再做。
- [ ] **I2 玩家侧的持久化（追踪清单 / 已购图纸授权 / 深挖计数 / playerLedger）**
      —— core 已经把**规则**做完了（`IntelBook.track` / `IntelBook.deepDive` / `BlueprintRules`），
      它们收的是"调用方递进来的事实"、输出的是判定与要扣多少。**存哪儿**属于计划 5 的玩家账本
      （和外交那套同一个理由：core 里塞一个玩家账本会让"同 seed 同输入必得同结果"变成
      "还得看它内部数到几"）。**触发条件**：做计划 5 的玩家账本时一起落盘。

---

## J. **实体存了但没装回世界**（2026-09-20 真机实测，**已定位一半**）

> 这是跑阶段 3 真机验收时撞出来的：**我们的村民每次重启都消失**。
> 往下查发现更狠的现象：重启后整个世界 `execute if entity @e` 是 **0 个**，
> 连原版 `/summon` 出来的村民也没有。
>
> **但盘上它们是有的** —— 用新写的 `tools/mca_probe.py` 直接读实体区：
>
> ```
> python tools/mca_probe.py --region server/world/entities/r.0.0.mca --chunk 6 6
>   Entities：7 个
>     - minecraft:villager @ 105.5,152.0,103.5 name="Jaunita"
>     - minecraft:villager @ 103.5,152.0,103.5 name="陆知白"
>         data={'statecraft:profession': 'scribe',
>               'statecraft:building': 'intel_station@minecraft:overworld:103,151,103'}
>     ...（另外 5 个同样是我们刷的，标签都在）
> ```
>
> 所以**不是存的问题，是读的问题**：区块的方块装回来了（讲台还在、`anchorIntact=true`），
> 实体却没装回来。

**2026-09-20 下午补的同步证据**（用 `--script` 而不是连着敲 `--cmd`，见 §K）：

| 步骤 | 回显 |
|---|---|
| `forceload add` + 等 6 秒 | 区块已 force-load |
| `/statecraft buildings` | `loaded=true`（方块、锚点都在） |
| `execute if entity @e[type=villager,x=103.5,y=152.0,z=103.5,distance=..16]` | **`Test failed`（0 个）** |
| `mca_probe.py --region world/entities/r.0.0.mca --chunk 6 6` | **Entities：4 个**（3 个我们 + Jaunita），文件时间 13:49:58 = 上一次关服保存 |

即：**区块加载了、盘上也有，就是没进世界**。已排除 `neruina.dat`
（`tickingEntries` 是空数组 —— 它没有禁掉任何实体）。

**2026-09-20 晚：客户端这条路实测到的两个硬约束**（都是环境，不是代码）

| 观察 | 数值 |
|---|---|
| 客户端从启动到「Loaded 16472 recipes」 | **约 18 分钟**（232 个 mod；Exposure 等几个 mod 还会去拉远程名单，本机连不上、每次等到连接超时）|
| 进世界那一刻的**可用内存** | 掉到 **1.3G**（客户端 `-Xmx8G` + `node` 3G + 系统）|
| 把等待从 420s 放到 900s | 仍然超时（当时还在重载资源包）|
| 现在（`WORLD_READY_SECONDS=1800` + `-Xmx6G`）| 走到 recipe 阶段 |

**结论**：跑客户端批次时，**必须先把内存腾出来**（`NEEDS-YOU.md` 第一节 有清单），
而且**要按半小时算**，不要按五分钟算。工具那侧的等待已经改成 1800s / 600s
（`tools/game_agent.py` 里有实测注释说明为什么）。

**最可能的原因**：这台验证服务端**从头到尾没有一个玩家在线**。
1.17 之后实体存在 `<world>/entities/*.mca`，而实体是在区块进入 **entity-ticking**
状态时才装进世界的 —— 那要求区块处在某个玩家的实体距离内。
force-load 只保证区块被加载、方块在跑，**不等于实体被装回来**。
（所以这很可能是原版行为，不是整合包 bug；之前怀疑的"某个 mod 把实体序列化搞坏了"可以降级。）

- [ ] **J1 带一个客户端重跑（第一步，最省事）**：
      `server_ctl.py --start` 之后用 `game_agent.py --server 127.0.0.1:25565` 连进去站到
      (103,152,103) 附近，再 `/statecraft buildings` —— 判据：**`healthy=true` 且房间里只有
      一个村民**。若是，J 关闭（只是"没人时实体不装"的原版行为，验收必须带客户端）。
- [ ] **J2 若带客户端仍不装**，才去二分那几个动实体序列化的 mod：
      `servercore`、`easy_mob_spawn_control`、`chunkplan`、`neruina`、`ScalableLux`、`connectivity`。
      判据依旧是重启后那一行 `execute if entity @e` 的 `Test passed` / `Test failed`，
      而 `mca_probe.py` 可以随时回答"盘上到底有没有"。
- [ ] **J3 修复后的回归**：连续重启两次，`/statecraft buildings` 稳定 `healthy=true` 且只有 1 个村民。
- [ ] **J4 结算调度的「有人在线的」分支** —— **已经不需要真等 2 小时**：
      `/statecraft pending <hours>` 往待结算的在线小时里注水，调度器下一拍就会走
      **和真实在线完全相同**的代码路径（只是时间不是自然流逝来的）。
      判据：日志出现 `STATECRAFT_SETTLE kind=PERIODIC`。
      另有 `tools/enable_cheats.py`：服务端世界拷成单机存档后 `allowCommands=0`，
      单人模式没有命令权限 → 必须先打开它，否则脚本会报一堆「未知命令」。
      单机验收脚本：`tools/tests/sc_singleplayer.txt`（同时验 §J 与 §J4）。
- [ ] ~~J4 旧描述~~：现在只实测了"0 人在线 → 一点都不推进"。
      带一个客户端连进来站几分钟，判据：日志出现 `STATECRAFT_SETTLE kind=PERIODIC`
      （每 2 小时累计在线）或 `kind=ERA_ADVANCE`（跨时代），且 `info` 里的 `hours` 真的在涨。
      为了不用真等 2 小时：进服前先把 `statecraft.dat` 里的 `pendingHours` 手改成 1.99
      （**它在 mod 层，不影响 core 的 schema**），进服后 1 分钟内就该结算一次。

**v1 的行为（有意如此）**：村民真没了 → 按 §10.4 走**重雇冷却（1 小时累计在线）**再雇一个。
而"**重复生成**"那条路已经被两道闸挡住：§10.3 的"同一 seq 不重复生成"+ mod 层"按标签改认"
（`BuildingService.findStaff`）—— 实测在修之前攒出过 **6 个文书**，修之后不会再发生。

**工具**：`tools/mca_probe.py`（读 region / 实体区里的区块 NBT，打印 `Entities` 清单与实体上的
`NeoForgeData`）—— 以后凡是"东西到底在不在盘上"的问题都用它，别猜。

---

## K. 工具坑：连着敲 `--cmd` 会看到**错位**的回显（2026-09-20 踩）

`server_ctl.py --cmd` 每次重连一次 RCON；服务端忙的时候，上一条命令的回显会**晚一拍**
才发出来，于是下一条命令读到的是**上一条的输出**。

- 这个坑真的害过我：`kill @e[...]` 后面跟一条查询，看到的 "No entity was found" 其实是
  kill 的回显，而查询的回显跑到了再下一条 —— 于是误判成"重启后实体全没了"。
- **纪律**：任何两步以上的验证都用 `server_ctl.py --script <文件>`（它每条都同步等回显），
  不要用 `--cmd` 串起来。脚本里还能写 `wait: 6`（等下一条之前先睡，force-load 的区块加载是异步的）。
- 现成的例子：`tools/tests/sc_duplicates.txt`（重复村民自愈，6/6）、
  `tools/tests/sc_restart.txt`（重启持久性）。

---

## L. 设计坑：**门槛值不能与惩罚值同值**（2026-09-20 真机）

两个真实案例，都是"边界值在整条流水线上观察不到"：

1. **国书递书门槛 −30 vs 宣战惩罚 −30**：宣战完态度正好 −30，而同一拍里
   `applyAttitudeRegression` 会把它拉回 −28/−29 → 门槛永远踩不到，国书一封都递不出来。
   修法：门槛改 −20（明显松于惩罚）。
2. **`Location.setBlock` 返回 false 那条**（见 `tools/tests/server_v0.txt` 的注释）：
   把方块设成完全相同的状态时原版返回 false → 断言不能写在 `setblock` 上。
   （同类：`§10.3` 的 `materializedAtSeq == 0` 既是"从未"又是"新世界的当前 seq"。）

**纪律**：写"阈值/门槛"时，先问一句"触发它的那个动作会把值放到哪"，
以及"同一拍里还有谁会改这个值"。两者相等时，这条规则在游戏里就是死代码。

---

## M. **测试 fixture 压缩时间 → 掩盖气候级问题**（2026-09-20 真机 + 离线）

**现象**：真机跑到第 110 个累计在线小时时，12 国里 10 国已亡（见 `NATIONS.md` 八点五）。
离线量：12 个 seed × 6 时代，末代平均只剩 **1.0 国**（每个 seed 都只剩 1 个）。

**为什么 379 个用例一个都没发现**：长跑 fixture 是
「每个时代 2 次小结算 + 1 次时代推进」—— **无论时代多长都只有 18 次结算**，
而世界清空发生在第 100 多次。所有「长跑类」断言（不会被推平、灭亡/吸收会发生、
重放一致）都在这 18 次里取样。

- [x] 已把 `SettlementEngineTest.replay` 改成**按时代时长走够小结算**
      （`时长 / periodicHours`），和真实推进一致；`sixErasLeaveAWorldStanding`
      里那条 `seq == eras.size() * 3` 也换成「时钟真的走完整条曲线」。
- [x] 新增 `WorldSurvivalTest`：钉「末代至少要有一半国家活着」（§八 要的是「兼并会发生」，
      不是「世界会清空」），并把每个 seed 的存活数打印出来当原始证据。
- [ ] **纪律**：以后写「长跑类」测试时，先问一句「这个 fixture 到底模拟了多少时间、
      多少次结算」，以及「我要找的现象量级是多少」。**fixture 里的时间压缩 = 假绿**。
      和 §L（门槛值与惩罚值同值）是同一类错误：规则/断言看起来在，其实观察不到。

**参数怎么定的**（全部来自实测表，不是拍的）：
`warScoreStep` 12 → **4**、`absorptionWarScore` 50 → **120**（两个都是【占位】）；
`collapseWarScore = 40` 是 §八.4 的文档值，没动。
结果：末代平均 6.2 国、半程 9.3 国、平均兼并 5.7 次/局（兼并仍在发生）。

---

## A. 联机功能验收（需要：服务端 + 2 个客户端同时开）

> 为什么必须后置：跨玩家权限**物理上需要两个真实玩家**；而且两个客户端 + 服务端大概要 5~6GB 内存，
> 和日常改包无关，不该零散占用。

- [ ] **A1 跨领地保护**：玩家 B 进玩家 A 的领地 → 尝试破坏 / 放置 / 开箱子，**应被拒绝**
- [ ] **A2 领地内机械**：玩家 B 在 A 的领地里操作 Create 机械（最典型是 **Deployer**，它用假玩家交互，是最容易被误拦的一类）
- [ ] **A3 队伍共有领地**：开 `partyOwnedClaims` 后，同队玩家能否共同操作（对应设计里的"国家领地"）
- [ ] **A4 图纸 × 领地**（**项目关键路径**）：在**已被圈占的领地内**用 Schematicannon 放一张图纸 —— 如果被领地拦下，整套图纸流水线在有国界的区域就废了
- [ ] **A5 领地界面**：按 `'` 打开 OPAC 界面，在联机下是否正常（单机已验）
- [ ] **A6 OPAC 的服务端配置落盘**：`serverconfig/` 里的领地配置改完后是否随世界保存

**工具已就绪**：`server_ctl.py`（起服/RCON/断言）、`game_agent.py --server`（客户端连服）。
两个客户端只要用不同的 `--username` / `--uuid` 起两次即可，不需要新写工具。

---

## B. 界面与视觉（需要人眼，单机即可）

- [ ] **B1** Create 的 **Ponder 教学界面**（中文？）——静态审计已知 Create 99% 有中文，但**排版/换行只能看**
- [ ] **B2** OPAC 领地界面在中文客户端下的实际观感（英文界面混在中文 UI 里有多违和）
- [ ] **B3** 图纸放出来后的**观感**（美化程度）—— 属于主观项，只能人判
- [ ] **B4** 目标字体：中文字体是否清晰（`zh_cn` 默认字体在 mod 界面上有没有缺字）

---

## C. 中文补全（**已完成**，2026-09-20）

> **结论先说**：C1/C2 那一栏的"613 条"是**低估**——它来自 `lang_audit.py`，而那个工具
> 只看 jar 内部有没有 `zh_cn.json`，**看不见 I18nUpdateMod 同步进来的社区汉化包**
> （本机实例里那份包是启用的）。新增 `tools/i18n_cover.py` 把社区包一起算之后：
>
> | 口径 | 数值 |
> |---|---|
> | 有文本的 jar | 174 个（en 键 52714） |
> | 社区包+jar 自带已覆盖 | 41564（**79%**），52 个 mod 达 100% |
> | 真实缺口 | **11150 条** —— 拆成 界面/命令/提示 **5288** · 方块/物品名 **5862** |

- [x] **C1+C2 界面/命令/提示共 5288 条全部译完**（116 个命名空间），产出在
      `pack/i18n/zh_cn/*.json`（可评审源文件），合成为
      `pack/resourcepacks/荒野建国-中文补全.zip`（139 KB，sha256 见 `pack/index.toml`）
      - 术语基准：一律以 `data/i18n-style/<命名空间>.json`（社区已有中英对照）为准；
        没有基准的按原版 MC 中文惯例
      - 并发翻译，**独立验收**由 `tools/i18n_verify.py` 做（键集合 / 占位符 / 空值；
        子代理自检只当参考）：**0 个命名空间有结构性问题**
      - 被标"疑似整句英文"的 91 条已**逐条人工复核**：全部是命令字面量、模组名、
        曲名/作者、或刻意保留的技术词 —— **没有一条是漏译**
- [x] **C3 覆盖率复核**：`python tools/i18n_cover.py --mods <实例mods> --packs <实例resourcepacks> pack/resourcepacks/荒野建国-中文补全.zip`
      → **52714 中覆盖 46852（89%）**，未覆盖 **5862**，**127 个 mod 达 100%**（原 52 个）
- [ ] **方块/物品名 5862 条（有意不做）**：几乎全是 rechiseled（2418）· Design-n-Decor（904）·
      createframed（569）· tfmg（382）这类"材质+型制"式组合名（"Amethyst Beams Stairs"）。
      逐条翻是浪费 —— 它适合按词表规则化生成（材质词 × 型制词），且社区包更新会持续缩小这块。
      **触发条件**：有人真的抱怨方块名是英文，或界面块稳定运行一个版本之后再做。
- [ ] **11 条维度名待官方口径回改**（`travelerstitles`）：bracken / backrooms / ceilands /
      voidscape 等维度**无任何现存中译**（已扫过 224 个 jar 的 zh_cn），暂按意译/音译自定
      （妖精荒野 / 幽暗地域 / 灵薄狱 / 永恒星光 / 多弥斯 / 格拉奇姆…）。
      **触发条件**：拿到这些模组的官方中文名时回改这 11 条。

---

## D. 性能（等你的服务器就绪）

- [ ] **D1** 并发压测：N 个客户端下的 TPS/MSPT（本机内存只够 2~3 个，真实人数请在你那台机器上测）
- [ ] **D2** 复现排查 `Can't keep up! Running 4503ms or 90 ticks behind`（出现在 `save-all` 同步存档时，稳态无此问题）
- [ ] **D3** 设计稿 §12 的其余指标：袭击场景 ≥15 TPS、实体 ≤200、启动 ≤3 分钟

---

## E. 可选

- [ ] **E1** 组0 纯原版基线（没有它就无法回答"性能组到底省了多少"）——现在能自动跑
- [ ] **E2** 图纸生成器扩到 5 个模块（公共仓库 / 农田 / 熔炉阵列 + 功能占位）

---

## F. 军事 / 带兵打仗（2026-09-18 新增，`hundred-years-warfare`）

> 为什么整组后置：这一组**全部需要真服务端 + 至少两个客户端**（军队是双端必需、且指挥与分队是多人行为），
> 而且它牵动三根承重墙 —— 零散测会反复重开服务端。攒到 A 组那次一起做。
> 规格见 `DESIGN.md` §17.9。

- [ ] **F1 三套袭击会不会打架**（**最高优先**）：同时开 The Hordes（我们要按日历指令触发）、Undead Nights（拆墙+难度档）、
      本 mod 自己的袭击扩展 → 观察是否叠加成灾难；**先确认本 mod 的袭击能否关掉或延后**。
      判据：能只留 The Hordes 作排期引擎、Undead Nights 供拆墙能力、本 mod 供"敌军成建制"。若不能，就要在两者里做减法。
- [ ] **F2 第一人称指挥手感**（**这是砍掉俯视角后唯一依赖的操作面**）：指挥轮盘 + 指挥杖在**不开 RTS 模式**时的实际可用性
      —— 选中/编队/下令/撤退在混战中是否够快。判据：能不看教程完成"集结→列阵→冲锋→撤退"。
- [ ] **F3 队伍概念有没有变成三套**：本 mod 的"关系/团队系统" vs OPAC party vs FTB Teams
      —— 与 FTB Teams 那次同一个坑。判据：只需要一套队伍概念就能跑通任务共享 + 领地共有 + 军队分队。
- [ ] **F4 性能红线**：作者建议把实体渲染距离提到 150%，与 §12 的"敌人 ≤200 / 袭击期 ≥15 TPS"直接冲突
      —— 量出 1 支满编军队 + 一波袭击时的实体数与 MSPT。判据：不破 §12 红线，或明确记下破了多少。
- [ ] **F5 工人系统与 Create 的重叠**：本 mod 的工人/工作台（源自 Ancient Warfare 2）能做资源生产、运输、合成、存储
      —— 与时代 2 的主菜（Create）功能重叠。判据：定出**时代闸门**该怎么切（"先机械、后人力"），
      以及有没有配置项能延后工人系统（没有就只能靠"不发工作站材料"做软闸门）。
- [ ] **F6 供养机制能不能落地**（对应 `DESIGN.md` §17.7 空缺 #8）：兵力上限与国力的挂钩、军队的食物/装备/损耗消耗
      —— 目前**只有设计、没有实现手段**。判据：找到实现路径（配置 / 指令 / 脚本）；
      找不到就说明军队会退化成"暴兵推平一切"，那样宁可缩小军队的规模或直接砍掉。
- [ ] **F7 攻打据点的循环**（对应 §17.7 空缺 #9，`DESIGN.md` §17.9 (7)）：
      ① 据点**能不能真的"被打下来"**（守军清空后是否还有"占领"这件事，还是只是个空壳结构）；
      ② **重新占据**靠什么实现（原版的重新刷怪？还是我们自己用指令重置）；
      ③ 拔点的**减益**能不能落到"袭击频率/规模下降"上（The Hordes 的波次是我们要手动触发的，所以这一条大概率靠**我们的脚本**而不是 mod）。
      判据：能写出"清剿 → 减益 N 天 → 重占 → 再清剿"这个闭环的参数；写不出来就把它降级为"多几个可以打的地牢"，
      不要假装它是战略层。
- [ ] **F8 攻城的性能与体验**：军队 + 攻城器械 + 据点守军同时在场时的实体数与 MSPT
      （这是全包**单点实体数最高**的场景，比袭击波次更高）；以及攻城是否需要**服务端手工布置占领区**（op 工作量的现实评估）。

---

## N. voxy 超远视距（2026-09-22 起，**已暂停；jar 已从实例移除**）

> **为什么整组后置**：voxy 需要**人眼看画面**验收，而且它是**唯一一个不在 packwiz 清单里、
> 只存在于本机实例的 mod**（授权 ARR、不可分发）。它还会污染其它验证的结论（见 N0）。
> 详细设计与全部证据：`DESIGN-VISTA.md`、`DESIGN-PREGEN.md`、`DESIGN-LOD-SERVER.md`。

### N0. 为什么必须先移除它（**这不是猜测**）

1. **它改 Embeddium 的渲染类**：日志里 `Embeddium-MixinTaintDetector: Mod(s) [voxy] are
   modifying Embeddium class ... This instance is now tainted`。只要 voxy 在场，
   任何与 Embeddium/渲染相关的验证结论都不可信（例如 `CONFLICTS.md` C10 那条 Sodium 对比）。
2. **它挂钩进世界必经路径**：`LevelRenderer.createRenderer` → `voxy$reloadVoxyRenderer`。
   任何"世界加载/渲染"的验证都会多一个变量。
3. **它不在清单里**：packwiz 无法复现它，所以"整合包状态"的验证本来就不该带着它。
   ⇒ **移除后，后续验证跑在干净、可复现的包状态上。**

### N1. 已完成的部分（有据可查，可续做）

- [x] **自己编出了 NeoForge 1.21.1 版 voxy**（j-shelfwood `embeddium-compat` 分支）
      —— 用 Embeddium，**不需要换渲染器**。工具链：`tools/fetch_voxy_deps.py`（多线程下依赖到本地
      Maven 仓库，绕开本机到境外源仅 13-20 KB/s）+ `tools/build_voxy.py`（编排与打补丁），
      说明见 `tools/README-voxy-build.md`。
- [x] **修掉"进世界必崩"的根因**：移植版把纹理循环上界写成 16，而 MC 1.21.1 的
      `GlStateManager.TEXTURE_COUNT = 12` ⇒ `Index 12 out of bounds for length 12`。
      上游一律写 `i < 12`，**是移植引入的 bug**。已在构建脚本里打补丁（只改含 `_bindTexture`
      的 2 处），并经**字节码级验证**（`bipush 12`）。
- [x] **冒烟通过**：232 mod、**10G 堆**，`已进入世界，停留 120 秒 → graceful`，旧崩溃签名消失。

> ⚠️ **测试内存：至少 10G，不要按 5G 跑**（2026-09-22 用户指出）。
> 我一度用 `--xmx 5G` 跑，效果是"没能确认进入世界"——**那是我给少了，不是包的问题**。
> 这个包 231 个 mod 且**尚未做优化环节**，正常验证要给 10G 以上。
> 以后跑 `autotest.py` 一律 `--xmx 10G`（并确认机器有足够空闲内存）。
- [x] **在用户世界里确认过工作**：`新的世界 (5)` 里 voxy 数据库 1.98 MB
      （RocksDB + ZSTD），日志有 `Voxy render system created` 与 `[VoxyDiag] ingestAttempts`。

### N2. 未完成 / 待办

- [ ] **N2.1 远景是否真的达到预期**（人眼看画面）：进世界后远处能否看到 LOD 地形。
      **需要用户**。voxy 的 LOD 是边玩边长的，刚进世界远处是空的，要站一会儿或走一段。
- [ ] **N2.2 `serviceThreads` 调优有没有效果**：已把 `config/voxy-client.toml` 从 2 改成 6
      （机制：`serviceThreads` 是 voxy + Embeddium 的**总预算**，`target - builderThreads`，
      默认下 voxy 实得 **1 个线程**）。**改完没实测**。
- [ ] **N2.3 预生成范围与提速**：实测 ingest 吞吐约 **13 区块/秒**，而满视距需要 **105 万区块**
      ⇒ 整片预生成 22 小时起步，不现实。范围/耗时表见 `DESIGN-PREGEN.md` §5.1。
- [ ] **N2.4 服务端 LOD 分发**（**多人的正解**）：`LOD Server Support`（MIT，有 1.21.1/NeoForge）。
      已核实：**服务端不需要装 voxy**；客户端网络层是它自己的；与 voxy 靠反射对接
      （它解析的 6 个 voxy 类在我们移植版里全部存在）。
      **未验证**：LSS 官方测的是 Roxy + 原版 Fabric voxy + **Sodium**，我们走的是自编
      Embeddium 移植版 ⇒ 需实测 `/lss diag`。见 `DESIGN-LOD-SERVER.md`。
- [ ] **N2.5 授权边界**：voxy 是 **ARR / "Do not redistribute"**。
      **只在本地自用，不得对外分发**（整合包、公开下载都不行）。这一条不解决就不能进 `pack/`。

### N3. 怎么把它装回来（jar 已留档，一条命令）

产物与脚本都还在（`data/` 被 gitignore，不进仓库）：

```powershell
# 产物
D:\project\nation-pack\data\_voxyjar\voxy-0.2.9-alpha-embeddium-neoforge-1.21.1.jar   # 71.6 MB

# 重新装进实例
Copy-Item "D:\project\nation-pack\data\_voxyjar\voxy-0.2.9-alpha-embeddium-neoforge-1.21.1.jar" `
          "D:\game\PCL\.minecraft\versions\1.21.1-NeoForge_21.1.250\mods\"

# 要从源码重建（会重下依赖，约 85 MB）
python tools\fetch_voxy_deps.py --jobs 8
python tools\build_voxy.py

# 冒烟验证（用我的测试世界，**不要用用户的存档**）
# ⚠️ 至少 10G —— 231 个 mod 且未做优化，5G 会进不去世界
python tools\autotest.py --world scdev --xmx 10G --seconds 120
```

### N4. 硬规则（续做时必须遵守）

1. **不要把它放进 `pack/`** —— ARR 不可分发。它只能待在本机实例的 `mods/`。
2. **带它跑出来的验证结论要标注**（Embeddium 被 taint、渲染路径被挂钩）。
3. **冒烟测试一律用 `--world scdev`** —— `autotest.py` 的默认世界是用户的 `新的世界`，
   **绝不能用默认值**。
4. **内存一律 `--xmx 10G` 起** —— 231 个 mod 且未做优化环节，5G 会进不去世界
   （2026-09-22 我为此浪费了一轮，用户指出）。


---

## §O 任务书文案的**已知欠账**（2026-09-22 记，不是"已完成"）

用户明确说过**这一轮先不加厚文案**（原话：「暂时先不加厚任务文案了吧，你重点是去学习
本机上其他的整合包任务线怎么写的，这个任务量太少了」）。所以本轮把力气全用在
**条数与结构**上，文案层面留下两笔可量化的欠账：

| 类别 | 数量 | 含义 | 要不要修 |
|---|---|---|---|
| 只有标题与判据，没有 desc | **266** | 玩家看得到"做什么"，看不到"为什么做" | **类目章里这是设计使然**（ATM10 同款做法：不写"怎么做"也不写长篇）；主线/技艺章里应该补 |
| desc 里没有可执行动词 | **76** | 写了背景但没告诉玩家动作 | 该修：这是"不知所云"的温和版本 |
| 章缺一句概括（subtitle） | 1 | 读者靠标题猜这章干什么 | 该修 |

复核命令（**只看当次运行的输出，别读旧 JSON**）：

```powershell
python tools/text_audit_live.py            # 全量
python tools/text_audit_live.py --errors   # 只看错误级（当前 0）
```

当前基线（2026-09-22，40 章 / 1748 条）：**错误 0 · 警告 343 · 提示 106**。

**为什么把"只有标题"降为警告而不是错误**：类目章的存在意义是**覆盖面**（告诉玩家
"这个 mod 有什么"），给每条都写 2~3 句会把它变成文案海洋 —— 这正是
`PLAN-questbook.md` §五 与 ATM10 都刻意避免的。所以这条**只对非类目章**算欠账。