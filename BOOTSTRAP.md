# 开工清单：v0 最小可启动集合

> **v0 的目标（明确、可验收）**：能进游戏 · 能圈一块地 · 能建出第一台机械 · **TPS 有基线**。
> 就这四件。不做领地经济、不做任务书、不做威胁、不做图纸——那些是 v1 之后的事。
>
> 预计 **6~10 小时**（其中大半是下载与等待）。

---

## 零、三个先定的决定

### 1. 先单机，再上服务端

单机 = 客户端与服务端同进程，**端侧问题不会暴露、性能问题减半、排错快得多**。
等 v0 在单机跑通，再复制到服务端做「一致性验证」（同一套 mod，服务端能起、朋友能连）。

→ 这一条能省掉至少 3~5 小时的折腾。

### 2. 先实例目录，后 packwiz

- **现在**：直接用启动器建一个 1.21.1 + NeoForge 实例，jar 丢进 `mods/`。
- **等要发给朋友时**再决定要不要 packwiz（源码形态、git、增量更新、朋友自动拉更新）。
- **不必现在决定**：packwiz 可以把已有实例的文件索引进包（`packwiz refresh`），**迁移成本很低**，所以先跑通更重要。

> 但要知道代价：**不加 packwiz 的话，每次改 mod 都要重新分发完整包给朋友。**

### 3. CF-only 的 mod 会挡一下

包里有几个只走 CurseForge（FTB 系列、Twilight Forest）。packwiz 走 CF 需要 API key，**手动下 jar 则没这个问题**——这也是"先实例目录"更省事的原因之一。

---

## 一、环境准备（约 30 分钟）

| 项 | 做法 | 验收 |
|---|---|---|
| **Java 21** | 装一个 21（不用卸掉旧的，启动器里指定实例用哪个） | `java -version` 显示 21 |
| **实例** | 启动器建一个 **1.21.1** 实例，装 **NeoForge**（版本号无所谓，取最新稳定） | 不装任何 mod 能进原版游戏、能建世界 |
| **内存** | 分配给实例 **6~8 GB**（不要更多） | 启动不报内存错 |
| **基准存档** | 建一个**测试世界**（随便什么种子） | 能进去、能挖方块 |

> **纪律**：这一阶段用的世界是**测试世界**，随便炸。真正的世界等 mod 稳定后再开——**别拿未来的主世界做实验**。

---

## 二、五组清单（一次只加一组，每组都要验证）

### 组 0 · 纯加载器（无 mod）

**验证**：能进主菜单 → 能进测试世界 → `logs/latest.log` 无 `ERROR`。
**记基线**：启动耗时。

### 组 1 · 性能核心（11 个，低风险）

| mod | slug | 作用 |
|---|---|---|
| Embeddium | `embeddium` | 渲染 |
| Lithium | `lithium` | 服务端逻辑 |
| FerriteCore | `ferrite-core` | 内存 |
| ModernFix | `modernfix` | 加载/修复 |
| EntityCulling | `entityculling` | 实体剔除 |
| ImmediatelyFast | `immediatelyfast` | 界面/包 |
| Clumps | `clumps` | 实体堆积 |
| ServerCore | `servercore` | 服务端通用 |
| **Neruina** | `neruina` | **崩溃守护**（拦下坏实体，避免全服回档） |
| spark | `spark` | 诊断（掉 TPS 时先问"谁在卡"） |
| Chunky | `chunky` | 预生成 |

**为什么先装这一组**：它们是**基线的一部分**。之后每加一组都要和这里的数字对照——**性能组晚装，你就没有可比基线**。

**验证**：能启动 → 进世界 → 记基线（帧数 / TPS / 启动耗时 / 内存）。

### 组 1.5 · 三个"稍重"的性能 mod（逐个加）

| mod | slug | 为什么要单独加 |
|---|---|---|
| MoreCulling | `moreculling` | 剔除类，可能与其他渲染 mod 交互 |
| ScalableLux | `scalablelux` | **替换光照引擎**，改动面比前一组大 |
| C2ME | `c2me-neoforge` | 区块生成并发；**⚠️ 它是 RoadWeaver 的取舍项**（作者明写 2.1.0 及以前不兼容） |

**纪律**：这三个**一个一个加**，每加一个启动验证 + 记基线。C2ME 装之前先想清楚：如果之后要用世界路网，就得在这里做取舍（**建议保 C2ME，路网留到第二季或等作者修复**）。

### 组 2 · Create（自动化 + 图纸）

| mod | slug | 说明 |
|---|---|---|
| Create | `create` | 机械 + **Schematicannon（图纸打印机）** + 物流 |

**注意**：Create 有前置（按 mod 页面一起装）；1.21.1 上取 **Create 6**。

**验证 + 一次关键实测**（这一步别跳过）：
1. 能进游戏 → 造一台最简机械（例如手摇曲柄 + 传送带）跑起来；
2. 🔬 **把 `tools\out\nbt\wall_ironhold.nbt` 放进实例的 `schematics/` 目录**，用 **Schematicannon** 对着放一次。

> **这是那个 10 分钟的关键实测**：Create 认不认我们生成的 `.nbt`。
> **认** → 图纸库可以自动批量产出（省 10~15 小时）；
> **不认** → 改走 WorldEdit + `.schem`（退路现成）。
> 三条结果与对应做法见 `tools/README.md` 第六节。

### 组 3 · 领地与权限（OPAC）

| mod | slug | 说明 |
|---|---|---|
| Open Parties and Claims | `open-parties-and-claims` | 队伍 + 领地 + 细粒度权限 |

**验证**：能建队伍 → 能圈一块地 → 别人（开第二个账号或换单人测试）进不来。

### 组 4 · 一个结构 mod（**世界生成首次入场，最可能出问题的一组**）

| mod | slug |
|---|---|
| YUNG's Better Dungeons | `yungs-better-dungeons` |

**为什么只装一个**：世界生成类 mod 是冲突重灾区（结构重叠、生成密度、地形拼接）。**先装一个探路**，确认没问题再加第二个、第三个。

**验证**：新开一个世界 → 用 `/locate structure` 找一个结构 → 传送过去看它长得对不对 → 看日志有无生成错误。

⚠️ **从这一组开始，测试世界要重开**（已生成区块不会补上新结构）。

### 组 5 · 中文显示（视觉三件里的前两件，低风险）

| 项 | 做法 |
|---|---|
| 中文界面 | 装社区汉化资源包（❓版本要与 mod 匹配，装前先看适用版本） |
| 中文字体 | 装一个中文字体资源包（任务书/成书/告示牌全是中文） |

**验证**：Create 的界面、OPAC 的界面显示中文；游戏内文字不发虚。

> 统一配色规范（第三件）**不是现在做的**——它是建筑层面的规范，等图纸阶段再定。

---

## 三、①→⑤ 每组都要做的四件事

1. **备份 mods 列表**：把 `mods/` 目录文件名存一份（或 `git commit`）→ **回退只需要一秒**；
2. **启动验证**：进主菜单 → 进世界 → 看 `logs/latest.log` 有无 `ERROR`；
3. **记基线**（见下节表格）；
4. **记录结论**：发生了什么、有没有新 WARN、这一组能不能留。

---

## 四、基线记录（v0 的核心产出之一）

建一个 `baseline.csv`，每组一行：

```csv
日期,阶段,mod数,启动耗时s,空闲帧数,TPS,内存峰值GB,新增内容,结论
2026-09-15,组0 纯加载器,0,41,,20.0,2.1,无,通过
2026-09-15,组1 性能核心,11,63,240,20.0,3.4,11个性能mod,通过（启动+22s）
```

**对照目标**（来自 `DESIGN.md` 第十二节）：

| 指标 | 目标 |
|---|---|
| TPS（常态） | **≥ 19.5** |
| MSPT（常态） | ≤ 30ms |
| 启动时间 | **≤ 3 分钟**（超过 5 分钟要查） |
| 内存 | 6~8GB（**配 GC，不能只调 Xmx**） |
| 敌人 / 常驻实体 | ≤ 200 / ≤ 300 |

**怎么看 TPS**：装 `spark` 后 `/spark tps`；或看 F3 的 `ms tick`（50ms = TPS 20）。

---

## 五、失败处置（照着做，别乱试）

| 现象 | 先看哪 | 处置 |
|---|---|---|
| **启动崩溃** | `crash-reports/` 最新一份 → 从堆栈**自下而上找第一个属于 mod 的帧** | 二分最近新增的那一组（一次只加一组的价值就在这里） |
| **启动卡住 / 黑屏** | `logs/latest.log` 尾部 | 常见三种：依赖缺失、内存不足、Java 版本不对 |
| **进世界就崩** | 同上 | 多半是世界生成类冲突 → **移除最近加的世界生成 mod** |
| **加载几秒后退出** | 内存设置 | 分配 6~8GB；不要超过物理内存的一半 |
| **TPS 低于 19.5** | `/spark tps` + `/spark profiler` | **先定位"谁在卡"，不要先加内存**（最常见也最无效的反应） |
| **连续两轮解决不了** | — | **整组回退到上一个通过状态**，然后单独重试其中一个 mod |

---

## 六、v0 完成的定义（Definition of Done）

四件事全过，v0 就算完成：

- [ ] 能进游戏，且**从选这个实例到进世界 ≤ 3 分钟**
- [ ] 能圈一块地（OPAC 生效：别人改不了）
- [ ] 能建出第一台机械（Create 跑起来）
- [ ] `baseline.csv` 里有 **6 行以上**记录，且 TPS ≥ 19.5
- [ ] 🔬 Create 认不认 `.nbt` 的结论已记录（认 / 不认 / 认但难看）

**v0 之后才是 v1**：图纸闭环（Portable Blueprints）· 任务书（**FTB Quests**，见 `MODLIST.md`）· 威胁（The Hordes + Undead Nights）· 回滚（GriefLogger + GLRA）· 备份（Simple Backups）· 时代碑与一页约定。

---

## 七、这一阶段**不要**装的东西（避免一次引入太多变量）

| 不装 | 为什么 |
|---|---|
| RoadWeaver（世界路网） | 与 C2ME 的兼容未解决，且它有"不能已加载区块生成"的运维约束 |
| 地形/群系扩充（Terralith） | **不是"未核实"了**——`terralith` + 前置 `lithostitched` 已定（接口核实 1.21.1 NeoForge，RoadWeaver 正文点名兼容）。这里不装的理由改成：**v0 是技术验证世界，不该引入地形变量**；而正式世界**必须在建世界前就位**（不能后加到旧世界，也不能移除） |
| Sculk Horde | 第二季内容，配置极多、性能要求高 |
| Illager Invasion | 已降级为可选内容 mod，不影响 v0 |
| FTB Quests 系列 | 任务书等 v1（载体已定为 FTB Quests，仅 CF 分发；v0 只验技术栈，不引任务内容） |
| The Hordes / Undead Nights | 威胁是 v1 的事；它们会引入大量实体，会污染性能基线 |
| 汉化之外的美化类 mod | 一切"看起来更好但增加变量"的东西都往后放 |
