# 待定设计（**故意先不定**，将来再聊）

> **用法**：这里只放"已经讨论过、但故意搁置"的设计问题。每条必须写清四件事：
> ① **已定了什么** ② **还没定什么** ③ **我的倾向与理由** ④ **它阻塞什么**（= 什么时候必须回来拍板）。
> 一旦拍板，就搬进 `NATIONS.md`（mod 侧）或 `DESIGN.md`（包侧），并**从这份文件里删掉**。
>
> **纪律**：不阻塞的部分一律照常推进 —— 搁置不等于停工。

---

## 1. 一国一城 vs 一国多城（2026-09-19 讨论后搁置）

**已定**
- 占领后：**都城归玩家**、**百姓留在原城**、**资源进该城**、**城池继续产出**
- **每座城有资源存储上限** —— 这是"想囤更多就得占更多城"的驱动力，也是远征从"可打"变成"必须打"的关键

**没定**
- 一个 NPC 国家是**一座城**还是**多座城**

**已评估的难度（四层拆解，代价差一个数量级）**

| 层 | 内容 | 一国一城 | 一国多城 | 差距 |
|---|---|---|---|---|
| ① core 数据模型 | `City`{位置/等级/资源/上限/设施/归属} + 进 `WorldState` + 迁移 + 编解码 | 便宜 | 便宜 | 几乎没差别 |
| ② core 规则 | 国力合成、每城上限、占领后继续产出、易主 | 便宜 | 便宜 | "对列表求和" vs "对对象取值" |
| ③ **世界实体化** | 选址（非海洋/坡度/无结构/无主）+ 平台化 + 放模板 + 百姓 + 守军 | 12 座 | 12 国 × N 座 | **唯一真贵的地方**：工作量 × N；节流队列（每结算最多 1 处 + 间隔 ≥5 分钟）被放大；世界会被铺满，性能与观感都要重估 |
| ④ 叙事与呈现 | 情报册/周报条目、书里的排版 | 12 条 | 12×N 条 | 中等：信息量翻倍 |

**我的倾向：B-lite —— "多城"不必等于"多实体"**
- **数据上**：每国 N 座城（都城 1 + 边镇若干），每座都有位置/等级/资源/上限/设施/归属
- **世界里**：**只有都城实体化**（沿用现有设计），**边镇只是数字上的城** —— 出现在情报与地图上、提供资源上限、随战争/外交结果易主，但没有建筑
- 这样"国力三维 / 每城上限 / 占领后产出 / 夺城打实体"**全部成立**，而实体化成本与现设计**完全相同**
- **代价**：边镇没有实体 ⇒ 不能"带兵走过去打下来"，只能通过国家间战争/外交/吞并易主。
  这与"零实体、抽象信息流"的既定原则一致；若你要"亲自打边镇"，就必须真做多实体，且城市总数要压在个位数

**阻塞什么**：`City` 数据模型、以及"国土面积"这一维国力的算法。
**不阻塞**：M1 收尾（环境判定 + 幂等决策）、计划 4 core 侧的数据层/文本/周报/国书/图纸授权。

---

## 2. 国力的合成方式（2026-09-19 讨论后搁置）

**已定**：国力是**多维**的 —— **城内兵力**（平时不渲染、打仗才用到）+ **国土面积** + **城内资源与设施等级**（商店几级、武器店几级这类）

**没定**
- 三维怎么**合成一个数**（权重）
- 是否**分场景用不同维度**（外交谈判用哪个？战争结算用哪个？）

**我的倾向**：三维修持为三个**读数**，再给一个**文档化的合成公式**只用于"需要一个标量"的地方（外交成功率）。
不把三个数揉成一个字段，否则"兵力强但地小"这种区分会丢。

**阻塞什么**：`NATIONS.md` §十二 外交成功率里的 `你方发展度` 项 ——
现在 core 的做法是"由调用方传，不给就按 0（这项不参与）"，也就是外交那一整套在数值上是空转的。

---

## 3. 城内资源的分类与上限公式（2026-09-19 讨论后搁置）

**已定**：**食物与生产资源分开**（这是最基本的两类）；占领后城池**继续产出**

**没定**
- 完整分类（除了食物/生产资源还有哪些）
- **每类上限怎么定**（按城等级？按设施等级？）
- 各类的**产出速率**怎么定

**我的倾向**：分类先做成**可扩展的类别表**（数据驱动，不写死在枚举里），
上限 = `城等级 × 基础容量 + 设施加成`，产出速率按"城池被占后仍按原速率跑"实现。

**阻塞什么**：内容层数据文件、"该占哪座城"的决策界面、以及国力第③维的算法。

---

## 4. 边镇要不要有实体（B-lite 的那个权衡，2026-09-19 搁置）

**没定**：玩家是否要能"亲自带兵走过去打下边镇"

**我的倾向**：**先不要**（与"零实体"原则一致）。等计划 5 的夺城**真机跑通一次**、
你有了实机手感再定 —— 那时"值不值得为边镇做实体"才有判断依据，而不是现在赌一把。

**阻塞什么**：计划 5 的工作量（多实体 = 成本 × 城市数）。**不阻塞**计划 5 本身的设计。

---

## 5. 终局（2026-09-19 讨论后搁置）

**已定**：**等写任务线时再细聊**

**没定**：六个时代走完之后 —— 继续无限玩 / 触发一个终局事件 / 就此结束

**阻塞什么**：任务书（`DEFERRED.md` H1）与旧设计里"毕业后进入**无尽纪元**"那条的落实。

---

## 附：本轮**已经定下来**的（写在这里，免得以后再被问一遍）

| 项 | 决定 |
|---|---|
| 时间口径 | **全服累计在线**，任何一人在线就烧全服预算；曲线翻倍 → **300 小时**（24/40/50/50/60/76）。**已实现并同步** |
| PvP | **不做**。推论：**所有玩家算"一方"** —— 外交态度是"对全服"的、国力是共同的、占领来的城市与资源**公有**；玩家之间的领地纠纷交给 OPAC / FTB Teams，本系统不碰 |
| 占领后 | 都城归玩家、百姓留原城、资源进该城、**继续产出**、**每城有存储上限** |
| 计划 4 的 mc 层 | 方块 / 实体 / 界面**先用占位内容**，形态之后由你定 |
| 第三方 mod 配置 | **允许改（魔改）**，并且要另出一份**"魔改潜力清单"** |
